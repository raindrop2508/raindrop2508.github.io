<?xml version="1.0" encoding="UTF-8" ?>
<?xml-stylesheet type="text/xsl" href="/linen-theme/css/rss-zh.xsl"?>
<rss version="2.0">
<channel>
  <title>Raindrop Blog</title>
  <link>https://raindrop2508.github.io</link>
  <description>Mark 的技术与生活记录</description>
  <lastBuildDate>Sat, 18 Apr 2026 13:44:27 GMT</lastBuildDate>
  <image>
      <url>https://raindrop2508.github.io/icons/avatar.png</url>
      <title>Raindrop Blog</title>
      <link>https://raindrop2508.github.io</link>
    </image>
  
    <item>
      <title></title>
      <link>https://raindrop2508.github.io/2026/04/18/tech/Hexo_GitHub_Pages_Actions_CICD/</link>
      <guid>https://raindrop2508.github.io/2026/04/18/tech/Hexo_GitHub_Pages_Actions_CICD/</guid>
      <pubDate>4 月 18 日，2026</pubDate>
      <description><![CDATA[<hr>
<p>title: Hexo + GitHub Pages + GitHub Actions：源码私有、站点公开的 CI&#x2F;CD 教程<br>date: “2026-04-18T12:00:00+08:00”<br>excerpt: 用一个仓库存源码（可私有），另一个仓库存静态产物（公开 Pages），并用 Actions 自动构建与部署<br>cover: &#x2F;linen-theme&#x2F;img&#x2F;pattern-randomized.jpg<br>categories:</p>
<ul>
<li>tech</li>
</ul>
<hr>
<blockquote>
<p>最近正好想要使用HEXO在部署一次自己的博客，但每次都要受到提交运行太麻烦了，好在GitHub Actions可以帮助我们自动化这个过程，而且目前还是免费的。</p>
</blockquote>
<h2 id="最终目标"><a href="#最终目标" class="headerlink" title="最终目标"></a>最终目标</h2><ul>
<li>Hexo 源码仓库可以保持私有（文章源文件、主题配置、脚本都在这里维护）</li>
<li>对外访问的站点仓库保持公开（只存放构建后的静态文件，给 GitHub Pages 发布）</li>
<li>只要我 push 源码，GitHub Actions 就自动构建并把 <code>public/</code> 推送到 Pages 仓库</li>
</ul>
<p>下面按这个思路把整套流程梳理一遍（也适用于“源码公开&#x2F;私有”两种情况）。</p>
<h2 id="方案概览"><a href="#方案概览" class="headerlink" title="方案概览"></a>方案概览</h2><h3 id="仓库职责"><a href="#仓库职责" class="headerlink" title="仓库职责"></a>仓库职责</h3><ul>
<li>源码仓库（例如：<code>hexo-blog</code>，可私有）<ul>
<li>存放：Hexo 项目源码（<code>source/_posts</code>、主题、<code>_config.yml</code>、<code>package.json</code> 等）</li>
<li>负责：GitHub Actions 构建与部署</li>
</ul>
</li>
<li>Pages 仓库（用户站点）：<code>&lt;你的用户名&gt;.github.io</code>（必须公开）<ul>
<li>存放：Hexo 生成的静态文件（<code>public/</code> 目录里的 HTML&#x2F;CSS&#x2F;JS&#x2F;图片等）</li>
<li>负责：开启 GitHub Pages 对外访问</li>
</ul>
</li>
</ul>
<h3 id="自动化流程"><a href="#自动化流程" class="headerlink" title="自动化流程"></a>自动化流程</h3><ol>
<li>你在源码仓库写文章并 push</li>
<li>GitHub Actions 触发：安装依赖 → <code>hexo generate</code> 生成静态站点到 <code>public/</code></li>
<li>Actions 把 <code>public/</code> 推送到 Pages 仓库指定分支（通常 <code>main</code>）</li>
<li>GitHub Pages 直接从 Pages 仓库发布</li>
</ol>
<h2 id="费用与计费简单说明"><a href="#费用与计费简单说明" class="headerlink" title="费用与计费简单说明"></a>费用与计费简单说明</h2><p>具体计费情况可以查看 <a href="https://docs.github.com/zh/billing/concepts/product-billing/github-actions">GitHub Actions 计费页面</a> 了解详情。</p>
<ul>
<li>公共仓库的 Actions 通常免费</li>
<li>私有仓库使用 GitHub 托管运行器有免费额度，超出可能计费（分钟数&#x2F;存储与账号计划相关）</li>
<li>缓存&#x2F;工件存储也可能产生占用，删除后不会继续累积</li>
</ul>
<p>对个人博客来说，构建频率不高、站点不大时，通常不会超过免费额度。想更省：减少触发频率、开启依赖缓存、不要上传大工件。</p>
<h2 id="前置准备"><a href="#前置准备" class="headerlink" title="前置准备"></a>前置准备</h2><ul>
<li>GitHub 账号</li>
<li>本地安装 Node.js（建议 LTS）与 Git</li>
<li>一个可正常本地运行的 Hexo 项目（本文默认依赖已在 <code>package.json</code> 中）</li>
</ul>
<h2 id="创建并配置两个仓库"><a href="#创建并配置两个仓库" class="headerlink" title="创建并配置两个仓库"></a>创建并配置两个仓库</h2><h3 id="1-创建-Pages-仓库（公开）"><a href="#1-创建-Pages-仓库（公开）" class="headerlink" title="1) 创建 Pages 仓库（公开）"></a>1) 创建 Pages 仓库（公开）</h3><ol>
<li>新建仓库：仓库名必须是 <code>&lt;你的用户名&gt;.github.io</code></li>
<li>仓库可见性：Public</li>
<li>Settings → Pages<ul>
<li>Source：Deploy from a branch</li>
<li>Branch：<code>main</code> &#x2F; <code>(root)</code>（推荐）</li>
</ul>
</li>
</ol>
<h3 id="2-创建-准备源码仓库（可私有）"><a href="#2-创建-准备源码仓库（可私有）" class="headerlink" title="2) 创建&#x2F;准备源码仓库（可私有）"></a>2) 创建&#x2F;准备源码仓库（可私有）</h3><p>把 Hexo 源码提交到源码仓库即可，文章建议放在：</p>
<ul>
<li><code>source/_posts/</code>（Hexo 默认文章目录）</li>
</ul>
<h2 id="配置-GitHub-Actions：构建后推送到另一个仓库"><a href="#配置-GitHub-Actions：构建后推送到另一个仓库" class="headerlink" title="配置 GitHub Actions：构建后推送到另一个仓库"></a>配置 GitHub Actions：构建后推送到另一个仓库</h2><p>关键点是：Actions 在源码仓库里构建完，需要“跨仓库写入” Pages 仓库。</p>
<p>常见做法：</p>
<ul>
<li>Fine-grained PAT（更直观）</li>
<li>Deploy Key（SSH，适合更严格的权限隔离）</li>
</ul>
<p>这里使用 Fine-grained PAT。</p>
<h3 id="1-创建-Fine-grained-PAT"><a href="#1-创建-Fine-grained-PAT" class="headerlink" title="1) 创建 Fine-grained PAT"></a>1) 创建 Fine-grained PAT</h3><p>GitHub：Settings → Developer settings → Personal access tokens → Fine-grained tokens：</p>
<ol>
<li>选择只授权给你的 Pages 仓库（<code>&lt;你的用户名&gt;.github.io</code>）</li>
<li>Permissions 至少给：<ul>
<li>Contents: Read and write</li>
</ul>
</li>
<li>生成后复制保存（只显示一次）</li>
</ol>
<h3 id="2-在源码仓库添加-Secrets"><a href="#2-在源码仓库添加-Secrets" class="headerlink" title="2) 在源码仓库添加 Secrets"></a>2) 在源码仓库添加 Secrets</h3><p>源码仓库 → Settings → Secrets and variables → Actions → New repository secret：</p>
<ul>
<li><code>PAGES_TOKEN</code>：填写上一步生成的 PAT</li>
</ul>
<h3 id="3-添加工作流文件"><a href="#3-添加工作流文件" class="headerlink" title="3) 添加工作流文件"></a>3) 添加工作流文件</h3><p>在源码仓库创建文件：<code>.github/workflows/deploy-to-pages-repo.yml</code>。</p>
<p>如果你使用 pnpm（推荐锁定依赖，构建更可复现），可以用下面这个版本（把仓库名替换成你自己的）：</p>
<pre><code class="language-yaml">name: Build and Deploy Hexo

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: hexo-deploy
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: &quot;20.19.0&quot;

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
          run_install: false

      - name: Get pnpm store directory
        id: pnpm-cache
        shell: bash
        run: |
          echo &quot;STORE_PATH=$(pnpm store path)&quot; &gt;&gt; $GITHUB_OUTPUT

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles(&#39;**/pnpm-lock.yaml&#39;) }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build site
        run: |
          pnpm run clean
          pnpm run build

      - name: Deploy to Pages repo
        uses: peaceiris/actions-gh-pages@v4
        with:
          personal_token: ${{ secrets.PAGES_TOKEN }}  #.后面的值要与你设置的Token名词保持一致
          external_repository: {你的公开仓库地址}
          publish_branch: main
          publish_dir: ./public
          commit_message: &quot;deploy: ${{ github.sha }}&quot;
</code></pre>
<p>说明：</p>
<ul>
<li><code>concurrency</code> 用来避免连续 push 时部署互相覆盖</li>
<li><code>publish_dir</code> 指向 Hexo 生成目录 <code>public</code></li>
<li><code>cache: pnpm</code> + <code>--frozen-lockfile</code> 用来保证依赖安装更稳定</li>
</ul>
<h2 id="Pages-仓库设置检查"><a href="#Pages-仓库设置检查" class="headerlink" title="Pages 仓库设置检查"></a>Pages 仓库设置检查</h2><p>在 <code>&lt;你的用户名&gt;.github.io</code> 仓库里确认：</p>
<ul>
<li>Settings → Pages<ul>
<li>Source：Deploy from a branch</li>
<li>Branch：与你工作流推送的分支一致（上面示例是 <code>main/(root)</code>）</li>
</ul>
</li>
</ul>
<p>第一次部署通常需要等待一会儿生效，地址是：</p>
<ul>
<li><code>https://&lt;你的用户名&gt;.github.io</code></li>
</ul>
<h2 id="日常发布流程"><a href="#日常发布流程" class="headerlink" title="日常发布流程"></a>日常发布流程</h2><ol>
<li>写文章：<code>source/_posts/xxx.md</code></li>
<li>本地预览（可选）：</li>
</ol>
<pre><code class="language-bash">pnpm run clean
pnpm run build
pnpm run server
</code></pre>
<ol>
<li>push 到源码仓库 <code>main</code></li>
<li>等 Actions 跑完后访问站点即可</li>
</ol>
<h2 id="常见问题排查"><a href="#常见问题排查" class="headerlink" title="常见问题排查"></a>常见问题排查</h2><h3 id="Actions-成功但页面不更新"><a href="#Actions-成功但页面不更新" class="headerlink" title="Actions 成功但页面不更新"></a>Actions 成功但页面不更新</h3><ul>
<li>检查 Pages 设置的分支&#x2F;目录是否与工作流一致（例如 <code>main/(root)</code>）</li>
<li>检查 Pages 仓库是否收到最新提交（Actions 会推送一个 commit）</li>
<li>等待 Pages 缓存刷新或清理浏览器缓存</li>
</ul>
<h3 id="部署报-403-permission-denied"><a href="#部署报-403-permission-denied" class="headerlink" title="部署报 403 &#x2F; permission denied"></a>部署报 403 &#x2F; permission denied</h3><ul>
<li>PAT 权限不足：确认 token 对 Pages 仓库有 Contents 写权限</li>
<li>PAT 过期&#x2F;撤销：重新生成并更新 <code>PAGES_TOKEN</code></li>
<li><code>external_repository</code> 写错：必须是 <code>用户名/用户名.github.io</code></li>
</ul>
<p><strong>新仓库中Index.js文件多为0KB，导致页面白屏</strong></p>
<h2 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h2><ul>
<li><a href="https://docs.github.com/zh/billing/concepts/product-billing/github-actions">https://docs.github.com/zh/billing/concepts/product-billing/github-actions</a></li>
<li><a href="https://docs.github.com/zh/pages">https://docs.github.com/zh/pages</a></li>
<li><a href="https://github.com/peaceiris/actions-gh-pages">https://github.com/peaceiris/actions-gh-pages</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/60578464">https://zhuanlan.zhihu.com/p/60578464</a></li>
<li><a href="https://www.cnblogs.com/xango/p/18909673">https://www.cnblogs.com/xango/p/18909673</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/161969042">https://zhuanlan.zhihu.com/p/161969042</a></li>
<li><a href="https://zhangmengwei.com/2025/07/18/05-hexo-cicd-automation/">https://zhangmengwei.com/2025/07/18/05-hexo-cicd-automation/</a></li>
</ul>
]]></description>
    </item>
    <item>
      <title>读书笔记：第一行代码Android（第三版）</title>
      <link>https://raindrop2508.github.io/2026/02/26/tech/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0%EF%BC%9A%E7%AC%AC%E4%B8%80%E8%A1%8C%E4%BB%A3%E7%A0%81Android%EF%BC%88%E7%AC%AC%E4%B8%89%E7%89%88%EF%BC%89/</link>
      <guid>https://raindrop2508.github.io/2026/02/26/tech/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0%EF%BC%9A%E7%AC%AC%E4%B8%80%E8%A1%8C%E4%BB%A3%E7%A0%81Android%EF%BC%88%E7%AC%AC%E4%B8%89%E7%89%88%EF%BC%89/</guid>
      <pubDate>2 月 26 日，2026</pubDate>
      <description><![CDATA[<table>
<thead>
<tr>
<th><strong>书名</strong></th>
<th>第一行代码：Android（第二版&#x2F;第三版）</th>
</tr>
</thead>
<tbody><tr>
<td><strong>作者</strong></td>
<td>郭霖</td>
</tr>
<tr>
<td><strong>状态</strong></td>
<td><font style="background:#DBF1B7;color:#2A4200">已读完</font></td>
</tr>
<tr>
<td><strong>简介</strong></td>
<td>Android + Java 开发</td>
</tr>
</tbody></table>
<p><a href="https://book.douban.com/subject/26915433/">第一行代码：Android（第2版）</a></p>
<p><strong>安卓系统架构</strong></p>
<ul>
<li>Linux 内核层：硬件驱动</li>
<li>系统运行库层：特性支持，如 SQLite 库</li>
<li>应用框架层：构建应用程序需要的 API ，如四大组件</li>
<li>应用层：APP</li>
</ul>
<p>安卓四大组件：<strong>活动（Activity）、服务（Service）、广播（Broadcast Receiver）和内容提供器（Content Provider）</strong>。</p>
<h1 id="项目结构"><a href="#项目结构" class="headerlink" title="项目结构"></a>项目结构</h1><p>java 文件夹: 项目代码</p>
<p>res 文件夹： 项目图片、布局、字符串等资源。</p>
<pre><code>- Drawable 文件夹：用于存放各种类型的图形资源，包括但不限于 PNG、JPEG、GIF 图片，XML 定义的可绘制资源（如形状、选择器等），以及九宫格图（9-patch）。这些资源可以被用来作为背景、图标、分割线等。
- Mipmap 文件夹：主要用于存放应用启动器图标。使用 mipmap 文件夹有助于确保应用程序图标在不同分辨率和屏幕密度的设备上都能获得最佳显示效果。
- Layout 文件夹：页面布局文件
- values 文件夹：字符串、样式、颜色等配置
</code></pre>
<p>build.gradle（最外层的）  app 模块的 gradle 构建脚本</p>
<p><font style="color:rgb(38, 38, 38);">build.gradle（app 内的） 编译版本，依赖</font></p>
<p>AndroidManifest.xml  项目的配置文件，四大组件都要在此注册。</p>
<p>assets: 大体积资源文件，静态文件等</p>
<h1 id="活动（Activity）"><a href="#活动（Activity）" class="headerlink" title="活动（Activity）"></a>活动（Activity）</h1><ul>
<li><strong>设置主活动:</strong> 在 activity 标签中加入 intent-filter 标签等声明。</li>
<li>**添加菜单：**构建 menu 资源文件并在活动中引用</li>
</ul>
<h2 id="Intent"><a href="#Intent" class="headerlink" title="Intent"></a>Intent</h2><p>通过使用 Intent 进行活动的跳转、启动服务、发送广播等。</p>
<ul>
<li>显示 Intent： 显式的指出要启动的活，<ul>
<li>启动活动时传参: putExtra -&gt; getStringExtra 在。</li>
<li>返回数据给上个活动：  startActivityForResult 启动新活动，使用 onActivityResult 接受新活动销毁时返回的参数</li>
</ul>
</li>
<li>隐式 <font style="color:rgb(38, 38, 38);">Intent： 不指定具体目标组件（Activity、Service等），而是通过</font><strong>描述操作意图</strong><font style="color:rgb(38, 38, 38);">让系统匹配合适组件来响应的机制。</font><ul>
<li><strong>Action</strong>：要执行的操作（如<code>ACTION_VIEW</code>、<code>ACTION_SEND</code>）</li>
<li><strong>Data</strong>：操作的数据URI和类型（如<code>tel:123456</code>、<code>http://...</code>）</li>
<li><strong>Category</strong>：组件的附加信息（如<code>CATEGORY_BROWSABLE</code>）</li>
</ul>
</li>
</ul>
<blockquote>
<p>每个 Intent 只能指定一个 action，但能指定多个 category。<action>标签中指明了当前Activity可以响应的 action， 而<category>标签则包含了 一些附加信息，更精确地指明了当前Activity能够响应的Intent中还可能带有的category。  </p>
</blockquote>
<pre><code class="language-kotlin">// 隐式Intent - 通过Action和数据类型描述意图
val implicitIntent = Intent().apply {
    action = Intent.ACTION_VIEW
    data = Uri.parse(&quot;https://www.example.com&quot;)
}
startActivity(implicitIntent)
</code></pre>
<p>隐式 Intent 的响应</p>
<pre><code class="language-xml">&lt;activity android:name=&quot;.MyShareActivity&quot;&gt;
  &lt;!-- 声明Intent过滤器 --&gt;
  &lt;intent-filter&gt;
    &lt;!-- 定义Action --&gt;
    &lt;action android:name=&quot;android.intent.action.SEND&quot; /&gt;

    &lt;!-- 定义数据类型 --&gt;
    &lt;data android:mimeType=&quot;text/plain&quot; /&gt;

    &lt;!-- 定义类别 --&gt;
    &lt;category android:name=&quot;android.intent.category.DEFAULT&quot; /&gt;
  &lt;/intent-filter&gt;
&lt;/activity&gt;
</code></pre>
<p>在Activity中处理隐式Intent</p>
<pre><code class="language-kotlin">class MyShareActivity : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_share)

        // 检查Intent的Action
        when (intent?.action) {
            Intent.ACTION_SEND -&gt; {
                if (intent.type == &quot;text/plain&quot;) {
                    handleSendText(intent)
                } else if (intent.type?.startsWith(&quot;image/&quot;) == true) {
                    handleSendImage(intent)
                }
            }
        }
    }

    private fun handleSendText(intent: Intent) {
        val sharedText = intent.getStringExtra(Intent.EXTRA_TEXT)
        // 处理文本分享
        textView.text = sharedText
    }

    private fun handleSendImage(intent: Intent) {
        val imageUri = intent.getParcelableExtra&lt;Uri&gt;(Intent.EXTRA_STREAM)
        // 处理图片分享
        imageView.setImageURI(imageUri)
    }
}
</code></pre>
<h3 id="返回数据给上一个Activity"><a href="#返回数据给上一个Activity" class="headerlink" title="返回数据给上一个Activity"></a>返回数据给上一个Activity</h3><p>使用 <code>startActivityForResult</code>方法</p>
<pre><code class="language-kotlin">// 第一个Activity
class FirstActivity : AppCompatActivity() {
    
    companion object {
        const val REQUEST_CODE = 1001
        const val EXTRA_KEY = &quot;result_data&quot;
    }
    
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_first)
        
        val button = findViewById&lt;Button&gt;(R.id.button)
        val resultTextView = findViewById&lt;TextView&gt;(R.id.result_text_view)
        
        button.setOnClickListener {
            val intent = Intent(this, SecondActivity::class.java)
            startActivityForResult(intent, REQUEST_CODE)
        }
    }
    
    override fun onActivityResult(requestCode: Int, resultCode: Int, data: Intent?) {
        super.onActivityResult(requestCode, resultCode, data)
        
        if (requestCode == REQUEST_CODE) {
            val result = data?.getStringExtra(EXTRA_KEY) ?: &quot;没有数据返回&quot;
            
            when (resultCode) {
                RESULT_OK -&gt; {
                    // 用户主动提交
                    Toast.makeText(this, &quot;提交成功: $result&quot;, Toast.LENGTH_SHORT).show()
                }
                RESULT_CANCELED -&gt; {
                    // 用户按返回键
                    Toast.makeText(this, &quot;已取消: $result&quot;, Toast.LENGTH_SHORT).show()
                }
            }
        }
    }
}

// 第二个Activity  按钮返回
class SecondActivity : AppCompatActivity() {
    
    private var isSubmitted = false
    
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_second)
        
        val submitButton = findViewById&lt;Button&gt;(R.id.submit_button)
        val editText = findViewById&lt;EditText&gt;(R.id.input_edit_text)
        
        submitButton.setOnClickListener {
            isSubmitted = true
            val input = editText.text.toString()
            val data = Intent().apply {
                putExtra(FirstActivity.EXTRA_KEY, input)
            }
            setResult(RESULT_OK, data)
            finish()
        }
        
        // 处理返回键
        onBackPressedDispatcher.addCallback(this, object : OnBackPressedCallback(true) {
            override fun handleOnBackPressed() {
                returnWithCancel(&quot;用户取消了操作&quot;)
            }
        })
    }
    
    private fun returnWithCancel(message: String) {
        val data = Intent().apply {
            putExtra(FirstActivity.EXTRA_KEY, message)
        }
        setResult(RESULT_CANCELED, data)
        finish()
    }
    
    // 或者重写onBackPressed（已弃用，但还能用）
    @Deprecated(&quot;Deprecated in Java&quot;)
    override fun onBackPressed() {
        returnWithCancel(&quot;通过onBackPressed返回&quot;)
    }
}
</code></pre>
<p>使用 Activity Result API</p>
<pre><code class="language-kotlin">// 第一个Activity
class FirstActivity : AppCompatActivity() {
    
    // 注册Activity Result合约
    private val startForResult = registerForActivityResult(
        ActivityResultContracts.StartActivityForResult()
    ) { result -&gt;
        when (result.resultCode) {
            RESULT_OK -&gt; {
                val data = result.data?.getStringExtra(&quot;result_data&quot;)
                val actionType = result.data?.getStringExtra(&quot;action_type&quot;)
                
                when (actionType) {
                    &quot;submit&quot; -&gt; {
                        Toast.makeText(this, &quot;提交成功: $data&quot;, Toast.LENGTH_SHORT).show()
                    }
                    &quot;cancel&quot; -&gt; {
                        Toast.makeText(this, &quot;用户取消: $data&quot;, Toast.LENGTH_SHORT).show()
                    }
                    &quot;back&quot; -&gt; {
                        Toast.makeText(this, &quot;用户按返回键: $data&quot;, Toast.LENGTH_SHORT).show()
                    }
                }
            }
            RESULT_CANCELED -&gt; {
                Toast.makeText(this, &quot;操作被取消&quot;, Toast.LENGTH_SHORT).show()
            }
        }
    }
    
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_first)
        
        val button = findViewById&lt;Button&gt;(R.id.button)
        button.setOnClickListener {
            val intent = Intent(this, SecondActivity::class.java)
            startForResult.launch(intent)
        }
    }
}

// 第二个Activity  按钮返回
class SecondActivity : AppCompatActivity() {
    
    private var isSubmitted = false
    private var inputData: String = &quot;&quot;
    
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_second)
        
        val submitButton = findViewById&lt;Button&gt;(R.id.submit_button)
        val cancelButton = findViewById&lt;Button&gt;(R.id.cancel_button)
        val editText = findViewById&lt;EditText&gt;(R.id.input_edit_text)
        
        submitButton.setOnClickListener {
            inputData = editText.text.toString()
            isSubmitted = true
            returnResult(&quot;submit&quot;, inputData)
        }
        
        cancelButton.setOnClickListener {
            returnResult(&quot;cancel&quot;, &quot;用户手动取消&quot;)
        }
        
        // 处理返回键 - 使用 onBackPressedDispatcher（推荐）
        onBackPressedDispatcher.addCallback(this, object : OnBackPressedCallback(true) {
            override fun handleOnBackPressed() {
                returnResult(&quot;back&quot;, &quot;用户按返回键&quot;)
            }
        })
    }
    
    private fun returnResult(actionType: String, data: String) {
        val resultData = Intent().apply {
            putExtra(&quot;result_data&quot;, data)
            putExtra(&quot;action_type&quot;, actionType)
        }
        setResult(RESULT_OK, resultData)
        finish()
    }
}
</code></pre>
<h2 id="生命周期"><a href="#生命周期" class="headerlink" title="生命周期"></a>生命周期</h2><p> 使用任务（task）来管理Activity ， 一个任务就是一组存放在栈里的Activity 的集合，这个栈也被称作返回栈（back stack）。  </p>
<p>根据活动的可见性、是否位于栈顶，活动生命周期的四个状态：运行状态、暂停状态、停止状态、销毁状态。</p>
<p><img loading="lazy" src="https://cdn.nlark.com/yuque/0/2025/png/40667138/1750255459841-fc57e178-214c-4e08-956e-5062c2f77eb1.png"></p>
<ul>
<li>_ _onCreate()  ：第一次创建时调用，Activity 初始化，包括加载布局，绑定事件等。</li>
<li>onStart(): <strong>由不可见变为可见</strong></li>
<li>onResume():   准备好与用户进行交互式调用，一般位于返回栈栈顶,此时 Activity 位于前台</li>
<li>onPause():   系统准备启动或恢复另一个 Activity 时调用。释放资源，可在此<strong>保留关键数据</strong>，<font style="color:rgb(38, 38, 38);">此时 Activity 不再位于前台（比如在 Activity 上展示了弹窗）</font></li>
<li>onStop(): 完全<strong>不可见</strong>时调用， 如果启动的新Activity是一个对话框式的Activity，那么onPause()方法会得到执 行，而onStop()方法并不会执行。  （该状态下很有可能被回收）</li>
<li>onDestory():  在Activity被销毁之前调用，之后Activity的状态将变为销毁状 态。  </li>
<li>onRestart()。这个方法在Activity由停止状态 (<font style="color:rgb(38, 38, 38);">onStop()</font>) 变为运行状态之前调用，也就是Activity 被重新启动了。</li>
</ul>
<p><font style="color:#DF2A3F;">OnSaveInstance: </font><strong><font style="color:#DF2A3F;">活动被回收之前调用，可保留临时数据。使用携带的 Bundle类型的参数 用以保存数据。可以在活动重新创建的</font></strong><font style="color:rgb(38, 38, 38);">onCreate()方法中通过 Bundle  获取。 因此， Intent也可以结合Bundle一起用于传递 数据。  </font></p>
<h2 id="活动的启动模式"><a href="#活动的启动模式" class="headerlink" title="活动的启动模式"></a>活动的启动模式</h2><p> AndroidManifest.xml   通过  launchMode  属性指定</p>
<h3 id="standard"><a href="#standard" class="headerlink" title="standard"></a>standard</h3><p>默认模式， 每当启 动一个新的Activity，它就会在返回栈中入栈，并处于栈顶的位置。对于使用standard模式的 Activity，系统不会在乎这个Activity是否已经在返回栈中存在，每次启动都会创建一个该 Activity的新实例。 </p>
<h3 id="SingleTop（栈顶复用模式）"><a href="#SingleTop（栈顶复用模式）" class="headerlink" title="SingleTop（栈顶复用模式）"></a>SingleTop（栈顶复用模式）</h3><p>如果实例已经在栈顶，则不会创建新实例，而是调用其 <code>onNewIntent()</code>方法，否则才创建新的实例。</p>
<h3 id="SingleTask（栈内复用模式）"><a href="#SingleTask（栈内复用模式）" class="headerlink" title="SingleTask（栈内复用模式）"></a>SingleTask（栈内复用模式）</h3><p>整个任务栈中只存在一个实例，如果实例已存在，会清除它上面的所有活动，调用 <code>onNewIntent()</code>，可以设置 <code>taskAffinity</code>（任务相关性）指定任务栈。</p>
<h3 id="SingleInstance（单实例模式）"><a href="#SingleInstance（单实例模式）" class="headerlink" title="SingleInstance（单实例模式）"></a>SingleInstance（单实例模式）</h3><ul>
<li>单独在一个新的任务栈中</li>
<li>该任务栈只有这个活动</li>
<li>系统内只存在一个实例</li>
<li>通常用于需要独立运行的活动</li>
</ul>
<p><img loading="lazy" src="https://cdn.nlark.com/yuque/0/2026/png/40667138/1768395414964-e3acaec9-a8b1-44f5-a954-daebca385fd0.png"></p>
<h1 id="页面控件及布局"><a href="#页面控件及布局" class="headerlink" title="页面控件及布局"></a>页面控件及布局</h1><ul>
<li>控件可见性： visible、invisible、gone</li>
<li>布局</li>
</ul>
<p>线性布局 LinearLayout： 在线性方向（水平 horizontal，垂直 vertical）上依次排列</p>
<p>相对布局 RelativeLayout： 相对定位方式（相对父布局、相对页面）让控件出现在任何位置</p>
<p>帧布局 FrameLayout： 所有控件默认在左上角</p>
<p>百分比布局：Precent<font style="color:rgb(38, 38, 38);">FrameLayout 和 PercentRelativeLayout</font></p>
<p><font style="color:rgb(38, 38, 38);">自定义布局： 定义好后，通过 include 引入；</font></p>
<ul>
<li>控件的继承结构</li>
</ul>
<p><img loading="lazy" src="https://cdn.nlark.com/yuque/0/2025/png/40667138/1753620924477-95758dd3-b985-467a-b9cc-3bad315fbd05.png"></p>
<h3 id="ListView"><a href="#ListView" class="headerlink" title="ListView"></a>ListView</h3><p>使用 adapter 来设置其中每部分要展示的内容</p>
<p>ArrayAdapter 通过getView()方法convertView参数缓存布局文件以及要操作的控件，以此在提升加载速度  </p>
<p>通过 OnItemClickListener  点击事件</p>
<ul>
<li>自定义控件：</li>
</ul>
<p>创建自定义布局，逻辑代码中通过 inflate 加载布局文件</p>
<p>引用时通过包名+类名来引用，对于逻辑层加入事件</p>
<p>只能实现纵向滚动的效果</p>
<h3 id="RecyclerView"><a href="#RecyclerView" class="headerlink" title="RecyclerView"></a>RecyclerView</h3><p>适配器构建</p>
<pre><code class="language-kotlin">class FruitAdapter(val fruitList: List&lt;Fruit&gt;) : 
RecyclerView.Adapter&lt;FruitAdapter.ViewHolder&gt;() { 

    // view 子项最外层布局
    inner class ViewHolder(view: View) : RecyclerView.ViewHolder(view) { 
        val fruitImage: ImageView = view.findViewById(R.id.fruitImage) 
        val fruitName: TextView = view.findViewById(R.id.fruitName) 
    } 

    override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): ViewHolder { 
        val view = LayoutInflater.from(parent.context) 
            .inflate(R.layout.fruit_item, parent, false) 
        // 此处添加点击事件
        return ViewHolder(view) 
    } 

    // 对子项数据复制
    override fun onBindViewHolder(holder: ViewHolder, position: Int) { 
        val fruit = fruitList[position] 
        holder.fruitImage.setImageResource(fruit.imageId) 
        holder.fruitName.text = fruit.name 
    } 

    override fun getItemCount() = fruitList.size 

} 
</code></pre>
<p>ListView的布局排列是由自身去管理的，而RecyclerView 则将这个工作交给了LayoutManagerLayoutManager制定了一套可扩展的布局排列接口，子类只要按照接口的规范来实现，就能定制出各种不同排列方式的布局。</p>
<p>GridLayoutManager可以用于实现网格布局，StaggeredGridLayoutManager可以用于实现瀑布流布局。</p>
<h1 id="Fragment"><a href="#Fragment" class="headerlink" title="Fragment"></a>Fragment</h1><ul>
<li>可以<strong>嵌入在活动中的 UI 片段</strong>，每个碎片都有其对于的布局文件、代码文件。</li>
<li>一个活动中可以包括多个碎片，<strong>通过 FrameLayout 碎片的切换</strong>。</li>
<li>在碎片中获取活动实现，碎片与活动，碎片与碎片之间的通讯。</li>
</ul>
<p>在 Activity 中添加 Fragment</p>
<pre><code class="language-kotlin">&lt;LinearLayout xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot; 
    android:orientation=&quot;horizontal&quot; 
    android:layout_width=&quot;match_parent&quot; 
    android:layout_height=&quot;match_parent&quot; &gt; 
 
    &lt;fragment 
        android:id=&quot;@+id/leftFrag&quot; 
        android:name=&quot;com.example.fragmenttest.LeftFragment&quot; 
        android:layout_width=&quot;0dp&quot; 
        android:layout_height=&quot;match_parent&quot; 
        android:layout_weight=&quot;1&quot; /&gt; 
 
    &lt;fragment 
        android:id=&quot;@+id/rightFrag&quot; 
        android:name=&quot;com.example.fragmenttest.RightFragment&quot; 
        android:layout_width=&quot;0dp&quot; 
        android:layout_height=&quot;match_parent&quot; 
        android:layout_weight=&quot;1&quot; /&gt; 
 
&lt;/LinearLayout&gt; 
</code></pre>
<p>通过 FrameLayout 可以实现 Fragment 的动态添加</p>
<pre><code class="language-kotlin">    private fun replaceFragment(fragment: Fragment) { 
        val fragmentManager = supportFragmentManager 
        val transaction = fragmentManager.beginTransaction() 
        transaction.replace(R.id.rightLayout, fragment) 
        transaction.commit() 
    } 
</code></pre>
<h3 id="Fragment-与-Activity-之间的交互"><a href="#Fragment-与-Activity-之间的交互" class="headerlink" title="Fragment 与 Activity 之间的交互"></a>Fragment 与 Activity 之间的交互</h3><p><strong>Activity 调用 Fragment</strong></p>
<p>调用FragmentManager的findFragmentById()方法，可以在Activity中得到相应 Fragment的实例，可以实现调用特定 Fragment 中方法。</p>
<p> 类似于findViewById()方法，kotlin-android-extensions插件也对 findFragmentById()方法进行了扩展，允许我们直接使用布局文件中定义的Fragment id名 称来自动获取相应的Fragment实例  </p>
<pre><code class="language-kotlin">val fragment = leftFrag as LeftFragment 
</code></pre>
<p><strong>Fragment 调用 Activity</strong></p>
<p> 在每个Fragment中都可以通过调用getActivity()方法来得到 和当前Fragment相关联的Activity实例，代码如下所示：  </p>
<pre><code class="language-kotlin">if (activity != null) { 
    // 将 activity 强制转换为 MainActivity 类型
    val mainActivity = activity as MainActivity 
} 
</code></pre>
<p><strong>不同 Fragment 之间的通信</strong></p>
<p> 首先在一个Fragment中 可以得到与它相关联的Activity，然后再通过这个Activity去获取另外一个Fragment的实例， 这样就实现了不同Fragment之间的通信功能。 </p>
<pre><code class="language-kotlin">// Activity中定义接口
class MainActivity : AppCompatActivity(), FragmentA.OnDataListener {
    
    fun sendDataToFragmentB(data: String) {
        val fragmentB = supportFragmentManager.findFragmentById(R.id.fragment_b) as? FragmentB
        fragmentB?.updateData(data)
    }
}

// FragmentA
class FragmentA : Fragment() {
    // 定义通信接口
    interface OnDataListener {
        fun onDataSent(data: String)
    }
    
    private var listener: OnDataListener? = null
    
    override fun onAttach(context: Context) {
        // 将Activity转换为OnDataListener接口
        super.onAttach(context)
        listener = context as? OnDataListener
    }

    // 调用Activity方法实现与另一个Fragment通信
    fun sendData() {
        listener?.onDataSent(&quot;Hello from FragmentA&quot;)
    }
} 
</code></pre>
<h2 id="碎片的生命周期"><a href="#碎片的生命周期" class="headerlink" title="碎片的生命周期"></a>碎片的生命周期</h2><p>Fragment 大多数状态与 Activity  相似。</p>
<p><strong>停止状态： <strong>当一个Activity进入停止状态时，与它相关联的Fragment就会进入停止状态  。通过调用FragmentTransaction的remove()、replace()方法将Fragment从Activity中移 除，但</strong>在事务提交之前调用了addToBackStack()方法</strong>，这时的Fragment也会进入停止 状态。  </p>
<p> <strong>销毁状态 ：</strong> Fragment总是依附于Activity而存在，因当Activity被销毁时，与它相关联的 Fragment就会进入销毁状态。或者通过调用FragmentTransaction的remove()、 replace()方法将Fragment从Activity中移除，但<strong>在事务提交之前并没有调用 addToBackStack()方法</strong>，这时的Fragment也会进入销毁状态。  </p>
<p><img loading="lazy" src="https://cdn.nlark.com/yuque/0/2026/png/40667138/1768703927569-d9d4dba5-7e97-46da-9442-3f7515c9dcc7.png"></p>
<h2 id="动态加载布局"><a href="#动态加载布局" class="headerlink" title="动态加载布局"></a>动态加载布局</h2><blockquote>
<p>比如：根据设备类型（平板 、手机）使用不同的布局</p>
</blockquote>
<ul>
<li>使用限定符  对应不同的文件夹名 （可以利用限定符区分 大小、分辨率、方向）如 layout-large</li>
</ul>
<p><img loading="lazy" src="https://cdn.nlark.com/yuque/0/2026/png/40667138/1768704151184-f96c2de3-a407-449c-8b30-eece3b575390.png"></p>
<ul>
<li>在逻辑层判断使用的是哪个布局</li>
</ul>
<pre><code class="language-kotlin">    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)
        
        // 检查是否是双面板布局
        isDualPane = findViewById&lt;View&gt;(R.id.fragment_container_right) != null
        
        if (savedInstanceState == null) {
            if (isDualPane) {
                // 平板设备 - 显示两个Fragment
                setupTabletLayout()
            } else {
                // 手机设备 - 显示一个Fragment
                setupPhoneLayout()
            }
        }
    }
</code></pre>
<h1 id="广播机制-BroadcastReceiver"><a href="#广播机制-BroadcastReceiver" class="headerlink" title="广播机制( BroadcastReceiver  )"></a>广播机制( BroadcastReceiver  )</h1><p>可以跨进程通信</p>
<ul>
<li>标准广播： 异步，同时受到，无法被打断</li>
<li>有序广播：同步执行 同一时刻只有一个收到</li>
</ul>
<h2 id="接收系统广播"><a href="#接收系统广播" class="headerlink" title="接收系统广播"></a>接收系统广播</h2><p>系统广播包括系统电量变化，开机，网路变化等</p>
<h3 id="注册广播"><a href="#注册广播" class="headerlink" title="注册广播"></a>注册广播</h3><h4 id="动态注册"><a href="#动态注册" class="headerlink" title="动态注册"></a>动态注册</h4><p>代码中注册，使用完成后需要及时关闭</p>
<pre><code class="language-java">class MainActivity : AppCompatActivity() { 
    lateinit var timeChangeReceiver: TimeChangeReceiver 
    override fun onCreate(savedInstanceState: Bundle?) { 
        super.onCreate(savedInstanceState) 
        setContentView(R.layout.activity_main) 
        val intentFilter = IntentFilter() 
        // 设置监听广播类型
        intentFilter.addAction(&quot;android.intent.action.TIME_TICK&quot;) 
        timeChangeReceiver = TimeChangeReceiver() 
        registerReceiver(timeChangeReceiver, intentFilter) 
    } 
    override fun onDestroy() { 
        super.onDestroy() 
        // 注销接收器
        unregisterReceiver(timeChangeReceiver) 
    } 
    
    inner class TimeChangeReceiver : BroadcastReceiver() { 
        override fun onReceive(context: Context, intent: Intent) { 
            Toast.makeText(context, &quot;Time has changed&quot;, Toast.LENGTH_SHORT).show()
        }
    }
}
</code></pre>
<h4 id="静态注册"><a href="#静态注册" class="headerlink" title="静态注册"></a>静态注册</h4><p> 在 AndroidManifest 中注册</p>
<blockquote>
<p>对于系统的广播接收，部分需要声明权限。 在Android 8.0系统之后，所有隐式广播都不允许使用静态注册的方式来接收了。**隐式广播指的是那些没有具体指定发送给哪个应用程序的广播，大多数系统广播属于隐式广播，但是少数特殊的系统广播目前仍然允许使用静态注册的方式来接收。 **</p>
</blockquote>
<p>声明权限</p>
<pre><code class="language-java"> &lt;uses-permission android:name=&quot;android.permission.RECEIVE_BOOT_COMPLETED&quot; /&gt;  
</code></pre>
<p>不要在onReceive()方法中添加过多的逻辑或者进行任何的耗时操作，因为** BroadcastReceiver中是不允许开启线程的，当onReceive()方法运行了较长时间而没有结束时，程序就会出现错误**。  </p>
<h3 id="发送广播"><a href="#发送广播" class="headerlink" title="发送广播"></a>发送广播</h3><h4 id="标准广播"><a href="#标准广播" class="headerlink" title="标准广播"></a>标准广播</h4><p>自定义 intent + sendBroadcast 方法</p>
<pre><code class="language-kotlin">val intent = Intent(&quot;com.example.broadcasttest.MY_BROADCAST&quot;) 
// 指明要发送的程序
intent.setPackage(packageName) 
sendBroadcast(intent) 
</code></pre>
<h4 id="有序广播"><a href="#有序广播" class="headerlink" title="有序广播"></a>有序广播</h4><p>自定义intent + sendOrderedBroadcast 方法</p>
<pre><code class="language-kotlin"> val intent = Intent(&quot;com.example.broadcasttest.MY_BROADCAST&quot;) 
intent.setPackage(packageName) 
sendOrderedBroadcast(intent, null)   //  intent,权限相关字符 
</code></pre>
<p>高优先级的 Receiver 先接收</p>
<pre><code class="language-xml">&lt;receiver 
  android:name=&quot;.MyBroadcastReceiver&quot; 
  android:enabled=&quot;true&quot; 
  android:exported=&quot;true&quot;&gt; 
  &lt;intent-filter android:priority=&quot;100&quot;&gt;   
    &lt;action android:name=&quot;com.example.broadcasttest.MY_BROADCAST&quot;/&gt; 
  &lt;/intent-filter&gt; 
&lt;/receiver&gt; 
</code></pre>
<p>如果接收器在 onReceive  中逻辑中执行  abortBroadcast()  ，将这条广播截断，后面 的BroadcastReceiver将无法再接收到这条广播。  </p>
<h4 id="本地广播"><a href="#本地广播" class="headerlink" title="本地广播"></a>本地广播</h4><p>使用 LocalBroadCast 对广播进行广利，使用其对于的发送和接收方法。</p>
<h1 id="数据持久化技术"><a href="#数据持久化技术" class="headerlink" title="数据持久化技术"></a>数据持久化技术</h1><p>Android 中的三种数据持久化方法</p>
<ul>
<li>文件</li>
<li>SharedPreferences</li>
<li>数据库</li>
</ul>
<h2 id="文件"><a href="#文件" class="headerlink" title="文件"></a>文件</h2><ul>
<li>存储在应用目录下的 files 文件夹下（ 所有的文件都默认存储到&#x2F;data&#x2F;data&#x2F;<pacjage name>&#x2F;files&#x2F; ）</li>
<li>文件流写入  （openFileOutput、openFileInput）</li>
</ul>
<pre><code class="language-kotlin">// 写入文件
fun save(inputText: String) { 
    try { 
        val output = openFileOutput(&quot;data&quot;, Context.MODE_PRIVATE) 
        val writer = BufferedWriter(OutputStreamWriter(output)) 
        writer.use {   // use 代码块结束后，自动关闭 流
            it.write(inputText) 
        } 
    } catch (e: IOException) { 
        e.printStackTrace() 
    } 
} 

fun load(): String { 
    val content = StringBuilder() 
    try { 
        val input = openFileInput(&quot;data&quot;) 
        val reader = BufferedReader(InputStreamReader(input)) 
        reader.use { 
            reader.forEachLine { 
                content.append(it) 
            } 
        } 
    } catch (e: IOException) { 
        e.printStackTrace() 
    } 
    return content.toString() 
} 
</code></pre>
<h2 id="SharedPreferences"><a href="#SharedPreferences" class="headerlink" title="SharedPreferences"></a>SharedPreferences</h2><p>使用键值对存储数据   数据存储在 包名&#x2F;shared_prefs&#x2F;  以 xml 形式存储</p>
<h3 id="存储数据"><a href="#存储数据" class="headerlink" title="存储数据"></a>存储数据</h3><h4 id="获取-sharedPreferences-对象"><a href="#获取-sharedPreferences-对象" class="headerlink" title="获取 sharedPreferences 对象"></a>获取 sharedPreferences 对象</h4><ul>
<li>Context 类中的getSharedPreferences(文件名,操作模式)    </li>
<li>Activity 类中的getPreferences(操作模式)</li>
</ul>
<h4 id="存储"><a href="#存储" class="headerlink" title="存储"></a>存储</h4><ul>
<li>使用 edit（）方法获取 Editor 对象</li>
<li>存取数据对象</li>
<li>使用 apply 方法保存</li>
</ul>
<pre><code class="language-kotlin">val editor = getSharedPreferences(&quot;data&quot;, Context.MODE_PRIVATE).edit() 
editor.putString(&quot;name&quot;, &quot;Tom&quot;) 
editor.putInt(&quot;age&quot;, 28) 
editor.putBoolean(&quot;married&quot;, false) 
editor.apply() 
</code></pre>
<h3 id="读取数据"><a href="#读取数据" class="headerlink" title="读取数据"></a>读取数据</h3><ul>
<li>使用 get 方法根据 键 获取对应的  值</li>
</ul>
<pre><code class="language-kotlin">val prefs = getSharedPreferences(&quot;data&quot;, Context.MODE_PRIVATE) 
val name = prefs.getString(&quot;name&quot;, &quot;&quot;) 
val age = prefs.getInt(&quot;age&quot;, 0) 
val married = prefs.getBoolean(&quot;married&quot;, false) 
</code></pre>
<h2 id="SQLite"><a href="#SQLite" class="headerlink" title="SQLite"></a>SQLite</h2><blockquote>
<p>书中提到使用 Android API 、 SQL 语句 以及 LitePal 方法进行数据库的操作，也可以使用 Greendao,对比可以参考下面文章。</p>
</blockquote>
<p><a href="https://blog.csdn.net/yingtian648/article/details/56672559">Android 中数据库框架GreenDao与LitePal对比、集成、使用详解,greendao与原生SQLite性能对比_litepal和sqlite,greendao哪个好-CSDN博客</a></p>
<h3 id="SQLiteOpenHelper"><a href="#SQLiteOpenHelper" class="headerlink" title="SQLiteOpenHelper"></a>SQLiteOpenHelper</h3><p> SQLiteOpenHelper是一个抽象类 ，包含两个抽象方法：onCreate()和 onUpgrade()  </p>
<ul>
<li>getReadableDatabase() )与 getWritableDatabase()  ：当数据库不可写入的时候（如磁盘空间已满），getReadableDatabase()方 法返回的对象将以只读的方式打开数据库，而getWritableDatabase()方法则将出现异常。</li>
</ul>
<p>构建出SQLiteOpenHelper的实例之 后，再调用它的getReadableDatabase()或getWritableDatabase()方法就能够创建数 据库了，数据库文件会存放在&#x2F;data&#x2F;data&#x2F;&#x2F;databases&#x2F;目录下。时，重 写的onCreate()方法也会得到执行，所以通常会在这里处理一些创建表的逻辑。 </p>
<pre><code class="language-kotlin">/*
context: 
name: 数据库名
cursor: 在查询数据的时候返回一个自定义的Cursor，一般传入null即可
version: 数据库版本号
*/
class MyDatabaseHelper(val context: Context, name: String, version: Int) : 
        SQLiteOpenHelper(context, name, null, version) {
    // 构建sql语句
    private val createBook = &quot;create table Book (&quot; + 
            &quot; id integer primary key autoincrement,&quot; + 
            &quot;author text,&quot; + 
            &quot;price real,&quot; + 
            &quot;pages integer,&quot; + 
            &quot;name text)&quot; 
 
    override fun onCreate(db: SQLiteDatabase) { 
        db.execSQL(createBook) 
        Toast.makeText(context, &quot;Create succeeded&quot;, Toast.LENGTH_SHORT).show() 
    } 
    // 用于对数据库进行升级
    override fun onUpgrade(db: SQLiteDatabase, oldVersion: Int, newVersion: Int) { 
    } 
 
} 
</code></pre>
<p> 借助<font style="color:rgb(38, 38, 38);">getReadableDatabase() 或 getWritableDatabase()</font>  返回的 SQLiteDatabase对象，借助这个对象就可以对数据进行CRUD操作了  </p>
<h4 id="添加数据"><a href="#添加数据" class="headerlink" title="添加数据"></a>添加数据</h4><pre><code class="language-kotlin">val db = dbHelper.writableDatabase 
val values1 = ContentValues().apply { 
    // 开始组装第一条数据 
    put(&quot;name&quot;, &quot;The Da Vinci Code&quot;) 
    put(&quot;author&quot;, &quot;Dan Brown&quot;) 
    put(&quot;pages&quot;, 454) 
    put(&quot;price&quot;, 16.96) 
} 
db.insert(&quot;Book&quot;, null, values1) // 插入第一条数据 
val values2 = ContentValues().apply { 
    // 开始组装第二条数据 
    put(&quot;name&quot;, &quot;The Lost Symbol&quot;) 
    put(&quot;author&quot;, &quot;Dan Brown&quot;) 
    put(&quot;pages&quot;, 510) 
    put(&quot;price&quot;, 19.95) 
} 
// insert(表名 为空的列自动赋值NULL ContentValues对象)
db.insert(&quot;Book&quot;, null, values2) // 插入第二条数据 
</code></pre>
<h4 id="更新数据"><a href="#更新数据" class="headerlink" title="更新数据"></a>更新数据</h4><pre><code class="language-kotlin">val db = dbHelper.writableDatabase 
val values = ContentValues() 
values.put(&quot;price&quot;, 10.99) 
db.update(&quot;Book&quot;, values, &quot;name = ?&quot;, arrayOf(&quot;The Da Vinci Code&quot;))   // 加入了数据约束
</code></pre>
<h4 id="删除数据"><a href="#删除数据" class="headerlink" title="删除数据"></a>删除数据</h4><pre><code class="language-kotlin">val db = dbHelper.writableDatabase 
db.delete(&quot;Book&quot;, &quot;pages &gt; ?&quot;, arrayOf(&quot;500&quot;)) 
</code></pre>
<h4 id="查询数据"><a href="#查询数据" class="headerlink" title="查询数据"></a>查询数据</h4><p><img loading="lazy" src="https://cdn.nlark.com/yuque/0/2026/png/40667138/1769257034073-d00fa453-409d-47c0-ad24-0bc0a1bc8b47.png"></p>
<p>直接使用 SQL 操作数据库</p>
<pre><code class="language-kotlin">db.execSQL(&quot;insert into Book (name, author, pages, price) values(?, ?, ?, ?)&quot;, 
    arrayOf(&quot;The Da Vinci Code&quot;, &quot;Dan Brown&quot;, &quot;454&quot;, &quot;16.96&quot;) 
) 
</code></pre>
<h3 id="使用事务"><a href="#使用事务" class="headerlink" title="使用事务"></a>使用事务</h3><p> 事务的特性可以保证让一系列的操作要么全部完成， 要么一个都不会完成。  </p>
<p>**使用   db.beginTransaction()   然后执行相关数据库 CRUD 操作，之后调用  db.endTransaction()  **</p>
<h4 id="数据库升级"><a href="#数据库升级" class="headerlink" title="数据库升级"></a>数据库升级</h4><p> 当指定的数据库版本号大于当前数据库版本号的时候，就会进入onUpgrade()方法中执 行更新操作。  可以 **为每一个版本号赋予其所对应的数据库变动，然后在onUpgrade()方法 中对当前数据库的版本号进行判断，再执行相应的改变就可以了。 ** </p>
<pre><code class="language-kotlin">override fun onUpgrade(db: SQLiteDatabase, oldVersion: Int, newVersion: Int) { 
    if (oldVersion &lt;= 1) { 
        db.execSQL(createCategory) 
    } 
    if (oldVersion &lt;= 2) { 
        db.execSQL(&quot;alter table Book add column category_id integer&quot;) 
    } 
} 
</code></pre>
<h1 id="内容提供器-ContentProvider"><a href="#内容提供器-ContentProvider" class="headerlink" title="内容提供器  ContentProvider"></a>内容提供器  ContentProvider</h1><p>主要用于不同的应用程序之间实现数据共享功能。</p>
<blockquote>
<p>例如电话簿程序共享联系人</p>
</blockquote>
<h2 id="运行时权限"><a href="#运行时权限" class="headerlink" title="运行时权限"></a>运行时权限</h2><p>运行时权限： Android 6.0系统中加入了运行时权限功 能。也就是说，用户不需要在安装软件的时候一次性授权所有申请的权限，而是可以在软件的 **使用过程中再对某一项权限申请进行授权。  **</p>
<ul>
<li>普通权限指的是那些不会直接威胁到用户的安全和隐私的权限， 系统会自动授权</li>
<li>危险权限表示那些可能会触及用户隐私或者对设备安全性造成影响的权限需要用户授权</li>
</ul>
<p>开发时</p>
<ul>
<li>判断用户是否授权  checkSelfPermission</li>
<li>授权则继续运行，否则就申请授权 requestPermission</li>
</ul>
<pre><code class="language-kotlin">class MainActivity : AppCompatActivity() { 
 
    override fun onCreate(savedInstanceState: Bundle?) { 
        super.onCreate(savedInstanceState) 
        setContentView(R.layout.activity_main) 
                makeCall.setOnClickListener { 
            // 判断用户是否授权
            if (ContextCompat.checkSelfPermission(this, 
                Manifest.permission.CALL_PHONE) != PackageManager.PERMISSION_GRANTED) { 
                // 未授权时则需要请求对应权限
                ActivityCompat.requestPermissions(this, 
                    arrayOf(Manifest.permission.CALL_PHONE), 1) 
            } else { 
                call() 
            } 
        } 
    } 

    //  未授权 -&gt;  请求对应权限 -&gt; 请求结果的回调
    // grantResults 为 请求结果
    override fun onRequestPermissionsResult(requestCode: Int, 
            permissions: Array&lt;String&gt;, grantResults: IntArray) { 
        super.onRequestPermissionsResult(requestCode, permissions, grantResults) 
        when (requestCode) { 
            1 -&gt; { 
                if (grantResults.isNotEmpty() &amp;&amp; 
                    grantResults[0] == PackageManager.PERMISSION_GRANTED) { 
                    call() 
                } else { 
                    Toast.makeText(this, &quot;You denied the permission&quot;, 
                        Toast.LENGTH_SHORT).show() 
                } 
            } 
        } 
    } 
 
    private fun call() { 
        try { 
            val intent = Intent(Intent.ACTION_CALL) 
            intent.data = Uri.parse(&quot;tel:10086&quot;) 
            startActivity(intent) 
        } catch (e: SecurityException) { 
            e.printStackTrace() 
        } 
    } 
 
}
</code></pre>
<h2 id="访问其他程序中的数据"><a href="#访问其他程序中的数据" class="headerlink" title="访问其他程序中的数据"></a>访问其他程序中的数据</h2><h3 id="ContentResolver"><a href="#ContentResolver" class="headerlink" title="ContentResolver"></a>ContentResolver</h3><p>通过 getContentResolver 获取 ContentResolver  实例</p>
<p> 不同于SQLiteDatabase，ContentResolver中的增删改查方法都是不接收表名参数的，而使用一个<strong>Uri参数</strong>代替，这个参数被称为内容URI。  内容URI给ContentProvider中的数据建立 了唯一标识符，它主要由两部分组成：authority和path。<strong>authority是用于对不同的应用程序做区分的</strong> ， **path则是用于对同一应用程序中不同的表做区分的，通常会添 加到authority的后面。   **</p>
<pre><code class="language-kotlin">val uri = Uri.parse(&quot;content://com.example.app.provider/table1&quot;) 
// 通过 query 查询数据
val cursor = contentResolver.query( 
    uri, 
    projection, 
    selection, 
    selectionArgs, 
    sortOrder) 
</code></pre>
<p><strong>查询联系人数据</strong></p>
<pre><code class="language-kotlin">        // 查询联系人数据 
        contentResolver.query(ContactsContract.CommonDataKinds.Phone.CONTENT_URI, 
                null, null, null, null)?.apply { 
            while (moveToNext()) { 
                // 获取联系人姓名 
                val displayName = getString(getColumnIndex( 
                ContactsContract.CommonDataKinds.Phone.DISPLAY_NAME)) 
                // 获取联系人手机号 
                val number = getString(getColumnIndex( 
                ContactsContract.CommonDataKinds.Phone.NUMBER)) 
                contactsList.add(&quot;$displayName\n$number&quot;) 
            } 
            adapter.notifyDataSetChanged() 
            close() 
        } 
</code></pre>
<h2 id="创建内容提供器"><a href="#创建内容提供器" class="headerlink" title="创建内容提供器"></a>创建内容提供器</h2><p>创建类继承自 ContentProvider，重写下面方法</p>
<ul>
<li>onCreate  初始化调用</li>
<li>query  查询，根据 uri 参数确定查哪张表</li>
<li>insert  插入</li>
<li>update  更新已有数据</li>
<li>delete 删除</li>
<li>getType   获取 uri 返回相应的 MIME 类型</li>
</ul>
<pre><code class="language-kotlin">class MyProvider : ContentProvider() { 
    // 初始化调用，数据库创建与升级
    override fun onCreate(): Boolean { 
        return false 
    } 
    // 查询数据 
    // uri -- 表   projection -- 列 selection和selectionArgs -- 约束参数  sortOrder -- 结果排序  
    override fun query(uri: Uri, projection: Array&lt;String&gt;?, selection: String?, 
            selectionArgs: Array&lt;String&gt;?, sortOrder: String?): Cursor? { 
        return null 
    }
    // 添加一条数据
    override fun insert(uri: Uri, values: ContentValues?): Uri? { 
        return null 
    } 
} 
    override fun update(uri: Uri, values: ContentValues?, selection: String?, 
            selectionArgs: Array&lt;String&gt;?): Int { 
        return 0 
    } 
    override fun delete(uri: Uri, selection: String?, selectionArgs: Array&lt;String&gt;?): Int { 
        return 0 
    } 
    // 获取Uri对象所对应的MIME类型
    override fun getType(uri: Uri): String? { 
        return null 
    }
</code></pre>
<p> 可以使用通配符分别匹配这两种格式的内容URI ：</p>
<ul>
<li>*表示匹配任意长度的任意字符  </li>
<li>#表示匹配任意长度的数字</li>
</ul>
<p> 借助UriMatcher实现匹配内容URI   </p>
<pre><code class="language-kotlin">class MyProvider : ContentProvider() { 
 
    private val table1Dir = 0 
    private val table1Item = 1 
    private val table2Dir = 2 
    private val table2Item = 3 
 
    private val uriMatcher = UriMatcher(UriMatcher.NO_MATCH) 
 
    init { 
        uriMatcher.addURI(&quot;com.example.app.provider&quot;, &quot;table1&quot;, table1Dir) 
        uriMatcher.addURI(&quot;com.example.app.provider &quot;, &quot;table1/#&quot;, table1Item) 
        uriMatcher.addURI(&quot;com.example.app.provider &quot;, &quot;table2&quot;, table2Dir) 
        uriMatcher.addURI(&quot;com.example.app.provider &quot;, &quot;table2/#&quot;, table2Item) 
    } 
    ... 
    override fun query(uri: Uri, projection: Array&lt;String&gt;?, selection: String?, 
            selectionArgs: Array&lt;String&gt;?, sortOrder: String?): Cursor? { 
        when (uriMatcher.match(uri)) { 
            table1Dir -&gt; { 
                // 查询table1表中的所有数据 
            } 
            table1Item -&gt; { 
                // 查询table1表中的单条数据 
            } 
            table2Dir -&gt; { 
                // 查询table2表中的所有数据 
            } 
            table2Item -&gt; { 
                // 查询table2表中的单条数据 
            } 
        } 
        ... 
    } 
    ... 
} 
</code></pre>
<p>getType 用以获取Uri对象所对应的MIME类型  </p>
<blockquote>
<p> URI所对应的MIME字符串主要由3部分组成，Android对这3个部分做了如下格式规定。 </p>
<ul>
<li>必须以vnd开头。 </li>
<li>如果内容URI以路径结尾，则后接android.cursor.dir&#x2F;；如果内容URI以id结尾，则后接android.cursor.item&#x2F;。 </li>
<li>最后接上vnd..。</li>
</ul>
</blockquote>
<h1 id="手机多媒体"><a href="#手机多媒体" class="headerlink" title="手机多媒体"></a>手机多媒体</h1><h2 id="通知"><a href="#通知" class="headerlink" title="通知"></a>通知</h2><p>通知渠道(8.0 以上版本)： 每条通知都要属于一个对应的渠道。每个应用程序都可以自由地创建当前应用拥有哪些通知渠道，但是这些通知渠道的控制权是掌握在用户手上的。用 户可以自由地选择这些通知渠道的重要程度，是否响铃、是否振动或者是否要关闭这个渠道的通知 。</p>
<p>通知渠道的重要等级越高，发出的通知就越容易获得用户的注意。  开发者只能在创建通知渠道的时候为它<strong>指定初始的重要等级</strong>，如果用户不认 可这个重要等级的话，可以随时进行修改，开发者对无权再进行调整和变更  </p>
<h3 id="创建通知渠道"><a href="#创建通知渠道" class="headerlink" title="创建通知渠道"></a>创建通知渠道</h3><ul>
<li>获取通知实例 NotificationManager</li>
</ul>
<pre><code class="language-kotlin">val manager = getSystemService(Context.NOTIFICATION_SERVICE) as NotificationManager 
if (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.O) { 
    // channelId--渠道ID channelName--渠道名称，表明渠道作用 importance--初始的重要等级
    val channel = NotificationChannel(channelId, channelName, importance) 
    manager.createNotificationChannel(channel) 
} 
</code></pre>
<ul>
<li>创建通知对象，加入 Intent 执行通知点击操作</li>
</ul>
<pre><code class="language-kotlin">val notification = NotificationCompat.Builder(context, channelId) 
    .setContentTitle(&quot;This is content title&quot;)
    .setContentText(&quot;This is content text&quot;) 
    .setSmallIcon(R.drawable.small_icon) 
    .setLargeIcon(BitmapFactory.decodeResource(getResources(),R.drawable.large_icon)) 
    .build() 
    
manager.notify(1, notification) 
</code></pre>
<h3 id="PendingIntent"><a href="#PendingIntent" class="headerlink" title="PendingIntent"></a>PendingIntent</h3><p> PendingIntent倾向于在某个合适的时机执行某个动作。所以，也可以把PendingIntent简单地理解为延迟执行的Intent。  </p>
<p><font style="color:rgb(35, 41, 48);">NotificationCompat</font><font style="color:rgb(38, 44, 49);">.</font><font style="color:rgb(35, 41, 48);">Builder 存在一个  setContentIntent()  方法， 接收的参数是一个PendingIntent对象。  可以通过PendingIntent构建一个 延迟执行的“意图”，当用户点击这条通知时就会执行相应的逻辑 </font></p>
<p><strong><font style="color:rgb(35, 41, 48);">取消通知的两种方式</font></strong></p>
<ul>
<li><font style="color:rgb(35, 41, 48);">setAutoCancel</font></li>
<li><font style="color:rgb(35, 41, 48);"> manager.cancel  </font></li>
</ul>
<p><strong>示例</strong></p>
<pre><code class="language-kotlin">class MainActivity : AppCompatActivity() { 

    override fun onCreate(savedInstanceState: Bundle?) { 
        super.onCreate(savedInstanceState) 
        setContentView(R.layout.activity_main) 
        val manager = getSystemService(Context.NOTIFICATION_SERVICE) as 
        NotificationManager 
        if (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.O) { 
            val channel = NotificationChannel(&quot;normal&quot;, &quot;Normal&quot;,NotificationManager. 
                                              IMPORTANCE_DEFAULT) 
            manager.createNotificationChannel(channel) 
        } 
        sendNotice.setOnClickListener { 
            val intent = Intent(this, NotificationActivity::class.java) 
            val pi = PendingIntent.getActivity(this, 0, intent, 0) 
            val notification = NotificationCompat.Builder(this, &quot;normal&quot;) 
                .setContentTitle(&quot;This is content title&quot;) 
                .setContentText(&quot;This is content text&quot;) 
                .setSmallIcon(R.drawable.small_icon) 
                .setLargeIcon(BitmapFactory.decodeResource(resources, 
                                                           R.drawable.large_icon)) 
                // 设置 通知点击跳转Intent
                .setContentIntent(pi) 
                // 点击通知后自动取消 
                .setAutoCancel(true) 
                .build() 
            manager.notify(1, notification) 
        } 
    } 
} 
</code></pre>
<ul>
<li>setStyle()   支持富文本</li>
</ul>
<h2 id="摄像头，音视频"><a href="#摄像头，音视频" class="headerlink" title="摄像头，音视频"></a>摄像头，音视频</h2><h3 id="摄像头拍照"><a href="#摄像头拍照" class="headerlink" title="摄像头拍照"></a>摄像头拍照</h3><pre><code class="language-kotlin">class MainActivity : AppCompatActivity() { 
    val takePhoto = 1 
    lateinit var imageUri: Uri 
    lateinit var outputImage: File 
    override fun onCreate(savedInstanceState: Bundle?) { 
        super.onCreate(savedInstanceState) 
        setContentView(R.layout.activity_main) 
        takePhotoBtn.setOnClickListener { 
            // 创建File对象，用于存储拍照后的图片 
            outputImage = File(externalCacheDir, &quot;output_image.jpg&quot;) 
            if (outputImage.exists()) { 
                outputImage.delete() 
            } 
            outputImage.createNewFile() 
            imageUri = if (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.N) {
                // 将File对象转换成一个封装过的Uri对象 
                FileProvider.getUriForFile(this, &quot;com.example.cameraalbumtest.
                                           fileprovider&quot;, outputImage) 
            } else { 
                Uri.fromFile(outputImage) 
            } 
            // 启动相机程序 
            val intent = Intent(&quot;android.media.action.IMAGE_CAPTURE&quot;) 
            // 指定图片输出地址
            intent.putExtra(MediaStore.EXTRA_OUTPUT, imageUri) 
            startActivityForResult(intent, takePhoto) 
        } 
    } 
    // 传入返回的结果
    override fun onActivityResult(requestCode: Int, resultCode: Int, data: Intent?) { 
        super.onActivityResult(requestCode, resultCode, data) 
        when (requestCode) { 
            takePhoto -&gt; { 
                if (resultCode == Activity.RESULT_OK) { 
                    // 将拍摄的照片显示出来 
                    val bitmap = BitmapFactory.decodeStream(contentResolver. 
                        openInputStream(imageUri)) 
                    imageView.setImageBitmap(rotateIfRequired(bitmap)) 
                } 
            } 
        } 
    } 
    // 根据图片信息确定是否需要旋转图片
    private fun rotateIfRequired(bitmap: Bitmap): Bitmap { 
        val exif = ExifInterface(outputImage.path) 
        val orientation = exif.getAttributeInt(ExifInterface.TAG_ORIENTATION, 
            ExifInterface.ORIENTATION_NORMAL) 
        return when (orientation) { 
            ExifInterface.ORIENTATION_ROTATE_90 -&gt; rotateBitmap(bitmap, 90) 
            ExifInterface.ORIENTATION_ROTATE_180 -&gt; rotateBitmap(bitmap, 180) 
            ExifInterface.ORIENTATION_ROTATE_270 -&gt; rotateBitmap(bitmap, 270) 
            else -&gt; bitmap 
        } 
    } 
    private fun rotateBitmap(bitmap: Bitmap, degree: Int): Bitmap { 
        val matrix = Matrix() 
        matrix.postRotate(degree.toFloat()) 
        val rotatedBitmap = Bitmap.createBitmap(bitmap, 0, 0, bitmap.width, bitmap.height, 
            matrix, true) 
        bitmap.recycle() // 将不再需要的Bitmap对象回收 
        return rotatedBitmap 
    } 
}
</code></pre>
<h3 id="通过相册选择图片"><a href="#通过相册选择图片" class="headerlink" title="通过相册选择图片"></a>通过相册选择图片</h3><pre><code class="language-kotlin">class MainActivity : AppCompatActivity() { 
    ... 
    val fromAlbum = 2 
    override fun onCreate(savedInstanceState: Bundle?) { 
        ... 
        fromAlbumBtn.setOnClickListener { 
            // 打开文件选择器 
            val intent = Intent(Intent.ACTION_OPEN_DOCUMENT) 
            intent.addCategory(Intent.CATEGORY_OPENABLE) 
            // 指定只显示图片 
            intent.type = &quot;image/*&quot; 
            // fromAlbum：当选择完图片回到onActivityResult()方法时，
            // 就会进入fromAlbum的条件下处理图片。
            startActivityForResult(intent, fromAlbum) 
        } 
    } 
    override fun onActivityResult(requestCode: Int, resultCode: Int, data: Intent?) { 
        super.onActivityResult(requestCode, resultCode, data) 
        when (requestCode) { 
            ... 
            fromAlbum -&gt; { 
                if (resultCode == Activity.RESULT_OK &amp;&amp; data != null) { 
                    data.data?.let { uri -&gt; 
                        // 将选择的图片显示 
                        val bitmap = getBitmapFromUri(uri) 
                        imageView.setImageBitmap(bitmap) 
                    } 
                } 
            } 
        } 
    } 
    private fun getBitmapFromUri(uri: Uri) = contentResolver 
        .openFileDescriptor(uri, &quot;r&quot;)?.use { 
        BitmapFactory.decodeFileDescriptor(it.fileDescriptor) 
    } 
    ... 
} 
</code></pre>
<h3 id="播放多媒体文件"><a href="#播放多媒体文件" class="headerlink" title="播放多媒体文件"></a>播放多媒体文件</h3><p> 在Android中播放音频文件一般是使用MediaPlayer类实现的  </p>
<p><img loading="lazy" src="https://cdn.nlark.com/yuque/0/2026/png/40667138/1769330436756-40389ed9-a0ac-467a-90fc-49e806dd08a8.png"></p>
<p>播放音频（视频与之相似）</p>
<pre><code class="language-kotlin">class MainActivity : AppCompatActivity() { 
 
    private val mediaPlayer = MediaPlayer() 
 
    override fun onCreate(savedInstanceState: Bundle?) { 
        super.onCreate(savedInstanceState) 
        setContentView(R.layout.activity_main) 
        initMediaPlayer() 
        play.setOnClickListener { 
            if (!mediaPlayer.isPlaying) { 
                mediaPlayer.start() // 开始播放 
            } 
        } 
        pause.setOnClickListener { 
            if (mediaPlayer.isPlaying) { 
                mediaPlayer.pause() // 暂停播放 
            } 
        } 
        stop.setOnClickListener { 
            if (mediaPlayer.isPlaying) { 
                mediaPlayer.reset() // 停止播放 
                initMediaPlayer() 
            } 
        } 
    } 
 
    private fun initMediaPlayer() { 
        val assetManager = assets 
        val fd = assetManager.openFd(&quot;music.mp3&quot;) 
        mediaPlayer.setDataSource(fd.fileDescriptor, fd.startOffset, fd.length) 
        mediaPlayer.prepare() 
    } 
 
    override fun onDestroy() { 
        super.onDestroy() 
        mediaPlayer.stop() 
        mediaPlayer.release() 
    } 
 
} 
</code></pre>
<h1 id="网络"><a href="#网络" class="headerlink" title="网络"></a>网络</h1><h2 id="使用-webView-显示网页"><a href="#使用-webView-显示网页" class="headerlink" title="使用 webView 显示网页"></a>使用 webView 显示网页</h2><pre><code class="language-java"> webView.settings.javaScriptEnabled=true
 webView.webViewClient = WebViewClient()
 webView.loadUrl(&quot;https://www.baidu.com&quot;)
</code></pre>
<h2 id="HttpURLConnection"><a href="#HttpURLConnection" class="headerlink" title="HttpURLConnection"></a>HttpURLConnection</h2><h3 id="Get-方法"><a href="#Get-方法" class="headerlink" title="Get 方法"></a>Get 方法</h3><pre><code class="language-kotlin">// 调用实例
val url = URL(&quot;https://www.baidu.com&quot;)
val connection = url.openConnection() as HttpURLConnection
// 定义请求方法
connection.requestMethod = &quot;GET&quot;
// 定义请求头
connection.connectTimeout = 8000
connection.readTimeout = 8000
// 获取输入流
val input = connection.inputStream
connection.disconnect()
</code></pre>
<h3 id="Post-方法"><a href="#Post-方法" class="headerlink" title="Post 方法"></a>Post 方法</h3><pre><code class="language-kotlin">connection.requestMethod = &quot;POST&quot;
val output = DataOutputStream(connection.outputStream)
output.writeBytes(&quot;username=admin&amp;password=123456&quot;)
</code></pre>
<h2 id="OKHttp"><a href="#OKHttp" class="headerlink" title="OKHttp"></a>OKHttp</h2><blockquote>
<p>通过 <font style="color:rgb(38, 38, 38);">OKHttp 可以组件 GET POST 等请求</font></p>
</blockquote>
<h3 id="Get"><a href="#Get" class="headerlink" title="Get"></a>Get</h3><pre><code class="language-kotlin">val client = OkHttpClient()
// 创建Request对象
val request = Request.Builder().build()
val request = Request.Builder()
     .url(&quot;https://www.baidu.com&quot;)
     .build()
val response = client.newCall(request).execute()
val responseData = response.body?.string()
</code></pre>
<h2 id="Post"><a href="#Post" class="headerlink" title="Post"></a>Post</h2><pre><code class="language-kotlin">// 构建待提交参数
val requestBody = FormBody.Builder()
    .add(&quot;username&quot;, &quot;admin&quot;)
    .add(&quot;password&quot;, &quot;123456&quot;)
    .build()
val request = Request.Builder()
    .url(&quot;https://www.baidu.com&quot;)
    .post(requestBody)
    .build()
val response = client.newCall(request).execute()
val responseData = response.body?.string()
</code></pre>
<h2 id="XML-解析"><a href="#XML-解析" class="headerlink" title="XML 解析"></a>XML 解析</h2><p>网路传输数据的两种格式 XML 和 JSON。（其实当前 JSON 更多）</p>
<h3 id="Pull-解析"><a href="#Pull-解析" class="headerlink" title="Pull 解析"></a>Pull 解析</h3><p>XML 数据</p>
<pre><code class="language-xml">&lt;apps&gt;
  &lt;app&gt;
    &lt;id&gt;1&lt;/id&gt;
    &lt;name&gt;Google Maps&lt;/name&gt;
    &lt;version&gt;1.0&lt;/version&gt;
  &lt;/app&gt;
  &lt;app&gt;
    &lt;id&gt;2&lt;/id&gt;
    &lt;name&gt;Chrome&lt;/name&gt;
    &lt;version&gt;2.1&lt;/version&gt;
  &lt;/app&gt;
  &lt;app&gt;
    &lt;id&gt;3&lt;/id&gt;
    &lt;name&gt;Google Play&lt;/name&gt;
    &lt;version&gt;2.3&lt;/version&gt;
  &lt;/app&gt;
&lt;/apps&gt;
</code></pre>
<p>解析代码</p>
<pre><code class="language-kotlin">// 通过网络等获取的 XML数据
val response = client.newCall(request).execute()
val responseData = response.body?.string()
if (responseData != null) {
    parseXMLWithPull(responseData)
}
// 解析XML数据
private fun parseXMLWithPull(xmlData: String) {
    try {
        // 创建实例，输入数据
        val factory = XmlPullParserFactory.newInstance()
        val xmlPullParser = factory.newPullParser()
        xmlPullParser.setInput(StringReader(xmlData))
        // 获取当前解析时间
        var eventType = xmlPullParser.eventType
        var id = &quot;&quot;
        var name = &quot;&quot;
        var version = &quot;&quot;
        // 循环节点解析
        while (eventType != XmlPullParser.END_DOCUMENT) {
            val nodeName = xmlPullParser.name
            when (eventType) {
                // 开始解析某个节点
                XmlPullParser.START_TAG -&gt; {
                    when (nodeName) {
                        &quot;id&quot; -&gt; id = xmlPullParser.nextText()
                        &quot;name&quot; -&gt; name = xmlPullParser.nextText()
                        &quot;version&quot; -&gt; version = xmlPullParser.nextText()
                    }
                }
                // 完成解析某个节点
                XmlPullParser.END_TAG -&gt; {
                    if (&quot;app&quot; == nodeName) {
                        Log.d(&quot;MainActivity&quot;, &quot;id is $id&quot;)
                        Log.d(&quot;MainActivity&quot;, &quot;name is $name&quot;)
                        Log.d(&quot;MainActivity&quot;, &quot;version is $version&quot;)
                    }
                }
            }
            eventType = xmlPullParser.next()
        }
    } catch (e: Exception) {
        e.printStackTrace()
    }
}
}
</code></pre>
<p>当 XML 数据发生变化时（更多的子节点），代码就需要修改。</p>
<h3 id="SAX-解析"><a href="#SAX-解析" class="headerlink" title="SAX 解析"></a>SAX 解析</h3><p> 要使用SAX解析，通常情况下会新建一个类继承自DefaultHandler，并重写父类的5个 方法  </p>
<pre><code class="language-kotlin">class ContentHandler : DefaultHandler() {
    private var nodeName = &quot;&quot;
    private lateinit var id: StringBuilder
    private lateinit var name: StringBuilder
    private lateinit var version: StringBuilder
    // 初始化
    override fun startDocument() {
        id = StringBuilder()
        name = StringBuilder()
        version = StringBuilder()
    }
    override fun startElement(uri: String, localName: String, qName: String, attributes:
                           Attributes) {
        // 记录当前节点名
        nodeName = localName
        Log.d(&quot;ContentHandler&quot;, &quot;uri is $uri&quot;)
        Log.d(&quot;ContentHandler&quot;, &quot;localName is $localName&quot;)
        Log.d(&quot;ContentHandler&quot;, &quot;qName is $qName&quot;)
        Log.d(&quot;ContentHandler&quot;, &quot;attributes is $attributes&quot;)
    }
    override fun characters(ch: CharArray, start: Int, length: Int) {
        // 根据当前节点名判断将内容添加到哪一个StringBuilder对象中
        when (nodeName) {
            &quot;id&quot; -&gt; id.append(ch, start, length)
            &quot;name&quot; -&gt; name.append(ch, start, length)
            &quot;version&quot; -&gt; version.append(ch, start, length)
        }
    }
    override fun endElement(uri: String, localName: String, qName: String) {
        if (&quot;app&quot; == localName) {
            Log.d(&quot;ContentHandler&quot;, &quot;id is ${id.toString().trim()}&quot;)
            Log.d(&quot;ContentHandler&quot;, &quot;name is ${name.toString().trim()}&quot;)
            Log.d(&quot;ContentHandler&quot;, &quot;version is ${version.toString().trim()}&quot;)
            // 最后要将StringBuilder清空
            id.setLength(0)
            name.setLength(0)
            version.setLength(0)
        }
    }
    override fun endDocument() {
    }
}
</code></pre>
<p>调用</p>
<pre><code class="language-kotlin">private fun parseXMLWithSAX(xmlData: String) {
    try {
        val factory = SAXParserFactory.newInstance()
        val xmlReader = factory.newSAXParser().XMLReader
        val handler = ContentHandler()
        // 将ContentHandler的实例设置到XMLReader中
        xmlReader.contentHandler = handler
        // 开始执行解析
        xmlReader.parse(InputSource(StringReader(xmlData)))
    } catch (e: Exception) {
        e.printStackTrace()
    }
}
</code></pre>
<h2 id="JSON-解析"><a href="#JSON-解析" class="headerlink" title="JSON 解析"></a>JSON 解析</h2><p>示例数据  </p>
<pre><code class="language-json">[{&quot;id&quot;:&quot;5&quot;,&quot;version&quot;:&quot;5.5&quot;,&quot;name&quot;:&quot;Clash of Clans&quot;},
  {&quot;id&quot;:&quot;6&quot;,&quot;version&quot;:&quot;7.0&quot;,&quot;name&quot;:&quot;Boom Beach&quot;},
  {&quot;id&quot;:&quot;7&quot;,&quot;version&quot;:&quot;3.5&quot;,&quot;name&quot;:&quot;Clash Royale&quot;}]
</code></pre>
<h3 id="JSONObject"><a href="#JSONObject" class="headerlink" title="JSONObject"></a>JSONObject</h3><pre><code class="language-kotlin">private fun parseJSONWithJSONObject(jsonData: String) {
    try {
        val jsonArray = JSONArray(jsonData)
        for (i in 0 until jsonArray.length()) {
            val jsonObject = jsonArray.getJSONObject(i)
            val id = jsonObject.getString(&quot;id&quot;)
            val name = jsonObject.getString(&quot;name&quot;)
            val version = jsonObject.getString(&quot;version&quot;)
            Log.d(&quot;MainActivity&quot;, &quot;id is $id&quot;)
            Log.d(&quot;MainActivity&quot;, &quot;name is $name&quot;)
            Log.d(&quot;MainActivity&quot;, &quot;version is $version&quot;)
        }
    } catch (e: Exception) {
        e.printStackTrace()
    }
}
</code></pre>
<h3 id="GSON"><a href="#GSON" class="headerlink" title="GSON"></a>GSON</h3><p>自动转换为对象</p>
<pre><code class="language-kotlin">val gson = Gson()
val person = gson.fromJson(jsonData, Person::class.java)
</code></pre>
<p>对于数组</p>
<pre><code class="language-kotlin">val typeOf = object : TypeToken&lt;List&lt;Person&gt;&gt;() {}.type
val people = gson.fromJson&lt;List&lt;Person&gt;&gt;(jsonData, typeOf)
</code></pre>
<h2 id="网络请求回调"><a href="#网络请求回调" class="headerlink" title="网络请求回调"></a>网络请求回调</h2><pre><code class="language-kotlin">HttpUtil.sendOkHttpRequest(address, object : Callback {
    override fun onResponse(call: Call, response: Response) {
        // 得到服务器返回的具体内容
        val responseData = response.body?.string()
    }
    override fun onFailure(call: Call, e: IOException) {
        // 在这里对异常情况进行处理
    }
})
</code></pre>
<h2 id="Retrofit"><a href="#Retrofit" class="headerlink" title="Retrofit"></a>Retrofit</h2><ul>
<li>配置一次根路径，后续接口地址使用相对路径即可</li>
<li>可以对服务器接口进行归类，将功能同属一类的服务器接口定义到同一个接 口文件当中</li>
<li>将服务器返回的 JSON数据自动解析成对象</li>
</ul>
<p><strong>创建 APP 类</strong></p>
<pre><code class="language-kotlin">class App(val id: String, val name: String, val version: String) 
</code></pre>
<p><strong>根据功能创建不同种类的接口文件，其中对应具体服务器的接口方法</strong></p>
<pre><code class="language-kotlin">// 命名： 以具体的功能种类名开头，以Service结尾
interface AppService { 
    // 返回值必须声明成Retrofit中内置的Call类型
    @GET(&quot;get_data.json&quot;) 
    fun getAppData(): Call&lt;List&lt;App&gt;&gt; 
} 
</code></pre>
<p><strong>使用</strong></p>
<pre><code class="language-kotlin">// 构建对象并设置根路径
val retrofit = Retrofit.Builder() 
    .baseUrl(&quot;http://10.0.2.2/&quot;) 
    .addConverterFactory(GsonConverterFactory.create())   // 指定解析库
    .build() 
// 创建接口的动态代理对象  Retrofit会自动检查并通过创建代理对象实现接口。调用方法时由代理对象
// 截调用并执行网络请求
val appService = retrofit.create(AppService::class.java) 
appService.getAppData().enqueue(object : Callback&lt;List&lt;App&gt;&gt; { 
    override fun onResponse(call: Call&lt;List&lt;App&gt;&gt;, 
                                        response: Response&lt;List&lt;App&gt;&gt;) { 
        val list = response.body() 
        if (list != null) { 
            for (app in list) { 
                Log.d(&quot;MainActivity&quot;, &quot;id is ${app.id}&quot;) 
                Log.d(&quot;MainActivity&quot;, &quot;name is ${app.name}&quot;) 
                Log.d(&quot;MainActivity&quot;, &quot;version is ${app.version}&quot;) 
            } 
        } 
    } 
    override fun onFailure(call: Call&lt;List&lt;App&gt;&gt;, t: Throwable) { 
        t.printStackTrace() 
    } 
}) 
</code></pre>
<h3 id="复杂接口处理"><a href="#复杂接口处理" class="headerlink" title="复杂接口处理"></a>复杂接口处理</h3><pre><code class="language-kotlin">interface ExampleService { 
     // 接口动态变化
    @GET(&quot;{page}/get_data.json&quot;) 
    fun getData(@Path(&quot;page&quot;) page: Int): Call&lt;Data&gt; 

    // get方法传参： GET http://example.com/get_data.json?u=&lt;user&gt;&amp;t=&lt;token&gt; 
    @GET(&quot;get_data.json&quot;) 
    fun getData(@Query(&quot;u&quot;) user: String, @Query(&quot;t&quot;) token: String): Call&lt;Data&gt; 

    @DELETE(&quot;data/{id}&quot;) 
    fun deleteData(@Path(&quot;id&quot;) id: String): Call&lt;ResponseBody&gt; 
} 
</code></pre>
<p> 对于服务器响应的数据并不关心时，可以使用ResponseBody，表示Retrofit能够接收任意类型的响应数据，并且不会对 响应数据进行解析。  </p>
<pre><code class="language-kotlin">interface ExampleService { 
    @POST(&quot;data/create&quot;) 
    fun createData(@Body data: Data): Call&lt;ResponseBody&gt; 

    // 自定义 Header
    @Headers(&quot;User-Agent: okhttp&quot;, &quot;Cache-Control: max-age=0&quot;) 
    @GET(&quot;get_data.json&quot;) 
    fun getData(): Call&lt;Data&gt; 

    // Header 的动态声明
    @GET(&quot;get_data.json&quot;) 
    fun getData(@Header(&quot;User-Agent&quot;) userAgent: String, 
        @Header(&quot;Cache-Control&quot;) cacheControl: String): Call&lt;Data&gt; 
} 
</code></pre>
<pre><code class="language-kotlin">object ServiceCreator { 
    // 构建单例类避免retrofit重复创建
    private const val BASE_URL = &quot;http://10.0.2.2/&quot; 
    private val retrofit = Retrofit.Builder() 
        .baseUrl(BASE_URL) 
        .addConverterFactory(GsonConverterFactory.create()) 
        .build() 

     fun &lt;T&gt; create(serviceClass: Class&lt;T&gt;): T = retrofit.create(serviceClass) 
} 
</code></pre>
<p>创建方法可简化为</p>
<pre><code class="language-kotlin">object ServiceCreator { 
    ... 
    inline fun &lt;reified T&gt; create(): T = create(T::class.java) 
    //  inline： 确保编译器知道要替换的类型
    // reified：告诉编译器保留类型信息，允许在函数体内访问类型信息
} 

// 使用
val appService = ServiceCreator.create&lt;AppService&gt;() 
</code></pre>
<h1 id="服务"><a href="#服务" class="headerlink" title="服务"></a>服务</h1><ul>
<li>Android 中实现程序后台运行的解决方案</li>
<li>当应用程序进程被杀掉时，所有依赖于该进程的服务也会停止</li>
<li>Service并不会自动开启线程，所有的代码 都是默认运行在主线程当中的。也就是说，我们需要在Service的内部手动创建子线程，并在这 里执行具体的任务，否则就有可能出现主线程被阻塞的情况。</li>
</ul>
<h2 id="Android-多线程编程"><a href="#Android-多线程编程" class="headerlink" title="Android 多线程编程"></a>Android 多线程编程</h2><ul>
<li>继承 Thread 类</li>
</ul>
<pre><code class="language-kotlin">class MyThread : Thread() {
    override fun run() {
        // 编写具体的逻辑
    }
}

// 启动
MyThread().start()
</code></pre>
<ul>
<li>实现 Runnable 接口</li>
</ul>
<pre><code class="language-kotlin">class MyThread : Runnable {
    override fun run() {
        // 编写具体的逻辑
    }
}

// 启动
val myThread = MyThread()
Thread(myThread).start()
</code></pre>
<ul>
<li>编写匿名类</li>
</ul>
<pre><code class="language-kotlin">Thread {
    // 编写具体的逻辑
}.start()
</code></pre>
<h3 id="子线程中更新-UI"><a href="#子线程中更新-UI" class="headerlink" title="子线程中更新 UI"></a>子线程中更新 UI</h3><blockquote>
<p>Android 中不允许在子线程中更新 UI</p>
</blockquote>
<p>通过异步消息处理机制，实现控件的更新。</p>
<pre><code class="language-kotlin">class MainActivity : AppCompatActivity() {
    val updateText = 1
    // Handler 被绑定到了主线程的消息队列（MessageQueue）上
    val handler = object : Handler(Looper.getMainLooper()) {
        override fun handleMessage(msg: Message) {
            // 在这里可以进行UI操作 when语句
            when (msg.what) {
                updateText -&gt; textView.text = &quot;Nice to meet you&quot;
            }
        }
    }
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)
        changeTextBtn.setOnClickListener {
            thread {
                val msg = Message()
                msg.what = updateText
                // 发送至主线程的消息队列
                handler.sendMessage(msg) // 将Message对象发送出去
            }
        }
    }
}
</code></pre>
<p>异步消息处理的构成</p>
<ul>
<li>Message： 线程之间传递的消息</li>
<li>Handler： 发送和处理消息   sendMessage()方法、post()方法  发送消息，传递到  handleMessage()方法  进行处理</li>
<li>MessageQueue：消息队列，存放所有 Handler 的消息，每个线程中只有一个</li>
<li>Looper: 管理线程 中的消息队列</li>
</ul>
<p>AsyncTask 使用</p>
<ul>
<li>onProExecute 执行前调用</li>
<li>doInBackground  子线程中运行的</li>
<li>onProgressUpdate 响应后台中 publishProgress是 更新 UI</li>
<li>onPostExecute 任务执行完毕后的操作</li>
</ul>
<pre><code class="language-kotlin">// 接收参数： 传入参数,进度，返回值
class DownloadTask : AsyncTask&lt;Unit, Int, Boolean&gt;() {
    override fun onPreExecute() {
        progressDialog.show() // 显示进度对话框
    }
    override fun doInBackground(vararg params: Unit?) = try {
        while (true) {
            val downloadPercent = doDownload() // 这是一个虚构的方法
            // 更新UI等操作
            publishProgress(downloadPercent)
            if (downloadPercent &gt;= 100) {
                break
            }
        }
        true
    } catch (e: Exception) {
        false
    }
    // 响应更新UI
    override fun onProgressUpdate(vararg values: Int?) {
        // 在这里更新下载进度
        progressDialog.setMessage(&quot;Downloaded ${values[0]}%&quot;)
    }
    override fun onPostExecute(result: Boolean) {
        progressDialog.dismiss()// 关闭进度对话框
        // 在这里提示下载结果
        if (result) {
            Toast.makeText(context, &quot;Download succeeded&quot;, Toast.LENGTH_SHORT).show()
        } else {
            Toast.makeText(context, &quot; Download failed&quot;, Toast.LENGTH_SHORT).show()
        }
    }
}
</code></pre>
<h2 id="服务-1"><a href="#服务-1" class="headerlink" title="服务"></a>服务</h2><pre><code class="language-kotlin">class MyService : Service() {
    ...
    override fun onCreate() {
        super.onCreate()
    }
    override fun onStartCommand(intent: Intent, flags: Int, startId: Int): Int {
        return super.onStartCommand(intent, flags, startId)
    }
    override fun onDestroy() {
        super.onDestroy()
    }
}
</code></pre>
<ul>
<li>定义后通过 Intent 启动或停止（startService、stopService）</li>
<li>服务实例只有一个，但每次调用 startService，onStartCommand 就会执行一次</li>
</ul>
<p><strong>服务的启动和停止</strong></p>
<pre><code class="language-kotlin">startServiceBtn.setOnClickListener {
    val intent = Intent(this, MyService::class.java)
    startService(intent) // 启动Service
}
stopServiceBtn.setOnClickListener {
    val intent = Intent(this, MyService::class.java)
    stopService(intent) // 停止Service
}
</code></pre>
<p>**前台服务：**服务中加入通知(通知栏展示)并调用 startForeground</p>
<p>**IntentService：**异步、自动停止  </p>
<h3 id="活动与服务的通信"><a href="#活动与服务的通信" class="headerlink" title="活动与服务的通信"></a>活动与服务的通信</h3><ul>
<li>构建 Binder 对象，在 Service 时使用 onBind 方法。</li>
</ul>
<pre><code class="language-kotlin">class MyService : Service() {
    private val mBinder = DownloadBinder()
    class DownloadBinder : Binder() {
        fun startDownload() {
            Log.d(&quot;MyService&quot;, &quot;startDownload executed&quot;)
        }
        fun getProgress(): Int {
            Log.d(&quot;MyService&quot;, &quot;getProgress executed&quot;)
            return 0
        }
    }
    override fun onBind(intent: Intent): IBinder {
        return mBinder
    }
        ...
}
</code></pre>
<ul>
<li>活动中调用 Binder 方法。当一个Activity和Service绑定了之后，就可以调用该Service里的Binder提供的方法了。</li>
</ul>
<pre><code class="language-kotlin">class MainActivity : AppCompatActivity() {
    lateinit var downloadBinder: MyService.DownloadBinder
    private val connection = object : ServiceConnection {
        override fun onServiceConnected(name: ComponentName, service: IBinder) {
            downloadBinder = service as MyService.DownloadBinder
            downloadBinder.startDownload()
            downloadBinder.getProgress()
        }
        override fun onServiceDisconnected(name: ComponentName) {
        }
    }
    override fun onCreate(savedInstanceState: Bundle?) {
        ...
        bindServiceBtn.setOnClickListener {
            val intent = Intent(this, MyService::class.java)
            bindService(intent, connection, Context.BIND_AUTO_CREATE) // 绑定Service
        }
        unbindServiceBtn.setOnClickListener {
            unbindService(connection) // 解绑Service
        }
    }
}
</code></pre>
<h3 id="服务的周期"><a href="#服务的周期" class="headerlink" title="服务的周期"></a>服务的周期</h3><p><strong>生命周期流程：</strong><br><code>&lt;font style=&quot;color:rgb(6, 10, 38);&quot;&gt;onCreate()&lt;/font&gt;</code><font style="color:rgb(6, 10, 38);"> </font><font style="color:rgb(6, 10, 38);">➡️</font><font style="color:rgb(6, 10, 38);"> </font><code>&lt;font style=&quot;color:rgb(6, 10, 38);&quot;&gt;onStartCommand()&lt;/font&gt;</code><font style="color:rgb(6, 10, 38);"> </font><font style="color:rgb(6, 10, 38);">➡️</font><font style="color:rgb(6, 10, 38);"> … (服务运行中) … </font><font style="color:rgb(6, 10, 38);">➡️</font><font style="color:rgb(6, 10, 38);"> </font><code>&lt;font style=&quot;color:rgb(6, 10, 38);&quot;&gt;onDestroy()&lt;/font&gt;</code></p>
<h4 id="通过-bindService-绑定的生命周期"><a href="#通过-bindService-绑定的生命周期" class="headerlink" title="通过 bindService() 绑定的生命周期"></a>通过 <code>bindService()</code> 绑定的生命周期</h4><p><code>&lt;font style=&quot;color:rgb(6, 10, 38);&quot;&gt;onCreate()&lt;/font&gt;</code><font style="color:rgb(6, 10, 38);"> </font><font style="color:rgb(6, 10, 38);">➡️</font><font style="color:rgb(6, 10, 38);"> </font><code>&lt;font style=&quot;color:rgb(6, 10, 38);&quot;&gt;onBind()&lt;/font&gt;</code><font style="color:rgb(6, 10, 38);"> </font><font style="color:rgb(6, 10, 38);">➡️</font><font style="color:rgb(6, 10, 38);"> … (服务运行中，可进行交互) … </font><font style="color:rgb(6, 10, 38);">➡️</font><font style="color:rgb(6, 10, 38);"> </font><code>&lt;font style=&quot;color:rgb(6, 10, 38);&quot;&gt;onUnbind()&lt;/font&gt;</code><font style="color:rgb(6, 10, 38);"> </font><font style="color:rgb(6, 10, 38);">➡️</font><font style="color:rgb(6, 10, 38);"> </font><code>&lt;font style=&quot;color:rgb(6, 10, 38);&quot;&gt;onDestroy()&lt;/font&gt;</code></p>
<ul>
<li><code>**onBind()**</code>：当客户端绑定到服务时调用。必须在这个方法中返回一个 <code>IBinder</code> 接口实例，以便客户端能通过它与服务通信。</li>
<li><code>**onUnbind()**</code>：当所有客户端都与服务解除绑定时调用。</li>
</ul>
<h2 id="前台服务"><a href="#前台服务" class="headerlink" title="前台服务"></a>前台服务</h2><blockquote>
<p> 从Android 8.0系统开始，只有当应用保持在前台可见状态的情况下，Service 才能保证稳定运行，一旦应用进入后台之后，Service随时都有可能被系统回收。  </p>
</blockquote>
<pre><code class="language-kotlin">class MyService : Service() {
    ...
    override fun onCreate() {
        super.onCreate()
        // 在 MainActivity（或其他组件）中通过调用 startForegroundService() 被启动的
        Log.d(&quot;MyService&quot;, &quot;onCreate executed&quot;)
        val manager = getSystemService(Context.NOTIFICATION_SERVICE) as
        NotificationManager
        if (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.O) {
            val channel = NotificationChannel(&quot;my_service&quot;, &quot;前台Service通知&quot;,
                                   NotificationManager.IMPORTANCE_DEFAULT)
            manager.createNotificationChannel(channel)
        }
        val intent = Intent(this, MainActivity::class.java)
        val pi = PendingIntent.getActivity(this, 0, intent, 0)
        val notification = NotificationCompat.Builder(this, &quot;my_service&quot;)
            .setContentTitle(&quot;This is content title&quot;)
            .setContentText(&quot;This is content text&quot;)
            .setSmallIcon(R.drawable.small_icon)
            .setLargeIcon(BitmapFactory.decodeResource(resources, R.drawable.large_icon))
            .setContentIntent(pi)
            .build()
        // 改用 startForeground 使得服务转为前台服务
        startForeground(1, notification)
    }
        ...
}
</code></pre>
<h2 id="IntentService（已启用）"><a href="#IntentService（已启用）" class="headerlink" title="IntentService（已启用）"></a>IntentService（已启用）</h2><p> 异步的、会自动停止的Service  （避免 ANR 以及 线程忘记关闭的问题）</p>
<blockquote>
<p><strong><font style="color:rgba(0, 0, 0, 0.8);background-color:rgb(248, 244, 241);">IntentService</font></strong><font style="color:rgba(0, 0, 0, 0.8);background-color:rgb(248, 244, 241);"> 在 Android API 30（Android 11）中被正式标记为 </font><strong><font style="color:rgba(0, 0, 0, 0.8);background-color:rgb(248, 244, 241);">deprecated</font></strong><font style="color:rgba(0, 0, 0, 0.8);background-color:rgb(248, 244, 241);">，虽然旧代码仍可运行，但未来版本可能彻底移除。</font></p>
</blockquote>
<pre><code class="language-kotlin">class MyIntentService : IntentService(&quot;MyIntentService&quot;) {
    override fun onHandleIntent(intent: Intent?) {
        // 打印当前线程的id   已经是在子线程了
        Log.d(&quot;MyIntentService&quot;, &quot;Thread id is ${Thread.currentThread().name}&quot;)
    }
    override fun onDestroy() {
        super.onDestroy()
        Log.d(&quot;MyIntentService&quot;, &quot;onDestroy executed&quot;)
    }
}
</code></pre>
<h1 id="基于位置的服务"><a href="#基于位置的服务" class="headerlink" title="基于位置的服务"></a>基于位置的服务</h1><ul>
<li>申请 API Key 并下载 SDK</li>
<li>权限声明</li>
<li>获取位置信息包括： 经纬度、地址、地图</li>
</ul>
<h1 id="Material-Design"><a href="#Material-Design" class="headerlink" title="Material Design"></a>Material Design</h1><h2 id="ToolBar"><a href="#ToolBar" class="headerlink" title="ToolBar"></a>ToolBar</h2><p>在 style.xml 中文件， 指定一个不带ActionBar的主题，通 常有Theme.AppCompat.NoActionBar 和Theme.AppCompat.Light.NoActionBar这两种 主题可选。  </p>
<pre><code class="language-xml">&lt;FrameLayout xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot; 
  &lt;!-- 定义新的命名空间以保证兼容性 --&gt;
  xmlns:app=&quot;http://schemas.android.com/apk/res-auto&quot; 
  android:layout_width=&quot;match_parent&quot; 
  android:layout_height=&quot;match_parent&quot;&gt; 
  &lt;androidx.appcompat.widget.Toolbar 
    android:id=&quot;@+id/toolbar&quot; 
    android:layout_width=&quot;match_parent&quot; 
    android:layout_height=&quot;?attr/actionBarSize&quot; 
    android:background=&quot;@color/colorPrimary&quot; 
    &lt;!-- 指定使用的主题 --&gt;
    android:theme=&quot;@style/ThemeOverlay.AppCompat.Dark.ActionBar&quot; 
    app:popupTheme=&quot;@style/ThemeOverlay.AppCompat.Light&quot; /&gt; 
&lt;/FrameLayout&gt; 
</code></pre>
<p>在 Activity 中添加  setSupportActionBar(toolbar)  </p>
<h2 id="滑动菜单-DrawerLayout"><a href="#滑动菜单-DrawerLayout" class="headerlink" title="滑动菜单 DrawerLayout"></a>滑动菜单 DrawerLayout</h2><ul>
<li>第一个子控件显示主页面内容，第二个为滑动菜单中内容</li>
</ul>
<pre><code class="language-xml">&lt;androidx.drawerlayout.widget.DrawerLayout 
    xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot; 
    xmlns:app=&quot;http://schemas.android.com/apk/res-auto&quot; 
    android:id=&quot;@+id/drawerLayout&quot; 
    android:layout_width=&quot;match_parent&quot; 
    android:layout_height=&quot;match_parent&quot;&gt; 
    &lt;FrameLayout 
        android:layout_width=&quot;match_parent&quot; 
        android:layout_height=&quot;match_parent&quot;&gt; 
        &lt;androidx.appcompat.widget.Toolbar 
            android:id=&quot;@+id/toolbar&quot; 
            android:layout_width=&quot;match_parent&quot; 
            android:layout_height=&quot;?attr/actionBarSize&quot; 
            android:background=&quot;@color/colorPrimary&quot; 
            android:theme=&quot;@style/ThemeOverlay.AppCompat.Dark.ActionBar&quot; 
            app:popupTheme=&quot;@style/ThemeOverlay.AppCompat.Light&quot; /&gt; 
    &lt;/FrameLayout&gt; 
    &lt;TextView 
        android:layout_width=&quot;match_parent&quot; 
        android:layout_height=&quot;match_parent&quot; 
        android:layout_gravity=&quot;start&quot; 
        android:background=&quot;#FFF&quot; 
        android:text=&quot;This is menu&quot; 
        android:textSize=&quot;30sp&quot; /&gt; 
&lt;/androidx.drawerlayout.widget.DrawerLayout&gt; 
</code></pre>
<p>第二个子空间中， ，layout_gravity这个属性是必须指定的  ，以表明活动菜单时左滑还是右化。</p>
<pre><code class="language-kotlin">class MainActivity : AppCompatActivity() { 
    override fun onCreate(savedInstanceState: Bundle?) { 
        super.onCreate(savedInstanceState) 
        setContentView(R.layout.activity_main) 
        setSupportActionBar(toolbar) 
        supportActionBar?.let { 
            // 显示按钮
            it.setDisplayHomeAsUpEnabled(true) 
            // 设置图标篇
            it.setHomeAsUpIndicator(R.drawable.ic_menu) 
        } 
    } 
        ... 
    override fun onOptionsItemSelected(item: MenuItem): Boolean { 
        when (item.itemId) { 
            android.R.id.home -&gt; drawerLayout.openDrawer(GravityCompat.START) 
                ... 
        } 
        return true 
    } 
} 
</code></pre>
<h2 id="NavigationView"><a href="#NavigationView" class="headerlink" title="NavigationView"></a>NavigationView</h2><p>可以自定义滑动菜单中顶部和菜单布局</p>
<pre><code class="language-xml">&lt;androidx.drawerlayout.widget.DrawerLayout 
  xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot; 
  xmlns:app=&quot;http://schemas.android.com/apk/res-auto&quot; 
  android:id=&quot;@+id/drawerLayout&quot; 
  android:layout_width=&quot;match_parent&quot; 
  android:layout_height=&quot;match_parent&quot;&gt; 

  &lt;FrameLayout 
    android:layout_width=&quot;match_parent&quot; 
    android:layout_height=&quot;match_parent&quot;&gt; 
    &lt;androidx.appcompat.widget.Toolbar 
      android:id=&quot;@+id/toolbar&quot; 
      android:layout_width=&quot;match_parent&quot; 
      android:layout_height=&quot;?attr/actionBarSize&quot; 
      android:background=&quot;@color/colorPrimary&quot; 
      android:theme=&quot;@style/ThemeOverlay.AppCompat.Dark.ActionBar&quot; 
      app:popupTheme=&quot;@style/ThemeOverlay.AppCompat.Light&quot; /&gt; 
  &lt;/FrameLayout&gt; 
  &lt;com.google.android.material.navigation.NavigationView 
    android:id=&quot;@+id/navView&quot; 
    android:layout_width=&quot;match_parent&quot; 
    android:layout_height=&quot;match_parent&quot; 
    android:layout_gravity=&quot;start&quot; 
    &lt;!-- 菜单 --&gt;
    app:menu=&quot;@menu/nav_menu&quot; 
    &lt;!-- 导航头像 --&gt;
    app:headerLayout=&quot;@layout/nav_header&quot;/&gt; 
&lt;/androidx.drawerlayout.widget.DrawerLayout&gt; 
</code></pre>
<h2 id="FloatingActionButton-悬浮按钮"><a href="#FloatingActionButton-悬浮按钮" class="headerlink" title="FloatingActionButton 悬浮按钮"></a>FloatingActionButton 悬浮按钮</h2><pre><code class="language-xml">&lt;com.google.android.material.floatingactionbutton.FloatingActionButton 
  android:id=&quot;@+id/fab&quot; 
  android:layout_width=&quot;wrap_content&quot; 
  android:layout_height=&quot;wrap_content&quot; 
  android:layout_gravity=&quot;bottom|end&quot; 
  android:layout_margin=&quot;16dp&quot; 
  android:src=&quot;@drawable/ic_done&quot; /&gt; 
</code></pre>
<p>对应其可以添加点击事件</p>
<h2 id="SnackBar-提示并增加可操作按钮"><a href="#SnackBar-提示并增加可操作按钮" class="headerlink" title="SnackBar  提示并增加可操作按钮"></a>SnackBar  提示并增加可操作按钮</h2><pre><code class="language-kotlin">fab.setOnClickListener { view -&gt; 
    Snackbar.make(view, &quot;Data deleted&quot;, Snackbar.LENGTH_SHORT) 
        .setAction(&quot;Undo&quot;) { 
            Toast.makeText(this, &quot;Data restored&quot;, Toast.LENGTH_SHORT).show() 
        } 
        .show() 
} 
</code></pre>
<h2 id="CoordinatorLayout"><a href="#CoordinatorLayout" class="headerlink" title="CoordinatorLayout"></a>CoordinatorLayout</h2><p>监听所有子控件的时间，自动做出合理响应</p>
<h2 id="CardVuiew-卡片布局"><a href="#CardVuiew-卡片布局" class="headerlink" title="CardVuiew 卡片布局"></a>CardVuiew 卡片布局</h2><pre><code class="language-xml">&lt;com.google.android.material.card.MaterialCardView 
  xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot; 
  xmlns:app=&quot;http://schemas.android.com/apk/res-auto&quot; 
  android:layout_width=&quot;match_parent&quot; 
  android:layout_height=&quot;wrap_content&quot; 
  android:layout_margin=&quot;5dp&quot; 
  app:cardCornerRadius=&quot;4dp&quot;&gt; 

  &lt;LinearLayout 
    android:orientation=&quot;vertical&quot; 
    android:layout_width=&quot;match_parent&quot; 
    android:layout_height=&quot;wrap_content&quot;&gt; 

    &lt;ImageView 
      android:id=&quot;@+id/fruitImage&quot; 
      android:layout_width=&quot;match_parent&quot; 
      android:layout_height=&quot;100dp&quot; 
      android:scaleType=&quot;centerCrop&quot; /&gt; 

    &lt;TextView 
      android:id=&quot;@+id/fruitName&quot; 
      android:layout_width=&quot;wrap_content&quot; 
      android:layout_height=&quot;wrap_content&quot; 
      android:layout_gravity=&quot;center_horizontal&quot; 
      android:layout_margin=&quot;5dp&quot; 
      android:textSize=&quot;16sp&quot; /&gt; 
  &lt;/LinearLayout&gt; 

&lt;/com.google.android.material.card.MaterialCardView&gt; 
</code></pre>
<h2 id="AppBarLayout"><a href="#AppBarLayout" class="headerlink" title="AppBarLayout"></a>AppBarLayout</h2><p>AppBarLayout实际 上是一个垂直方向的LinearLayout，它在内部做了很多滚动事件的封装，并应用了一些 Material Design的设计理念。  </p>
<pre><code class="language-xml">&lt;androidx.drawerlayout.widget.DrawerLayout 
    xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot; 
    xmlns:app=&quot;http://schemas.android.com/apk/res-auto&quot; 
    android:id=&quot;@+id/drawerLayout&quot; 
    android:layout_width=&quot;match_parent&quot; 
    android:layout_height=&quot;match_parent&quot;&gt; 
 
    &lt;androidx.coordinatorlayout.widget.CoordinatorLayout 
        android:layout_width=&quot;match_parent&quot; 
        android:layout_height=&quot;match_parent&quot;&gt; 
 
        &lt;com.google.android.material.appbar.AppBarLayout 
            android:layout_width=&quot;match_parent&quot; 
            android:layout_height=&quot;wrap_content&quot;&gt; 
 
            &lt;androidx.appcompat.widget.Toolbar 
                android:id=&quot;@+id/toolbar&quot; 
                android:layout_width=&quot;match_parent&quot; 
                android:layout_height=&quot;?attr/actionBarSize&quot; 
                android:background=&quot;@color/colorPrimary&quot; 
                android:theme=&quot;@style/ThemeOverlay.AppCompat.Dark.ActionBar&quot; 
                app:popupTheme=&quot;@style/ThemeOverlay.AppCompat.Light&quot;
              &lt;!-- 通过app:layout_scrollFlags响应滚动事件 --&gt;
                app:layout_scrollFlags=&quot;scroll|enterAlways|snap&quot; /&gt; 
 
        &lt;/com.google.android.material.appbar.AppBarLayout&gt; 
 
        &lt;androidx.recyclerview.widget.RecyclerView 
            android:id=&quot;@+id/recyclerView&quot; 
            android:layout_width=&quot;match_parent&quot; 
            android:layout_height=&quot;match_parent&quot; 
          &lt;!-- 指定布局行为 --&gt;
            app:layout_behavior=&quot;@string/appbar_scrolling_view_behavior&quot; /&gt; 
        ... 
    &lt;/androidx.coordinatorlayout.widget.CoordinatorLayout&gt; 
    ... 
&lt;/androidx.drawerlayout.widget.DrawerLayout&gt; 
</code></pre>
<p> 当RecyclerView滚动的时候就已经将滚动事件通知给AppBarLayout 。</p>
<h2 id="SwipeRefreshLayout-下拉刷新"><a href="#SwipeRefreshLayout-下拉刷新" class="headerlink" title="SwipeRefreshLayout 下拉刷新"></a>SwipeRefreshLayout 下拉刷新</h2><p> SwipeRefreshLayout用于实现下拉刷新功能的核心类，把想要实现下拉刷新功能的 控件放置到SwipeRefreshLayout中，就可以迅速让这个控件支持下拉刷新。  </p>
<pre><code class="language-xml">&lt;androidx.drawerlayout.widget.DrawerLayout 
    xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot; 
    xmlns:app=&quot;http://schemas.android.com/apk/res-auto&quot; 
    android:id=&quot;@+id/drawerLayout&quot; 
    android:layout_width=&quot;match_parent&quot; 
    android:layout_height=&quot;match_parent&quot;&gt; 
 
    &lt;androidx.coordinatorlayout.widget.CoordinatorLayout 
        android:layout_width=&quot;match_parent&quot; 
        android:layout_height=&quot;match_parent&quot;&gt; 
        ... 
        &lt;androidx.swiperefreshlayout.widget.SwipeRefreshLayout 
            android:id=&quot;@+id/swipeRefresh&quot; 
            android:layout_width=&quot;match_parent&quot; 
            android:layout_height=&quot;match_parent&quot; 
            app:layout_behavior=&quot;@string/appbar_scrolling_view_behavior&quot;&gt; 
 
            &lt;androidx.recyclerview.widget.RecyclerView 
                android:id=&quot;@+id/recyclerView&quot; 
                android:layout_width=&quot;match_parent&quot; 
                android:layout_height=&quot;match_parent&quot; 
                app:layout_behavior=&quot;@string/appbar_scrolling_view_behavior&quot; /&gt; 
 
        &lt;/androidx.swiperefreshlayout.widget.SwipeRefreshLayout&gt; 
        ... 
    &lt;/androidx.coordinatorlayout.widget.CoordinatorLayout&gt; 
    ... 
&lt;/androidx.drawerlayout.widget.DrawerLayout&gt; 
</code></pre>
<p>下拉刷新逻辑</p>
<pre><code class="language-kotlin">class MainActivity : AppCompatActivity() { 
    ... 
    override fun onCreate(savedInstanceState: Bundle?) { 
        super.onCreate(savedInstanceState) 
        setContentView(R.layout.activity_main) 
            ... 
        swipeRefresh.setColorSchemeResources(R.color.colorPrimary) 
        swipeRefresh.setOnRefreshListener { 
            refreshFruits(adapter) 
        } 
    } 
} 
private fun refreshFruits(adapter: FruitAdapter) { 
    thread { 
        Thread.sleep(2000) 
        runOnUiThread { 
            initFruits() 
            adapter.notifyDataSetChanged() 
            swipeRefresh.isRefreshing = false 
        } 
    } 
} 
</code></pre>
<h2 id="可折叠式标题栏"><a href="#可折叠式标题栏" class="headerlink" title="可折叠式标题栏"></a>可折叠式标题栏</h2><h3 id="CollapsingToolbarLayout"><a href="#CollapsingToolbarLayout" class="headerlink" title="CollapsingToolbarLayout"></a>CollapsingToolbarLayout</h3><p> CollapsingToolbarLayout是不能独立存在的，它在设计的时候就被限定只能作为 AppBarLayout的直接子布局来使用。而AppBarLayout又必须是CoordinatorLayout的子布局  </p>
<pre><code class="language-xml">&lt;androidx.coordinatorlayout.widget.CoordinatorLayout 
  xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot; 
  xmlns:app=&quot;http://schemas.android.com/apk/res-auto&quot; 
  android:layout_width=&quot;match_parent&quot; 
  android:layout_height=&quot;match_parent&quot;&gt; 
  &lt;com.google.android.material.appbar.AppBarLayout 
    android:id=&quot;@+id/appBar&quot; 
    android:layout_width=&quot;match_parent&quot; 
    android:layout_height=&quot;250dp&quot;&gt; 
    &lt;com.google.android.material.appbar.CollapsingToolbarLayout 
      android:id=&quot;@+id/collapsingToolbar&quot; 
      android:layout_width=&quot;match_parent&quot; 
      android:layout_height=&quot;match_parent&quot; 
      android:theme=&quot;@style/ThemeOverlay.AppCompat.Dark.ActionBar&quot; 
      app:contentScrim=&quot;@color/colorPrimary&quot; 
      app:layout_scrollFlags=&quot;scroll|exitUntilCollapsed&quot;&gt; 

      &lt;ImageView 
        android:id=&quot;@+id/fruitImageView&quot; 
        android:layout_width=&quot;match_parent&quot; 
        android:layout_height=&quot;match_parent&quot; 
        android:scaleType=&quot;centerCrop&quot; 
        app:layout_collapseMode=&quot;parallax&quot; /&gt; 
      &lt;androidx.appcompat.widget.Toolbar 
        android:id=&quot;@+id/toolbar&quot; 
        android:layout_width=&quot;match_parent&quot; 
        android:layout_height=&quot;?attr/actionBarSize&quot; 
        app:layout_collapseMode=&quot;pin&quot; /&gt; 

    &lt;/com.google.android.material.appbar.CollapsingToolbarLayout&gt; 
  &lt;/com.google.android.material.appbar.AppBarLayout&gt; 
&lt;/androidx.coordinatorlayout.widget.CoordinatorLayout&gt; 
</code></pre>
<p><strong><font style="color:rgb(0, 79, 180);">contentScrim：</font></strong><font style="color:rgb(38, 44, 49);">趋于折叠状态以及折叠之后的背景色</font></p>
<p><strong><font style="color:rgb(38, 44, 49);">layout_scrollFlags ：</font></strong>** **scroll表 示CollapsingToolbarLayout会随着水果内容详情的滚动一起滚动，exitUntilCollapsed：当CollapsingToolbarLayout随着滚动完成折叠之后就保留在界面上，不再移出屏幕。</p>
<p> layout_collapseMode ：   pin，表示在折叠的过程中位置始终保持不变  ， parallax，表示会在折叠的 过程中产生一定的错位偏移  </p>
<p>** 利用系统状态栏空间  **：将控件的android:fitsSystemWindows属性指定成true，就表示该控件会出现在系统状态栏里。  </p>
<h1 id="Jetpack⭐⭐"><a href="#Jetpack⭐⭐" class="headerlink" title="Jetpack⭐⭐"></a>Jetpack⭐⭐</h1><blockquote>
<p><strong>Jetpack</strong> 是 Google 官方推出的一套**“现代化开发组件库”<strong>的总称。它包含了很多部分：架构组件（如 ViewModel、Room）、UI 组件、行为组件（如 Navigation、WorkManager）等。<strong>Jetpack Compose</strong> 是在这个体系下诞生的，它是 Jetpack 中专门负责</strong>“构建用户界面 (UI)”**的新工具包。</p>
</blockquote>
<h2 id="MVVM-在-Jetpack-中的对应关系"><a href="#MVVM-在-Jetpack-中的对应关系" class="headerlink" title="MVVM 在 Jetpack 中的对应关系"></a><font style="color:rgb(17, 17, 51);">MVVM 在 Jetpack 中的对应关系</font></h2><table>
<thead>
<tr>
<th><strong><font style="color:rgb(17, 17, 51);">MVVM 角色</font></strong></th>
<th><strong><font style="color:rgb(17, 17, 51);">Jetpack 组件</font></strong></th>
<th><strong><font style="color:rgb(17, 17, 51);">职责说明</font></strong></th>
</tr>
</thead>
<tbody><tr>
<td><strong><font style="color:rgb(17, 17, 51);">View</font></strong></td>
<td><code>&lt;font style=&quot;color:rgb(17, 17, 51);background-color:rgba(175, 184, 193, 0.2);&quot;&gt;Activity&lt;/font&gt;</code><br/><font style="color:rgb(17, 17, 51);"> </font><font style="color:rgb(17, 17, 51);">&#x2F;</font><font style="color:rgb(17, 17, 51);"> </font><code>&lt;font style=&quot;color:rgb(17, 17, 51);background-color:rgba(175, 184, 193, 0.2);&quot;&gt;Fragment&lt;/font&gt;</code></td>
<td><font style="color:rgb(17, 17, 51);">负责 UI 展示和用户交互，观察 ViewModel 中的数据变化并更新界面。</font></td>
</tr>
<tr>
<td><strong><font style="color:rgb(17, 17, 51);">ViewModel</font></strong></td>
<td><code>&lt;font style=&quot;color:rgb(17, 17, 51);background-color:rgba(175, 184, 193, 0.2);&quot;&gt;androidx.lifecycle.ViewModel&lt;/font&gt;</code></td>
<td><font style="color:rgb(17, 17, 51);">持有和管理 UI 相关的状态数据，为 View 提供数据，</font><strong><font style="color:rgb(17, 17, 51);">不持有 View 的引用</font></strong><font style="color:rgb(17, 17, 51);">，生命周期感知。</font></td>
</tr>
<tr>
<td><strong><font style="color:rgb(17, 17, 51);">Model</font></strong></td>
<td><font style="color:rgb(17, 17, 51);">Repository &#x2F; Data Source（如 Room、Retrofit、本地数据库、网络 API 等）</font></td>
<td><font style="color:rgb(17, 17, 51);">负责数据获取、缓存、业务逻辑，与 ViewModel 通信，</font><strong><font style="color:rgb(17, 17, 51);">不直接接触 UI</font></strong><font style="color:rgb(17, 17, 51);">。</font></td>
</tr>
<tr>
<td><strong><font style="color:rgb(17, 17, 51);">数据绑定机制</font></strong></td>
<td><code>&lt;font style=&quot;color:rgb(17, 17, 51);background-color:rgba(175, 184, 193, 0.2);&quot;&gt;LiveData&lt;/font&gt;</code><br/><font style="color:rgb(17, 17, 51);">（或</font><font style="color:rgb(17, 17, 51);"> </font><code>&lt;font style=&quot;color:rgb(17, 17, 51);background-color:rgba(175, 184, 193, 0.2);&quot;&gt;StateFlow&lt;/font&gt;</code><br/><font style="color:rgb(17, 17, 51);">） + 观察者模式</font></td>
<td><font style="color:rgb(17, 17, 51);">实现 ViewModel 与 View 之间的</font><strong><font style="color:rgb(17, 17, 51);">自动、生命周期感知的数据绑定</font></strong><font style="color:rgb(17, 17, 51);">。</font></td>
</tr>
</tbody></table>
<h2 id="ViewModel"><a href="#ViewModel" class="headerlink" title="ViewModel"></a>ViewModel</h2><blockquote>
<p> Activity 既要负责逻辑处理，又要控制UI展示，甚至还得处理网络回调，等等。项目增大时，会导致项目十分臃肿且难以维护。</p>
</blockquote>
<p>ViewModel  专门用于存放与界面相关的数据的，在在一定程度上减少Activity中的逻辑。  </p>
<p>ViewModel的生命周期和 Activity不同，它可以保证在手机屏幕发生旋转的时候不会被重新创建，只有当Activity退出的 时候才会跟着Activity一起销毁。</p>
<p><img loading="lazy" src="https://cdn.nlark.com/yuque/0/2025/png/40667138/1759642807997-51bbb9e0-6550-4949-85a4-af76a60541f1.png">  </p>
<p>不能直接创建ViewModel实例，而是通过ViewModelProvider来获取ViewModel的实例。是 ViewModel 生命周期独立于 Activity 生命周期之外。</p>
<blockquote>
<p> 比较好的编程规范是给每一个Activity和Fragment都创建一个对应的ViewModel  </p>
</blockquote>
<h3 id="基本使用"><a href="#基本使用" class="headerlink" title="基本使用"></a>基本使用</h3><pre><code class="language-kotlin">ViewModelProvider(&lt;Activity或Fragment实例&gt;).get(&lt;对应的ViewModel&gt;::class.java) 
</code></pre>
<h3 id="向-ViewModel-传递参数"><a href="#向-ViewModel-传递参数" class="headerlink" title="向 ViewModel 传递参数"></a>向 ViewModel 传递参数</h3><p> 如果要传递参数，则可以使用  ViewModelProvider.Factory  </p>
<pre><code class="language-kotlin">// ViewModel  使用构造函数以保存传入参数
class MainViewModel(countReserved: Int) : ViewModel() { 
    var counter = countReserved 
} 

// 创建 MainViewModelFactory 实现 ViewModelProvider.Factory
class MainViewModelFactory(private val countReserved: Int) : ViewModelProvider.Factory { 
    // 声明周期与Activity 独立
    override fun &lt;T : ViewModel&gt; create(modelClass: Class&lt;T&gt;): T { 
        return MainViewModel(countReserved) as T 
    } 

} 

// Activity
class MainActivity : AppCompatActivity() { 
    lateinit var viewModel: MainViewModel 
    lateinit var sp: SharedPreferences 

    override fun onCreate(savedInstanceState: Bundle?) { 
        super.onCreate(savedInstanceState) 
        setContentView(R.layout.activity_main) 
        sp = getPreferences(Context.MODE_PRIVATE) 
        val countReserved = sp.getInt(&quot;count_reserved&quot;, 0) 
        // viewModel = ViewModelProvider(this).get(MainViewModel::class.java)
        // 使用 MainViewModelFactory
        viewModel = ViewModelProvider(this, MainViewModelFactory(countReserved)) 
            .get(MainViewModel::class.java)
        // 增加
        plusOneBtn.setOnClickListener { 
            viewModel.counter++ 
            refreshCounter() 
        } 
        // 清0
        clearBtn.setOnClickListener { 
            viewModel.counter = 0 
            refreshCounter() 
        } 
        refreshCounter() 
    } 
    private fun refreshCounter() { 
        infoText.text = viewModel.counter.toString() 
    } 

    override fun onPause() { 
        super.onPause() 
        sp.edit { 
            putInt(&quot;count_reserved&quot;, viewModel.counter) 
        } 
    }
}
</code></pre>
<h2 id="Lifecycles"><a href="#Lifecycles" class="headerlink" title="Lifecycles"></a>Lifecycles</h2><p>在非 Activity 类中感知 Activity 的生命周期，实现逻辑控制。</p>
<pre><code class="language-kotlin">class MyObserver : LifecycleObserver { 
    // 根据需要感知哪个声明周期就添加对应方法
    @OnLifecycleEvent(Lifecycle.Event.ON_START) 
    fun activityStart() { 
        Log.d(&quot;MyObserver&quot;, &quot;activityStart&quot;) 
    } 
    @OnLifecycleEvent(Lifecycle.Event.ON_STOP) 
    fun activityStop() { 
        Log.d(&quot;MyObserver&quot;, &quot;activityStop&quot;) 
    } 
} 
</code></pre>
<p> @OnLifecycleEvent注解 ： 传入了一种生命周期事件。</p>
<p>生命周期事件的类型一共有7种：<strong>ON_CREATE、ON_START、ON_RESUME、ON_PAUSE、 ON_STOP和ON_DESTROY</strong>分别匹配Activity中相应的生命周期回调；另外还有一种ON_ANY类型，表示可以匹配Activity的任何生命周期回调。</p>
<pre><code class="language-kotlin">// 继承自 AppCompatActivity 或 AndroidX 自带 LifecycleOwner 实例
class MainActivity : AppCompatActivity() { 
    ... 
    override fun onCreate(savedInstanceState: Bundle?) { 
        super.onCreate(savedInstanceState) 
        setContentView(R.layout.activity_main) 
        ... 
        lifecycle.addObserver(MyObserver()) 
        // 主动获取声明周期对象
        // lifecycle.addObserver(MyObserver(lifecycle))
    } 
    ... 
} 
</code></pre>
<p><img loading="lazy" src="https://cdn.nlark.com/yuque/0/2026/png/40667138/1770032400274-dccb095f-455b-44f4-b4f9-f391a9611625.png"></p>
<h2 id="LiveData"><a href="#LiveData" class="headerlink" title="LiveData"></a>LiveData</h2><p> 作用：**可以包含任何类型的数据，并在数据发生变化的时候通知给观察者。  **</p>
<blockquote>
<p> MutableLiveData是一种可变的LiveData，：</p>
<p> getValue()方法用于获取LiveData中包含的数据；<br> setValue()方法用于给LiveData设置数据，但是只能在主线程中调用；<br> postValue()方法用于在非主线程中给LiveData设置数据。  </p>
</blockquote>
<p>**永远只暴露不可变的LiveData给外部。这样在非ViewModel中就只能观察 LiveData的数据变化，而不能给LiveData设置数据。 ** </p>
<pre><code class="language-kotlin">class MainViewModel(countReserved: Int) : ViewModel() { 
    // 定义以为不可变的 live data 在get() 中返回
    //  避免外部修改
    val counter: LiveData&lt;Int&gt; 
        get() = _counter 
    // 使用 MutableLiveData 修饰 并保证不可见
    private val _counter = MutableLiveData&lt;Int&gt;()
    
    init { 
        _counter.value = countReserved 
    } 
    
    fun plusOne() { 
        val count = _counter.value ?: 0 
        _counter.value = count + 1 
    } 
 
    fun clear() { 
        _counter.value = 0 
    } 
} 
</code></pre>
<p>使用  viewModel.counter的observe()方法来观察数据的变化。 当数据发生变化时，执行对应逻辑。   </p>
<p><strong>使用</strong></p>
<pre><code class="language-kotlin">class MainActivity : AppCompatActivity() { 
    ... 
    override fun onCreate(savedInstanceState: Bundle?) { 
        ... 
        plusOneBtn.setOnClickListener { 
            viewModel.plusOne() 
        } 
        clearBtn.setOnClickListener { 
            viewModel.clear() 
        } 
        // 实时更新数据
        viewModel.counter.observe(this, Observer { count -&gt; 
            infoText.text = count.toString() 
        }) 
    } 
    override fun onPause() { 
        super.onPause() 
        sp.edit { 
            putInt(&quot;count_reserved&quot;, viewModel.counter.value ?: 0) 
        } 
    } 
}
</code></pre>
<h3 id="Transformations-map"><a href="#Transformations-map" class="headerlink" title="Transformations.map()"></a>Transformations.map()</h3><p> **将实际包含数据的LiveData和仅用于观察数据的 LiveData进行转换。  **</p>
<blockquote>
<p>** 有一个User类，User中包含用户的姓名和年龄 。 如果MainActivity中明 确只会显示用户的姓名，而完全不关心用户的年龄，那么这个时候还将整个User类型的 LiveData暴露给外部，就显得不那么合适了  **</p>
</blockquote>
<p> map()方法接收两个参数：第一个参数是原始的LiveData对象；第二个参数是一个转换函数，我们在转换函数里编写具体的转换逻辑即可。  </p>
<pre><code class="language-kotlin">class MainViewModel(countReserved: Int) : ViewModel() { 

    private val userLiveData = MutableLiveData&lt;User&gt;() 

    // 将User类型的LiveData自由地转型成任意其他类型的LiveData
    val userName: LiveData&lt;String&gt; = Transformations.map(userLiveData) { user -&gt; 
        &quot;${user.firstName} ${user.lastName}&quot; 
    } 
        ... 
} 
</code></pre>
<h3 id="switchMap"><a href="#switchMap" class="headerlink" title="switchMap()"></a>switchMap()</h3><p>不一定所有 Livedata 实例是在 ViewModel中创建的。 比如后端接口返回 </p>
<p> 处理 ViewModel中的某个LiveData对象是调用另外的方法获取的情况。  </p>
<pre><code class="language-kotlin">object Repository { 
    fun getUser(userId: String): LiveData&lt;User&gt; { 
        val liveData = MutableLiveData&lt;User&gt;() 
        liveData.value = User(userId, userId, 0) 
        // 每次都会返回一个新的 liveData 实例 无法直接观察
        return liveData 
    } 
} 
</code></pre>
<pre><code class="language-kotlin">class MainViewModel(countReserved: Int) : ViewModel() { 
    ... 
    private val userIdLiveData = MutableLiveData&lt;String&gt;() 
    val user: LiveData&lt;User&gt; = Transformations.switchMap(userIdLiveData) { userId -&gt; 
        Repository.getUser(userId) 
    } 
    fun getUser(userId: String) { 
        userIdLiveData.value = userId 
    } 
} 
</code></pre>
<pre><code class="language-kotlin">class MyViewModel : ViewModel() { 
    private val refreshLiveData = MutableLiveData&lt;Any?&gt;() 
    // 要监听的数据
    val refreshResult = Transformations.switchMap(refreshLiveData) {
                // 某些不需要获取参数的方法
                Repository.refresh()  // 假设Repository中已经定义了refresh()方法 
    } 
    // 在外部调用
    fun refresh() { 
        // 触发一次数据刷新
        refreshLiveData.value = refreshLiveData.value 
    } 
} 
</code></pre>
<p> LiveData在内部使用了Lifecycles组件来 自我感知生命周期的变化，从而可以在Activity销毁的时候及时释放引用，避免产生内存泄漏的问题  </p>
<h2 id="Room"><a href="#Room" class="headerlink" title="Room"></a>Room</h2><blockquote>
<p> ORM（Object Relational Mapping）也叫对象关系映射。简单来讲，我们使用的编程语言是 面向对象语言，而使用的数据库则是关系型数据库，<strong>将面向对象的语言和面向关系的数据库之间建立一种映射关系</strong>，这就是ORM了 。</p>
</blockquote>
<ul>
<li>Entity：用于定义封装实际数据的实体类 Dao ，每个实体类都会在数据库中有一张<strong>对应的表</strong>，并且表中的列是根据实体类中的字段自动生成的。 </li>
<li>Dao：数据访问对象的意思，相当于数据库<strong>数据库操作的接口</strong>。</li>
</ul>
<p>Dao的内部就是根据业务需求对各种数据库操作进行的封装。**覆盖所有业务需求，使得业务方永远只需要与 Dao 层进行交互。**数据库操作通常有增删改查这4种，因此 Room也提供了@Insert、@Delete、@Update和@Query这4种相应的注解。   根据实际业务需求，编写对应 SQL 语句。</p>
<ul>
<li>Database：定义数据库信息。</li>
</ul>
<pre><code class="language-kotlin">// 实体类声明
@Entity 
data class User(var firstName: String, var lastName: String, var age: Int) { 
    @PrimaryKey(autoGenerate = true) 
    var id: Long = 0 
} 

// 定义Dao
@Dao 
interface UserDao { 
    @Insert 
    fun insertUser(user: User): Long 
    @Update 
    fun updateUser(newUser: User) 
    @Query(&quot;select * from User&quot;) 
    fun loadAllUsers(): List&lt;User&gt; 
    @Query(&quot;select * from User where age &gt; :age&quot;) 
    fun loadUsersOlderThan(age: Int): List&lt;User&gt; 
    @Delete
    fun deleteUser(user: User) 
 
    @Query(&quot;delete from User where lastName = :lastName&quot;) 
    fun deleteUserByLastName(lastName: String): Int 
 
} 

// 数据库定义
// 注解声明版本号 和 实体类
@Database(version = 1, entities = [User::class]) 
abstract class AppDatabase : RoomDatabase() { 
    // 获取Dao实例
    abstract fun userDao(): UserDao 
 
    companion object { 
        // 构建单例模式
        private var instance: AppDatabase? = null 
 
        @Synchronized 
        fun getDatabase(context: Context): AppDatabase { 
            instance?.let { 
                return it 
            } 
            return Room.databaseBuilder(context.applicationContext, 
                AppDatabase::class.java, &quot;app_database&quot;) 
                .build().apply { 
                instance = this 
            } 
        } 
    } 
 
} 
</code></pre>
<h3 id="数据库升级-1"><a href="#数据库升级-1" class="headerlink" title="数据库升级"></a>数据库升级</h3><p>定义匿名类，编写对应数据库修改的 SQL 语句，在数据库实例化时执行。</p>
<pre><code class="language-kotlin">@Database(version = 2, entities = [User::class, Book::class]) 
abstract class AppDatabase : RoomDatabase() { 

    abstract fun userDao(): UserDao 

    abstract fun bookDao(): BookDao 

    companion object { 
        // 数据库有版本1 升至 版本2的方法
        val MIGRATION_1_2 = object : Migration(1, 2) { 
            override fun migrate(database: SupportSQLiteDatabase) { 
                database.execSQL(&quot;create table Book (id integer primary 
                                      key autoincrement not null, name text not null, 
                                      pages integer not null)&quot;) 
            } 
        } 

        private var instance: AppDatabase? = null 

        fun getDatabase(context: Context): AppDatabase { 
            instance?.let { 
                return it 
            } 
            return Room.databaseBuilder(context.applicationContext, 
                                        AppDatabase::class.java, &quot;app_database&quot;) 
                .addMigrations(MIGRATION_1_2)   // 加入升级方法
                .build().apply { 
                    instance = this 
                } 
        } 
    }
}
</code></pre>
<h2 id="WorkManager"><a href="#WorkManager" class="headerlink" title="WorkManager"></a>WorkManager</h2><p>WorkManager和Service并不相同，也没有直接的联系。 Service是Android系统的四大组件之一，它在没有被销毁的情况下是一直保持在后台运行的。 而WorkManager只是一个处理定时任务的工具，它可以保证即使在应用退出甚至手机重启的情 况下，之前注册的任务仍然将会得到执行 。</p>
<blockquote>
<p> 使用WorkManager注册的周期性任务不能保证一定会准时执行，这并不是bug，而是系 统为了减少电量消耗，可能会将触发时间临近的几个任务放在一起执行，这样可以大幅度地减 少CPU被唤醒的次数，从而有效延长电池的使用时间。  </p>
</blockquote>
<table>
<thead>
<tr>
<th>特性</th>
<th>WorkManager</th>
<th>Service</th>
</tr>
</thead>
<tbody><tr>
<td>设计目标</td>
<td>处理<strong>可延迟、可重试、需保证执行</strong>的后台任务（即使应用退出）</td>
<td>处理<strong>即时、持续、与用户交互相关</strong>的后台操作</td>
</tr>
<tr>
<td>生命周期</td>
<td>任务式，任务完成即结束</td>
<td>可长期运行，直到主动停止或系统回收</td>
</tr>
<tr>
<td>执行时机</td>
<td>由系统根据设备状态（电量、网络、充电等）调度</td>
<td>立即执行（startService）或绑定执行（bindService）</td>
</tr>
<tr>
<td>前台展示</td>
<td>不直接支持，需结合 ForegroundService</td>
<td>可升级为前台服务（ForegroundService），显示通知保活</td>
</tr>
<tr>
<td>交互能力</td>
<td>无直接 UI 交互能力</td>
<td>可与 Activity 绑定，直接进行进程内通信</td>
</tr>
<tr>
<td>应用场景</td>
<td>+ <font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">数据同步（如定时同步用户数据到服务器）</font><br/>+ <font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">图片 &#x2F; 文件上传（允许延迟，可重试）</font><br/>+ <font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">定期数据备份</font><br/>+ <font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">日志上报（非即时性）</font><br/>+ <font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">周期性任务（如每天凌晨清理缓存）</font></td>
<td>+ <font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">音乐播放器后台播放</font><br/>+ <font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">实时定位追踪</font><br/>+ <font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">即时消息推送（如 IM 聊天的后台连接）</font><br/>+ <font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">与 Activity 实时交互的后台操作</font><br/>+ <font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">需要持续运行的前台任务（如文件下载进度展示）</font></td>
</tr>
</tbody></table>
<ul>
<li>定义后台任务：创建类继承自 Worker，重写 doWork 方法。</li>
</ul>
<pre><code class="language-kotlin">class SimpleWorker(context: Context, params: WorkerParameters) : Worker(context, params) { 
    override fun doWork(): Result { 
        Log.d(&quot;SimpleWorker&quot;, &quot;do work in SimpleWorker&quot;) 
        return Result.success() 
    } 
}
</code></pre>
<ul>
<li>配置后台任务的运行条件和约束信息：</li>
</ul>
<pre><code class="language-kotlin">// 单次定时任务  
val request = OneTimeWorkRequest.Builder(SimpleWorker::class.java).build() 
// 时间间隔不能短于15Min
val request = PeriodicWorkRequest.Builder(SimpleWorker::class.java, 15, 
    TimeUnit.MINUTES).build() 
</code></pre>
<ul>
<li>执行： WorkManager.getInstance(context).enqueue(request)</li>
</ul>
<h3 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h3><ul>
<li>通过  setInitialDelay   可以延时执行</li>
</ul>
<pre><code class="language-kotlin">val request = OneTimeWorkRequest.Builder(SimpleWorker::class.java) 
    .setInitialDelay(5, TimeUnit.MINUTES) 
    .build() 
</code></pre>
<ul>
<li>添加标签  addTag   可以对相同变迁的定时任务进行管理</li>
<li>取消所有定时任务  WorkManager.getInstance(this).cancelAllWork() </li>
<li>setBackoffCriteria  失败后重复执行方式，多久后重新执行定时任务( 时间最短不能少于10秒钟  )</li>
</ul>
<pre><code class="language-kotlin">val request = OneTimeWorkRequest.Builder(SimpleWorker::class.java) 
    ... 
    .setBackoffCriteria(BackoffPolicy.LINEAR, 10, TimeUnit.SECONDS) 
    .build() 
</code></pre>
<ul>
<li>通过 then 实现任务链式执行</li>
</ul>
<pre><code class="language-kotlin">val sync = ... 
val compress = ... 
val upload = ... 
WorkManager.getInstance(this) 
    .beginWith(sync) 
    .then(compress) 
    .then(upload) 
    .enqueue() 
</code></pre>
<h1 id="进阶开发"><a href="#进阶开发" class="headerlink" title="进阶开发"></a>进阶开发</h1><ul>
<li>自定义 Application 类全局获取 Context</li>
</ul>
<pre><code class="language-kotlin">class MyApplication : Application() { 
    companion object { 
         @SuppressLint(&quot;StaticFieldLeak&quot;)
        lateinit var context: Context 
    } 
    override fun onCreate() {
        super.onCreate() 
        context = applicationContext 
    } 
} 

// 调用
fun Int.showToast(duration: Int = Toast.LENGTH_SHORT) { 
    Toast.makeText(MyApplication.context, this, duration).show() 
} 
</code></pre>
<ul>
<li>通过对象的 Serializable（序列化实现） 或 Parcelabel（将完整对象分解） 接口实现，使 Intent 传递对象</li>
<li>自定义日志工具</li>
<li>创建定时任务：Alarm 设定实现实现</li>
</ul>
<h3 id="深色模式"><a href="#深色模式" class="headerlink" title="深色模式"></a>深色模式</h3><ul>
<li>使用Force Dark</li>
</ul>
<blockquote>
<p>分析浅色主题应用下的每一层 View，并且在这些View绘制到屏幕之前，自动将它们的颜色转换成更加适合深色主题的颜色。  </p>
</blockquote>
<ul>
<li>DayNight主题</li>
</ul>
<p> 在系统设置中开启深 色主题时，应用程序会自动使用深色主题，反之则会使用浅色主题 。</p>
<p>指定颜色值引用的方式相当于对控件的颜色进行了硬编码，DayNight主题是不能对这些颜 色进行动态转换的。  在普通情况下，系统仍然会读取values&#x2F;colors.xml文件中的颜色值，而一旦用户开 启了深色主题，系统就会去读取values-night&#x2F;colors.xml文件中的颜色值了。  </p>
<pre><code class="language-kotlin">fun isDarkTheme(context: Context): Boolean { 
    val flag = context.resources.configuration.uiMode and 
        Configuration.UI_MODE_NIGHT_MASK 
    return flag == Configuration.UI_MODE_NIGHT_YES 
} 
</code></pre>
]]></description>
    </item>
    <item>
      <title>Kotlin总结</title>
      <link>https://raindrop2508.github.io/2026/02/26/tech/Kotlin%E6%80%BB%E7%BB%93/</link>
      <guid>https://raindrop2508.github.io/2026/02/26/tech/Kotlin%E6%80%BB%E7%BB%93/</guid>
      <pubDate>2 月 26 日，2026</pubDate>
      <description><![CDATA[<p>编译型语言：源代码一次性地编译成计算机可识别的二进制文件，然后计算机直接执行。 </p>
<p>解释型语言：解释器会一行行地读取我们编写的源代码，然后实时地将这些源代码解释成计算机可识别的二进制数据后再执行。<strong>JAVA 编译程式 class 文件，通过 Java虚拟机  担任解释器。</strong></p>
<h2 id="基本语法"><a href="#基本语法" class="headerlink" title="基本语法"></a>基本语法</h2><ul>
<li>val 不可变，var 可变   基于类型推导判断数据类型。对于延迟初始化时则需要显式声明。</li>
<li>Kotlin 完全抛弃了Java中的 基本数据类型，全部使用了对象数据类型（引用类）。</li>
</ul>
<blockquote>
<p>** 永远优先使用val来声明一个变量，而当val没有办法满足你的需求时再使用var。这样设计出来的程 序会更加健壮，也更加符合高质量的编码规范  **</p>
</blockquote>
<ul>
<li>When 语句</li>
</ul>
<pre><code class="language-kotlin">fun checkNumber(num: Number) { 
    when (num) { 
        is Int -&gt; println(&quot;number is Int&quot;) 
        is Double -&gt; println(&quot;number is Double&quot;) 
        else -&gt; println(&quot;number not support&quot;) 
    } 
} 
</code></pre>
<ul>
<li>区间</li>
</ul>
<pre><code class="language-kotlin">val range = 0..10   // 双端闭区间
val range = 0 until 10  // 左闭右开区间
</code></pre>
<h2 id="面向对象"><a href="#面向对象" class="headerlink" title="面向对象"></a>面向对象</h2><p>**任何一个非抽象类默认都是不可以被继承的（与 Java 不同） **，如果一个类不是专门为继承而设计的，就应该主动加上 final 声明，<strong>对于可继承的类需要添加 open 关键词</strong>。</p>
<p>Kotlin将构造函数分 成了两种：主构造函数和次构造函数。 每个类默认都会有一个不带参数的主构造函数，。 所有主构造函数中的逻辑 ，在  init   中实现。</p>
<h3 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h3><pre><code class="language-kotlin">class Student(val sno: String, val grade: Int, name: String, age: Int) : 
    Person(name, age) { 
    ... 
} 
</code></pre>
<p>每个类默认会有一个主构造函数，默认为不带参的。</p>
<p> 任何一个类只能有一个主构造函数，但是可以有多个次构造函数。次构造函数也可 以用于实例化一个类，这一点和主构造函数没有什么不同，只不过它是有函数体的。  </p>
<p> **子类的主构造函数调用父类中的哪个构造函数，在继承的时候通过括号来指定。 **</p>
<p>** 在主构造函数中声明为 **<code>**val**</code>**或 **<code>**var**</code>**的参数将自动成为该类的属性（字段）。没有 **<code>**val**</code>**或 **<code>**var**</code><strong>前缀的参数只是构造函数参数，不会成为该类的属性。</strong></p>
<pre><code class="language-java">open class Person(val name: String, val age: Int)

class Student(
    val sno: String,
    val grade: Int,
    val studentName: String,  // Student自己的属性
    val studentAge: Int        // Student自己的属性
) : Person(studentName, studentAge) {
    
    fun showInfo1() {
        // 访问继承的属性
        println(&quot;继承的属性:&quot;)
        println(&quot;姓名: $name&quot;)      // 继承自Person
        println(&quot;年龄: $age&quot;)       // 继承自Person
    }
    
    fun showInfo2() {
        // 访问自己的属性
        println(&quot;自己的属性:&quot;)
        println(&quot;学生姓名: $studentName&quot;)
        println(&quot;学生年龄: $studentAge&quot;)
    }
    
    fun compareNames() {
        println(&quot;继承的name: $name&quot;)
        println(&quot;自己的studentName: $studentName&quot;)
        println(&quot;是否相等: ${name == studentName}&quot;)  // 应该是true
    }
}
</code></pre>
<p>大多数情况下需要加入 super 关键字。除非如上，子类给父类 构造器  传参。</p>
<p> 当一个类既有主构造函数又有次构造函数时，所有的次构造函数都必须调用主构造 函数（包括间接调用）。  </p>
<pre><code class="language-kotlin">class Student(val sno: String, val grade: Int, name: String, age: Int) : 
         Person(name, age) { 
    constructor(name: String, age: Int) : this(&quot;&quot;, 0, name, age) { 
    } 
    constructor() : this(&quot;&quot;, 0) { // 调用了第一个次构造函数
    } 
}
</code></pre>
<h3 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h3><p>无论是继承还是接口， Kotlin中统一使用冒号，中间用逗号进行分隔。 </p>
<h3 id="可见性修饰符"><a href="#可见性修饰符" class="headerlink" title="可见性修饰符"></a>可见性修饰符</h3><h3 id=""><a href="#" class="headerlink" title=""></a><img loading="lazy" src="https://cdn.nlark.com/yuque/0/2026/png/40667138/1768220636361-8c92a2bc-4f56-4e2d-b89b-9cdf5c4d363a.png"></h3><h3 id="数据类与单例类"><a href="#数据类与单例类" class="headerlink" title="数据类与单例类"></a>数据类与单例类</h3><p>当在一个类前 面声明了<strong>data关键字</strong>时，就表明你希望这个类是一个数据类，Kotlin会根据主构造函数中的参数帮你将equals()、hashCode()、toString()等固定且无实际逻辑意义的方法自动生成， 从而大大减少了开发的工作量。  </p>
<p>在Kotlin中创建一个单例类的方式极其简单，只需要将class关键字改成object关键字即可。  </p>
<p><img loading="lazy" src="https://cdn.nlark.com/yuque/0/2026/png/40667138/1770349955543-f75ffde2-fb8c-42e4-8e25-78f4bb130e34.png"></p>
<h3 id="Lambda-编程"><a href="#Lambda-编程" class="headerlink" title="Lambda 编程"></a>Lambda 编程</h3><p> listOf()函数创建的是一个不可变的列表。你也许不太能理解什么叫作不可变的<font style="color:rgb(38, 38, 38);">列表</font>。 使用 mutableListOf()  创建是可变的<font style="color:rgb(38, 38, 38);">列表</font>。</p>
<p>对应的还有以下函数</p>
<pre><code class="language-kotlin">setOf()
mutableSetOf()
mapOf()
mutableMapOf()
</code></pre>
<p>  **集合的函数式API  **</p>
<pre><code class="language-kotlin">val list = listOf(&quot;Apple&quot;, &quot;Banana&quot;, &quot;Orange&quot;, &quot;Pear&quot;, &quot;Grape&quot;, &quot;Watermelon&quot;) 
val maxLengthFruit = list.maxBy { it.length } 

val lambda = { fruit: String -&gt; fruit.length } 
val maxLengthFruit = list.maxBy(lambda)

// 可简化为
val maxLengthFruit = list.maxBy({ fruit: String -&gt; fruit.length }) 

val maxLengthFruit = list.maxBy() { fruit: String -&gt; fruit.length } 

val maxLengthFruit = list.maxBy { fruit: String -&gt; fruit.length } 

val maxLengthFruit = list.maxBy { fruit -&gt; fruit.length }

val maxLengthFruit = list.maxBy { it.length } 
</code></pre>
<p>Lambda表达式  </p>
<pre><code class="language-kotlin">{参数名1: 参数类型, 参数名2: 参数类型 -&gt; 函数体} 
</code></pre>
<p>集合中的map函数是最常用的一种函数式API，它<strong>用于将集合中的每个元素都映射成一个另外的值</strong>，映射的规则在Lambda表达式中指定。</p>
<ul>
<li>filter函数：则是根据条件，过滤参数。</li>
<li>any函数：用于判断集 合中是否至少存在一个元素满足指定条件  </li>
<li>all函数：用于判断集合中是否所有元素都满足指定条件</li>
</ul>
<p> **如果我们在Kotlin代码中调用了一个 Java方法，并且该方法接收一个Java单抽象方法接口参数，就可以使用函数式API。**Java单抽象 方法接口指的是接口中只有一个待实现方法，如果接口中有多个待实现方法，则无法使用函数式API。  </p>
<pre><code class="language-kotlin">button.setOnClickListener(new View.OnClickListener() { 
    @Override 
    public void onClick(View v) {
        v.visibility = View.GONE
    } 
}); 

// 转为Kotlin写法 并不断简化写法
button.setOnClickListener(object : View.OnClickListener { 
    override fun onClick(v: View?) {
        v?.visibility = View.GONE
    } 
}); 

button.setOnClickListener(View.OnClickListener { 
    v: View? -&gt; v?.visibility = View.GONE
});

button.setOnClickListener { 
    v: View? -&gt; v?.visibility = View.GONE
}

button.setOnClickListener { 
    v -&gt; v?.visibility = View.GONE
}

button.setOnClickListener { 
    it?.visibility = View.GONE
}
</code></pre>
<h3 id="空指针检查"><a href="#空指针检查" class="headerlink" title="空指针检查"></a>空指针检查</h3><pre><code class="language-java">if (a != null) { 
    a.doSomething() 
} 

// 简化

a?.doSomething() 

val c = if (a ! = null) { 
    a 
} else { 
    b 
} 

// 简化

val c = a ?: b

// 非空断言
a!!.doSomething() 
</code></pre>
<p>let 函数</p>
<p> 这个函数提供了函数式API的编程接口，并将原始调用对象作为参数传递到 Lambda表达式中。  </p>
<pre><code class="language-kotlin">fun doStudy(study: Study?) { 
    study?.let { stu -&gt; 
        stu.readBooks() 
        stu.doHomework() 
    } 
} 
</code></pre>
<p>字符串内嵌表达式</p>
<pre><code class="language-java">&quot;hello, ${obj.name}. nice to meet you!&quot;
// 仅有一个变量时
&quot;hello, $name. nice to meet you!&quot;
</code></pre>
<p>函数参数默认值（构造函数也可以）</p>
<pre><code class="language-java">fun printParams(num: Int = 100, str: String) { 
    println(&quot;num is $num , str is $str&quot;) 
} 

</code></pre>
<h2 id="标准函数"><a href="#标准函数" class="headerlink" title="标准函数"></a>标准函数</h2><h3 id="with"><a href="#with" class="headerlink" title="with"></a>with</h3><p> with函数接收两个参数：第一个参数可以是一个任意类型的对 象，第二个参数是一个Lambda表达式。Lambda表达式的最后一行代码会作为with函数的返回值返回。</p>
<pre><code class="language-kotlin">val list = listOf(&quot;Apple&quot;, &quot;Banana&quot;, &quot;Orange&quot;, &quot;Pear&quot;, &quot;Grape&quot;) 
val result = with(StringBuilder()) { 
    // 省去了反复调用 builder 的语句
    append(&quot;Start eating fruits.\n&quot;) 
    for (fruit in list) { 
        append(fruit).append(&quot;\n&quot;) 
    } 
    append(&quot;Ate all fruits.&quot;) 
    toString() 
} 
println(result) 
</code></pre>
<h3 id="run"><a href="#run" class="headerlink" title="run"></a>run</h3><p> run函数通常不会直接调用， 而是要在某个对象的基础上调用；其次r**un函数只接收一个Lambda参数，并且会在Lambda表 达式中提供调用对象的上下文。 **</p>
<pre><code class="language-kotlin">val list = listOf(&quot;Apple&quot;, &quot;Banana&quot;, &quot;Orange&quot;, &quot;Pear&quot;, &quot;Grape&quot;) 
val result = StringBuilder().run { 
    append(&quot;Start eating fruits.\n&quot;) 
    for (fruit in list) {
        append(fruit).append(&quot;\n&quot;) 
    } 
    append(&quot;Ate all fruits.&quot;) 
    toString() 
} 
println(result) 
</code></pre>
<h3 id="apply"><a href="#apply" class="headerlink" title="apply"></a>apply</h3><p> 是apply函数无法指定返回值，而是会自动返回调用对象本身。  </p>
<pre><code class="language-kotlin">val list = listOf(&quot;Apple&quot;, &quot;Banana&quot;, &quot;Orange&quot;, &quot;Pear&quot;, &quot;Grape&quot;) 
val result = StringBuilder().apply { 
    append(&quot;Start eating fruits.\n&quot;) 
    for (fruit in list) { 
        append(fruit).append(&quot;\n&quot;) 
    } 
    append(&quot;Ate all fruits.&quot;) 
    // toString()方法写在了下边
} 
println(result.toString()) 
</code></pre>
<h2 id="静态方法"><a href="#静态方法" class="headerlink" title="静态方法"></a>静态方法</h2><p>Kotlin提供了比静态方法更好用的语法特性，即单例类。</p>
<p>使类中的某一个方法变成静态方法的调用方式：companion object 伴生类</p>
<blockquote>
<p> <img loading="lazy" src="https://cdn.nlark.com/yuque/0/2026/png/40667138/1768396851629-bd0048bd-9aae-431d-b47c-544d4d93dad1.png"><img loading="lazy" src="https://cdn.nlark.com/yuque/0/2026/png/40667138/1768396842466-f6053d20-ad71-425f-97d9-81f2aa50cf0e.png"></p>
</blockquote>
<p>真正的静态方法： 注解和顶层方法</p>
<ul>
<li>注解：半生类方法加上@JvmStatic注解  </li>
<li>顶层方法： 指的是那些没有定义在任何类中的方法， Kotlin编译器会将所有的顶层方法全部编译成静态方法，因只要你定义了一 个顶层方法，那么它就一定是静态方法。  （直接不在类中定义）</li>
</ul>
<h2 id="延迟初始化"><a href="#延迟初始化" class="headerlink" title="延迟初始化"></a>延迟初始化</h2><pre><code class="language-kotlin">class MainActivity : AppCompatActivity(), View.OnClickListener { 
    private var adapter: MsgAdapter? = null   // 全局定义
    override fun onCreate(savedInstanceState: Bundle?) { 
        ... 
        // onCreate生命周期中初始化
        adapter = MsgAdapter(msgList) 
        ... 
    } 
} 
    override fun onClick(v: View?) { 
        ... 
        // 多处需要判空
        adapter?.notifyItemInserted(msgList.size - 1) 
        ... 
    }
</code></pre>
<p>延迟初始化 - (有可能产生空指针问题)</p>
<pre><code class="language-kotlin"> private lateinit var adapter: MsgAdapter
</code></pre>
<p>在adapter变量的前面加上了lateinit关键字，这样就不用在一开始的时候 将它赋值为null 。 它可以告诉Kotlin编译器，  该变量会在晚些时候进行初始化。</p>
<h2 id="密封类"><a href="#密封类" class="headerlink" title="密封类"></a>密封类</h2><p> sealed class  </p>
<pre><code class="language-kotlin">sealed class Result 
class Success(val msg: String) : Result() 
class Failure(val error: Exception) : Result() 

fun getResultMsg(result: Result) = when (result) { 
    is Success -&gt; result.msg 
    is Failure -&gt; &quot;Error is ${result.error.message}&quot; 
} 
</code></pre>
<p> 当在when语句中传入一个密封类变量 作为条件时，Kotlin编译器会自动检查该密封类有哪些子类，并强制要求你将每一个子类所对应 的条件全部处理。这样就可以保证，即使没有编写else条件，也不可能会出现漏写条件分支的 情况。</p>
<h2 id="扩展函数"><a href="#扩展函数" class="headerlink" title="扩展函数"></a>扩展函数</h2><p>扩展函数表示即使在不修改某个类的源码的情况下，仍然可以打 开这个类，向该类添加新的函数。  </p>
<pre><code class="language-kotlin">fun ClassName.methodName(param1: Int, param2: Int): Int { 
    return 0 
} 
</code></pre>
<h2 id="运算符重载"><a href="#运算符重载" class="headerlink" title="运算符重载"></a>运算符重载</h2><pre><code class="language-kotlin">class Obj { 
    operator fun plus(obj: Obj): Obj { 
        // 
处理相加的逻辑 
    } 
} 
</code></pre>
<p><img loading="lazy" src="https://cdn.nlark.com/yuque/0/2026/png/40667138/1768704747965-d018681b-e0b0-4809-83da-bdd5af2e0cde.png"></p>
<h2 id="高阶函数"><a href="#高阶函数" class="headerlink" title="高阶函数"></a>高阶函数</h2><p> **如果一个函数接收另一个函数作为参数，或者返回值的类型是 另一个函数，那么该函数就称为高阶函数。 ** </p>
<pre><code class="language-kotlin">fun num1AndNum2(num1: Int, num2: Int, operation: (Int, Int) -&gt; Int): Int { 
    val result = operation(num1, num2) 
    return result 
} 

// 使用 Lambda 表达式，省去定义类型相匹配的函数
val result2 = num1AndNum2(num1, num2) { n1, n2 -&gt; 
    n1 + n2 
} 
</code></pre>
<pre><code class="language-kotlin">// build 方法接收 一个函数
fun StringBuilder.build(block: StringBuilder.() -&gt; Unit): StringBuilder { 
    // 调用build函数时传入的Lambda表达式将会自动拥有StringBuilder的上下文
    block() 
    return this 
}
</code></pre>
<h3 id="内联函数"><a href="#内联函数" class="headerlink" title="内联函数"></a>内联函数</h3><p>高阶函数转换为 Java 的代码</p>
<pre><code class="language-kotlin">public static int num1AndNum2(int num1, int num2, Function operation) { 
    int result = (int) operation.invoke(num1, num2); 
    return result; 
} 
public static void main() { 
    int num1 = 100; 
    int num2 = 80; 
    int result = num1AndNum2(num1, num2, new Function() { 
        @Override 
        public Integer invoke(Integer n1, Integer n2) { 
            return n1 + n2; 
        } 
    }); 
} 
</code></pre>
<p>实际： Lambda表达式在底层被转换成了匿名类的实现方式。这就表明，我们每调用一次Lambda表达式，都会创建一个新的匿名类实例，当然也会造成额外的内存和性能开销 。<strong>通过使用  inline关键字  可以避免此问题</strong>。</p>
<p><font style="color:rgb(38, 38, 38);">内联函数</font><strong>函数体复制到调用处，避免创建额外的Function对象，从而提高性能。内联函数类型参数在编译的时候会被进行代码替换</strong>，因此它没有真正的参数属性。非内联的函数类型参数可以自由地传递给其他任何函数，因为它就是一个真实的参数，而<strong>内联的函数类型参数只允许传递给另外一个内联函数</strong>，这也是它最大的局限性。  </p>
<p>**内联函数所引用的Lambda表达中是可以使用return关键字来进行函数返回的，而非内联函数只能进行局部返回。  **</p>
<blockquote>
<ul>
<li><strong><font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">非内联函数的 Lambda</font></strong><font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">：</font><code>&lt;font style=&quot;color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);&quot;&gt;return&lt;/font&gt;</code><font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">只能返回</font><strong><font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">Lambda 自身</font></strong><font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">（局部返回），想返外部函数要写</font><code>&lt;font style=&quot;color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);&quot;&gt;return@函数名&lt;/font&gt;</code><font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">；</font></li>
<li><strong><font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">内联函数的 Lambda</font></strong><font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">：</font><code>&lt;font style=&quot;color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);&quot;&gt;return&lt;/font&gt;</code><font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">直接返回</font><strong><font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">调用 Lambda 的外部函数</font></strong><font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">（函数返回），这是内联的专属特性（也叫</font><strong><font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">非局部返回</font></strong><font style="color:rgb(0, 0, 0);background-color:rgba(0, 0, 0, 0);">）。</font></li>
</ul>
</blockquote>
<pre><code class="language-kotlin">inline fun runRunnable(block: () -&gt; Unit) { 
    // 为内联函数的Lambda表达式中允许使用return关键字，
    // 和高阶函数的匿名类实现中不允许使用return关键字之间造成了冲突。
    val runnable = Runnable { 
        block() 
    } 
    runnable.run() 
} 
</code></pre>
<p>** 在高阶函数中创建了另外的Lambda或者匿名类的实现，并且在这些实现 中调用函数类型参数，此时再将高阶函数声明成内联函数，就一定会提示错误。此时需要使用 crossline 关键字**  </p>
<h4 id="noinline"><a href="#noinline" class="headerlink" title="noinline"></a>noinline</h4><p> 一个高阶函数中如果接收了两个或者更多函数类型的参数，这时我们给函数加上了inline关键字，那么Kotlin编译器会自动将所有引用的 Lambda表达式全部进行内联 。<strong>使用 noinline 来内联其中的一个Lambda表达式 。</strong></p>
<pre><code class="language-kotlin"> inline fun inlineTest(block1: () -&gt; Unit, noinline block2: () -&gt; Unit) { }  
</code></pre>
<ul>
<li>** 内联的函数类型参数只允许传递给另外一个内联函数 **</li>
</ul>
<pre><code class="language-kotlin">// 内联函数
inline fun process(action: () -&gt; Unit) {
    println(&quot;开始处理&quot;)
    action()  // 这里会内联展开
    println(&quot;结束处理&quot;)
}

// 普通函数
fun normalFunction(action: () -&gt; Unit) {
    println(&quot;普通函数&quot;)
    action()  // 这里不会内联
}

inline fun highOrderInline(action: () -&gt; Unit) {
    // 如果将 action 传递给 normalFunction
    normalFunction(action)  // ❌ 编译错误！
    // 原因：action 在编译时需要被内联展开，
    // 但 normalFunction 的实现是未知的（可能在另一个模块中）
    // 编译器无法确定如何展开
}
</code></pre>
<ul>
<li>** 内联函数所引用的Lambda表达式中是可以使用return关键字来进行函数返回的，而非内联函数只能进行局部返回**</li>
</ul>
<pre><code class="language-kotlin"> fun printString(str: String, block: (String) -&gt; Unit) { 
    println(&quot;printString begin&quot;) 
    block(str) 
    println(&quot;printString end&quot;) 
} 
fun main() { 
    println(&quot;main start&quot;) 
    val str = &quot;&quot; 
    printString(str) { s -&gt; 
        println(&quot;lambda start&quot;) 
        if (s.isEmpty()) return@printString   // 局部返回，若需要全局返回则需要添加 inline 关键字
        println(s) 
        println(&quot;lambda end&quot;) 
    } 
    println(&quot;main end&quot;) 
}  
</code></pre>
<h4 id="crossinline"><a href="#crossinline" class="headerlink" title="crossinline"></a><font style="color:rgb(38, 38, 38);">crossinline</font></h4><p>如果我们在高阶函数中创建了另外的Lambda或者匿名类的实现，并且在这些实现中调用函数类型参数时,再将高阶函数声明成内联函数，就一定会提示错误。  内联函数的Lambda表达式中允许使用return关键字，和高阶函数的匿名类实现中不允许使用return关键字之间造成了冲突。</p>
<pre><code class="language-kotlin">inline fun runRunnable(block: () -&gt; Unit) { 
    val runnable = Runnable { 
        block() 
    } 
    runnable.run() 
} 
</code></pre>
<p>而crossinline关键字 用于保证在内联函数的Lambda表达式中一定不会使用return关键字。   所以上述代码要修改为<code>inline fun runRunnable(crossinline block: () -&gt; Unit)  </code></p>
<h2 id="泛型和委托"><a href="#泛型和委托" class="headerlink" title="泛型和委托"></a>泛型和委托</h2><h3 id="泛型"><a href="#泛型" class="headerlink" title="泛型"></a>泛型</h3><p> 如果想要让泛型的类型不可为空，需要将泛型的上界手动指定成Any</p>
<h4 id="泛型进行实化"><a href="#泛型进行实化" class="headerlink" title="泛型进行实化"></a>泛型进行实化</h4><ul>
<li><font style="color:rgb(38, 38, 38);">函数必须是内联函数</font></li>
<li><font style="color:rgb(38, 38, 38);">声明泛型的地方必须加上reified关键字来表示该泛型要进行实化。</font></li>
</ul>
<p> Java的泛型功能是通过类型擦除机制来实现的，<strong>编译时进行类型检查，运行时被擦除</strong>。  </p>
<p> 泛型对于类型的约束只在编译时期存在， JVM是识别不出来代码中指定的泛型类型的。</p>
<pre><code class="language-kotlin">// 无法获取泛型的实际类型
public &lt;T&gt; void printType(T obj) {
    // 这里无法获取T的具体类型
    Class&lt;T&gt; clazz = T.class;  // 编译错误！
    
    // 只能获取运行时类型
    Class&lt;?&gt; runtimeClass = obj.getClass();  // 可行，但这不是泛型类型
}
</code></pre>
<p>Kotlin <strong>内联函数</strong>中的代码会在编译的时候自动被替换到调用它的地方，这样也就不存在泛型擦除问题了</p>
<pre><code class="language-kotlin">inline fun &lt;reified T&gt; getGenericType() = T::class.java 
</code></pre>
<p>通过内联方式将泛型进行实化。 <strong>同时使用 inline和</strong>**<font style="color:rgb(38, 38, 38);">reified关键字</font>**让泛型T成为一个被实化的泛型。 </p>
<blockquote>
<p>**<font style="color:rgb(38, 38, 38);">reified关键字</font>**只能用于内联函数，不能用于普通类的泛型参数也不能用作类型参数</p>
</blockquote>
<p><strong>泛型实化简化 Intent 的跳转</strong></p>
<pre><code class="language-kotlin">inline fun &lt;reified T&gt; startActivity(context: Context, block: Intent.() -&gt; Unit) { 
    val intent = Intent(context, T::class.java) 
    intent.block() 
    context.startActivity(intent) 
} 

startActivity&lt;TestActivity&gt;(context) { 
    putExtra(&quot;param1&quot;, &quot;data&quot;) 
    putExtra(&quot;param2&quot;, 123) 
} 
</code></pre>
<h4 id="泛型的协变与逆变"><a href="#泛型的协变与逆变" class="headerlink" title="泛型的协变与逆变"></a>泛型的协变与逆变</h4><p><strong>协变</strong></p>
<p> 假如定义了一个MyClass<T>的泛型类，其中A 是B 的子类型，同时MyClass<font style="color:rgb(38, 38, 38);"><A></font>又是MyClass<B>的子类型，那么我们就可以称MyClass在T 这个泛型上是协变的。  子→父   对应到 子→父</p>
<pre><code class="language-kotlin">open class Animal
class Dog : Animal()
class Cat : Animal()

// 默认：Box&lt;T&gt; 是不变的
class Box&lt;T&gt;(var item: T)

fun main() {
    val dogBox: Box&lt;Dog&gt; = Box(Dog())
    val animalBox: Box&lt;Animal&gt; = dogBox  // ❌ 编译错误
    // 虽然 Dog 是 Animal 的子类
    // 但 Box&lt;Dog&gt; 不是 Box&lt;Animal&gt; 的子类
}
</code></pre>
<p>**如果一个泛型类在其泛型类型的数据上是只读的话，那么它是没有类型转换安全隐患的。**而要实现这一点，则 需要让MyClass类中的所有方法都不能接收T类型的参数。换句话说，T只能出现在out位置 上，而不能出现在in位置上。  </p>
<pre><code class="language-kotlin">class SimpleData&lt;T&gt; { 
    private var data: T? = null 
    fun set(t: T?) { 
        data = t 
    } 
    fun get(): T? { 
        return data 
    } 
} 

fun main() { 
    val student = Student(&quot;Tom&quot;, 19) 
    val data = SimpleData&lt;Student&gt;() 
    data.set(student) 
    handleSimpleData(data) // 实际上这行代码会报错，这里假设它能编译通过 
    val studentData = data.get() 
} 
// Student 作为 Person 的子类，若可以能够 协变  
//  那么 handleSimpleData(SimpleData&lt;Student&gt;())  
//  会在SimpleData&lt;Student&gt;()中添加 Teacher 导致编译无法通过
fun handleSimpleData(data: SimpleData&lt;Person&gt;) { 
    val teacher = Teacher(&quot;Jack&quot;, 35) 
    data.set(teacher) 
} 
</code></pre>
<p><font style="color:#DF2A3F;"> T只能出现在out位置 上，而不能出现在in位置上。  </font></p>
<pre><code class="language-kotlin">// 声明处协变：在定义类时声明 out
class ReadOnlyBox&lt;out T&gt;(private val item: T) {
    fun get(): T = item
    // 不能有以T为参数的方法
    // fun set(value: T) { }  // ❌
}

// 使用处协变：在使用时声明
interface Source&lt;out T&gt; {
    fun get(): T
}

fun main() {
    val dogBox: ReadOnlyBox&lt;Dog&gt; = ReadOnlyBox(Dog())
    val animalBox: ReadOnlyBox&lt;Animal&gt; = dogBox  // ✅ 允许！
    // 因为T是&quot;输出&quot;位置
    
    val animal: Animal = animalBox.get()  // 安全
}
</code></pre>
<p><strong>逆变</strong></p>
<p> 假如定义了一个 MyClass<font style="color:rgb(38, 38, 38);"><T></font>的泛型类，其中A是B的子类型，同时MyClass<font style="color:rgb(38, 38, 38);"><B></font>又是MyClass<font style="color:rgb(38, 38, 38);"><A></font>的子类型， 那么我们就可以称MyClass在T这个泛型上是逆变的。**     子→父 对应  父→子**</p>
<blockquote>
<ul>
<li>如果<code>Dog</code>是<code>Animal</code>的子类</li>
<li>那么<code>Consumer&lt;Animal&gt;</code>是<code>Consumer&lt;Dog&gt;</code>的子类</li>
</ul>
</blockquote>
<pre><code class="language-kotlin">// 声明处逆变
class Consumer&lt;in T&gt; {
    fun consume(value: T) {
        println(&quot;Consuming: $value&quot;)
    }
    // 不能有返回T的方法
    // fun produce(): T  // ❌
}

// 使用处逆变
interface Comparable&lt;in T&gt; {
    operator fun compareTo(other: T): Int
}
</code></pre>
<p><strong>需要对接口加入 in 关键字</strong></p>
<pre><code class="language-kotlin">fun main() { 
    val trans = object : Transformer&lt;Person&gt; { 
        override fun transform(name: String, age: Int): Person { 
            return Teacher(name, age) 
        } 
    }
        handleTransformer(trans) 
} 
// 现在 Transformer&lt;Student&gt; 为  Transformer&lt;Person&gt; 的父类 
//  Transformer&lt;Student&gt; 存在 transform方法 但其返回 Teacher 导致报错
fun handleTransformer(trans: Transformer&lt;Student&gt;) { 
    val result = trans.transform(&quot;Tom&quot;, 19) 
}
</code></pre>
<h3 id="类委托与委托属性"><a href="#类委托与委托属性" class="headerlink" title="类委托与委托属性"></a>类委托与委托属性</h3><p>委托是一种设计模式，它的基本理念是：操作对象自己不会去处理某段逻辑，而是会<strong>把工作委托给另外一个辅助对象去处理</strong>。  </p>
<h4 id="类委托"><a href="#类委托" class="headerlink" title="类委托"></a>类委托</h4><p>在接口声明的后面使用by关键字，再接上受委托 的辅助对象，就可以免去之前所写的一大堆模板式的代码了  </p>
<h4 id="委托属性"><a href="#委托属性" class="headerlink" title="委托属性"></a>委托属性</h4><p> 委托属性的核心思想是将 一个属性（字段）的具体实现委托给另一个类去完成。</p>
<pre><code class="language-kotlin">class MyClass { 
    var p by Delegate() 
} 

// 当调用p属性的时候会自动调用Delegate类的getValue()方法，
// 当给p属性赋值的时候会自动调用Delegate类的setValue()方法。
class Delegate { 
    var propValue: Any? = null 
    // 第一个参数用于声明该Delegate类的委托功能可以在什么类中使用
    // 第二个参数KProperty&lt;*&gt;是Kotlin中的一个属性操作类，可用于获取各种属性相关的值
    operator fun getValue(myClass: MyClass, prop: KProperty&lt;*&gt;): Any? { 
        return propValue 
    } 
    operator fun setValue(myClass: MyClass, prop: KProperty&lt;*&gt;, value: Any?) { 
        propValue = value 
    }
}
</code></pre>
<p>  懒加载的实现</p>
<pre><code class="language-kotlin">fun &lt;T&gt; later(block: () -&gt; T) = Later(block) 

class Later&lt;T&gt;(val block: () -&gt; T) { 
    var value: Any? = null 
    operator fun getValue(any: Any?, prop: KProperty&lt;*&gt;): T { 
        // 值为空时采取获取
        if (value == null) { 
            value = block() 
        } 
        return value as T 
    } 
} 


// 调用
val uriMatcher by later { 
    val matcher = UriMatcher(UriMatcher.NO_MATCH) 
    matcher.addURI(authority, &quot;book&quot;, bookDir) 
    matcher.addURI(authority, &quot;book/#&quot;, bookItem) 
    matcher.addURI(authority, &quot;category&quot;, categoryDir) 
    matcher.addURI(authority, &quot;category/#&quot;, categoryItem) 
    matcher 
} 
</code></pre>
<h2 id="infix"><a href="#infix" class="headerlink" title="infix"></a>infix</h2><pre><code class="language-kotlin">infix fun String.beginsWith(prefix: String) = startsWith(prefix) 
</code></pre>
<p>infix 实现了下述调用方式</p>
<pre><code class="language-kotlin">if (&quot;Hello Kotlin&quot; beginsWith &quot;Hello&quot;) { 
    // 处理具体的逻辑 
} 
</code></pre>
<ul>
<li>不能定义成顶层函数 。 必须是某个类的成员函数，可以使用扩展函数的方式将它定义到某个类当中；  </li>
<li>必须接收且只能接收一个参数</li>
</ul>
<pre><code class="language-kotlin">public infix fun &lt;A, B&gt; A.to(that: B): Pair&lt;A, B&gt; = Pair(this, that) 
</code></pre>
<ul>
<li>public - 公开可见</li>
<li>infix - 中缀函数，可以省略点号和括号调用</li>
<li>&lt;A, B&gt; - 泛型参数，A 和 B 可以是任意类型</li>
<li>A.to - 扩展函数，接收者是类型 A</li>
<li>(that: B) - 参数是类型 B</li>
<li>: Pair&lt;A, B&gt; - 返回值是 Pair&lt;A, B&gt;</li>
<li>&#x3D; Pair(this, that) - 函数体，创建 Pair 对象</li>
</ul>
<h2 id="多线程"><a href="#多线程" class="headerlink" title="多线程"></a>多线程</h2><pre><code class="language-kotlin">thread { 
    // 编写具体的逻辑 
} 
</code></pre>
<h2 id="协程"><a href="#协程" class="headerlink" title="协程"></a>协程</h2><p>线程需要依靠操作系统层面的调度才能实现切换，协程是在编程语言层面实现的。</p>
<p><a href="https://www.yuque.com/iris4096/gnvr42/akonkk97gcqs2xs4#JltHp">Java&#x2F;Kotlin</a></p>
<p>协程可以在单线程模式下模拟多线程编程的效果。</p>
<h3 id="协程的使用"><a href="#协程的使用" class="headerlink" title="协程的使用"></a>协程的使用</h3><ul>
<li><font style="color:rgb(35, 41, 48);">GlobalScope</font><font style="color:rgb(38, 44, 49);">.</font><font style="color:rgb(35, 41, 48);">launch  创建协程的作用域  顶层协程</font></li>
</ul>
<pre><code class="language-kotlin">fun main() { 
    GlobalScope.launch {
        // 程序结束后协程便会结束
        println(&quot;codes run in coroutine scope&quot;) 
    } 
    // 应用程序结束时协程也会结束，因此加上延时
    Thread.sleep(1000) 
} 
</code></pre>
<ul>
<li>runBlocking</li>
</ul>
<p>实现了协程运行结束后整个代码才会结束。</p>
<p>但存在了下面问题，因此不建议使用：</p>
<ol>
<li>阻塞线程违背协程设计初衷</li>
<li>可能导致性能问题 UI 界面卡顿</li>
<li>作用域管理困难</li>
</ol>
<pre><code class="language-kotlin">fun main() { 
    runBlocking { 
        // 协程运行结束代码才会结束
        println(&quot;codes run in coroutine scope&quot;) 
        delay(1500) 
        println(&quot;codes run in coroutine scope finished&quot;) 
    } 
} 
</code></pre>
<ul>
<li>通过使用 launch 可以创建多个协程（在当前协程的作用域下创建子协程）</li>
<li>suspend关键字，可以将任意函数声明成挂起函数 ，而挂起函数之间都是可以互相调用的 （表明该函数可以被挂起，而不阻塞线程）。但不提供协程作用域。</li>
<li><strong>借助coroutineScope函数 ， 以给任意挂起函数提供协程作用域 。</strong><code>&lt;font style=&quot;color:rgb(17, 17, 51);background-color:rgba(175, 184, 193, 0.2);&quot;&gt;coroutineScope&lt;/font&gt;</code><font style="color:rgb(17, 17, 51);"> 会创建一个</font><strong><font style="color:rgb(17, 17, 51);">子协程作用域</font></strong><font style="color:rgb(17, 17, 51);">，集成父协程的上下文，父协程会</font><strong><font style="color:rgb(17, 17, 51);">自动等待</font></strong><font style="color:rgb(17, 17, 51);">所有在 </font><code>&lt;font style=&quot;color:rgb(17, 17, 51);background-color:rgba(175, 184, 193, 0.2);&quot;&gt;coroutineScope&lt;/font&gt;</code><font style="color:rgb(17, 17, 51);"> 中启动的子协程完成，才继续执行后续代码。如果子协程抛出异常，会</font><strong><font style="color:rgb(17, 17, 51);">取消其他子协程</font></strong><font style="color:rgb(17, 17, 51);">，并向上抛出异常。</font></li>
<li><font style="color:rgb(17, 17, 51);"> 虽然coroutineScope 和runBlocking 都可以 保证其作用域内的所 有代码和子协程在全部执行完之前，外部的协程会一直被挂起，</font><strong><font style="color:rgb(17, 17, 51);">但</font></strong><font style="color:rgb(17, 17, 51);"> </font><strong><font style="color:rgb(17, 17, 51);">coroutineScope函数只会阻塞当前协程，既不影响其他协程，也不影响任何线程，因此是不 会造成任何性能上的问题的。而runBlocking会挂起外部线程</font></strong><font style="color:rgb(17, 17, 51);"> 。</font></li>
</ul>
<pre><code class="language-kotlin">fun main() { 
    runBlocking { 
        coroutineScope { 
            launch { 
                for (i in 1..10) { 
                    println(i) 
                    delay(1000) 
                } 
            } 
        } 
        println(&quot;coroutineScope finished&quot;) 
    } 
    println(&quot;runBlocking finished&quot;) 
} 
</code></pre>
<p><img loading="lazy" src="https://cdn.nlark.com/yuque/0/2026/png/40667138/1769848078231-86f75980-9adc-4767-9740-2e102c4ec728.png"></p>
<p><strong>取消协程</strong></p>
<pre><code class="language-kotlin">val job = GlobalScope.launch { 
    // 
处理具体的逻辑 
} 
job.cancel() 
</code></pre>
<p>GlobalScope.launch 创建的是<font style="color:rgb(35, 41, 48);">顶层协程，没有父级管理，不会自动取消，容易导致内存泄漏和资源浪费。</font></p>
<p><font style="color:rgb(35, 41, 48);">与线程管理的区别：</font></p>
<ul>
<li>✅<font style="color:rgb(35, 41, 48);"> </font><strong>父协程可以管理运行在不同线程中的子协程</strong></li>
<li>✅<font style="color:rgb(35, 41, 48);"> </font><strong>管理是基于逻辑关系（Job父子关系），不是基于线程</strong></li>
<li>✅<font style="color:rgb(35, 41, 48);"> </font><strong>线程只是协程运行的”位置”，不影响父子管理关系</strong></li>
<li>✅<font style="color:rgb(35, 41, 48);"> </font><strong>父协程取消时，所有子协程无论在哪条线程都会收到取消信号</strong></li>
</ul>
<p>每个协程都有自己的作用域<br><img loading="lazy" src="https://cdn.nlark.com/yuque/0/2026/png/40667138/1769849098158-83381610-1e9a-4872-9d6a-42ec95d4dbba.png"></p>
<p><strong>实际使用</strong></p>
<pre><code class="language-kotlin">val job = Job() 
val scope = CoroutineScope(job) 
scope.launch { 
    // 处理具体的逻辑 
} 
job.cancel() 
</code></pre>
<h3 id="异步"><a href="#异步" class="headerlink" title="异步"></a>异步</h3><p>** async**： 创建一个新的子协程并返回一个Deferred对象，如果我们想要获取async函数代码块的执行结果，只需要调用Deferred对象的await() 方法即可 。</p>
<p> <strong>withContext()</strong> ： 挂起函数  </p>
<pre><code class="language-kotlin">fun main() { 
    runBlocking { 
        val result = withContext(Dispatchers.Default) { 
            5 + 5 
        } 
        println(result) 
    } 
} 
</code></pre>
<ul>
<li>调用withContext()函数之后，会立即执行代码块中的代码，同时将外部协程挂起。当代码块中的代码全部执行完之后，会将最后一行的执行结果作为 withContext()函数的返回值返回  </li>
<li>withContext()函数强制要求我们指定一个线程参数</li>
</ul>
<blockquote>
<p> 线程参数主要有以下3种值可选：<strong>Dispatchers.Default、Dispatchers.IO和 Dispatchers.Main</strong>。Dispatchers.Default表示会使用一种默认<strong>低并发</strong>的线程策略，当 你要执行的代码属于<strong>计算密集型</strong>任务时，开启过高的并发反而可能会影响任务的运行效率，此 时就可以使用Dispatchers.Default。Dispatchers.IO表示会使用一种<strong>较高并发的线程策略</strong>，当你要执行的代码大多数时间是在阻塞和等待中，比如说执行<strong>网络请求</strong>时，为了能够支持更高的并发数量，此时就可以使用Dispatchers.IO。<strong>Dispatchers.Main则表示不会开启子线程，<strong>而是在Android</strong>主线程中执行代码</strong>，但是这个值只能在Android项目中使用，纯Kotlin 程序使用这种类型的线程参数会出现错误  </p>
</blockquote>
<h3 id="简化回调写法"><a href="#简化回调写法" class="headerlink" title="简化回调写法"></a>简化回调写法</h3><p> <strong><font style="color:rgb(38, 38, 38);">suspendCoroutine</font></strong></p>
<p>suspendCoroutine 函数必须在协程作用域或挂起函数中才能调用，它接收一个Lambda表达式参数，主要作用是**将当前协程立即挂起，然后在一个普通的线程中执行Lambda表达式中的代码。**Lambda表达式的参数列表上会传入一个Continuation参数，调用它的resume()方 法或resumeWithException()可以让协程恢复执行。</p>
<pre><code class="language-kotlin">suspend fun request(address: String): String { 
    return suspendCoroutine { continuation -&gt; 
        HttpUtil.sendHttpRequest(address, object : HttpCallbackListener { 
            override fun onFinish(response: String) { 
                continuation.resume(response) 
            } 
            override fun onError(e: Exception) { 
                continuation.resumeWithException(e) 
            } 
        }) 
    } 
} 


// 调用
suspend fun getBaiduResponse() { 
    try { 
        val response = request(&quot;https://www.baidu.com/&quot;) 
        // 对服务器响应的数据进行处理 
    } catch (e: Exception) { 
        // 对异常情况进行处理 
    } 
} 
</code></pre>
<p>网络请求</p>
<pre><code class="language-kotlin">// 定义为  Call&lt;T&gt; 的扩展函数
suspend fun &lt;T&gt; Call&lt;T&gt;.await(): T { 
    return suspendCoroutine { continuation -&gt; 
        enqueue(object : Callback&lt;T&gt; { 
            override fun onResponse(call: Call&lt;T&gt;, response: Response&lt;T&gt;) { 
                val body = response.body() 
                if (body != null) continuation.resume(body) 
                else continuation.resumeWithException( 
                    RuntimeException(&quot;response body is null&quot;)) 
            } 

            override fun onFailure(call: Call&lt;T&gt;, t: Throwable) { 
                continuation.resumeWithException(t) 
            } 
        }) 
    } 
} 

// 调用
suspend fun getAppData() { 
    try { 
        val appList = ServiceCreator.create&lt;AppService&gt;().getAppData().await() 
        // 对服务器响应的数据进行处理 
    } catch (e: Exception) { 
        // 对异常情况进行处理 
    } 
} 
</code></pre>
<p>Retrofit 库中已直接支持</p>
<pre><code class="language-kotlin">// 🆕 Retrofit 2.6.0+ 原生协程
interface AppService {
    // 方式1: 直接返回数据（推荐）
    @GET(&quot;apps&quot;)
    suspend fun getAppData(): List&lt;App&gt;  // Retrofit 自动处理
    
    // 方式2: 返回完整 Response（需要更多控制时）
    @GET(&quot;apps&quot;)
    suspend fun getAppDataWithResponse(): Response&lt;List&lt;App&gt;&gt;
    
    // 方式3: 返回 Result 类型（Kotlin 1.3+）
    @GET(&quot;apps&quot;)
    suspend fun getAppDataResult(): Result&lt;List&lt;App&gt;&gt;
}

// 使用 - 非常简单！
suspend fun loadApps() {
    try {
        val apps = appService.getAppData()  // 直接调用
        updateUI(apps)
    } catch (e: Exception) {
        showError(e)
    }
}
</code></pre>
]]></description>
    </item>
    <item>
      <title>Java总结</title>
      <link>https://raindrop2508.github.io/2026/02/26/tech/Java%E6%80%BB%E7%BB%93/</link>
      <guid>https://raindrop2508.github.io/2026/02/26/tech/Java%E6%80%BB%E7%BB%93/</guid>
      <pubDate>2 月 26 日，2026</pubDate>
      <description><![CDATA[<h2 id="java基础"><a href="#java基础" class="headerlink" title="java基础"></a><font style="color:rgb(51, 51, 51);">java基础</font></h2><p><img loading="lazy" src="https://cdn.nlark.com/yuque/0/2026/svg/40667138/1770296441939-771a3983-d975-4865-9d12-94356a805f61.svg"></p>
<ul>
<li><font style="color:rgb(51, 51, 51);">类：类的成员（方法和变量）。</font></li>
<li><font style="color:rgb(51, 51, 51);">静态语言与动态语言的区别</font><ol>
<li><font style="color:rgb(51, 51, 51);">类型检查时机</font></li>
<li><font style="color:rgb(51, 51, 51);">变量类型是否可变</font></li>
</ol>
</li>
<li><font style="color:rgb(51, 51, 51);">类的实例化（对象）</font></li>
<li><font style="color:rgb(51, 51, 51);">实例变量,构造器（决定如何实例化类）</font></li>
<li><font style="color:rgb(51, 51, 51);">静态方法与非静态方法及其区别（调用）</font></li>
<li><font style="color:rgb(51, 51, 51);">private、this关键词</font></li>
</ul>
<p><font style="color:rgb(51, 51, 51);">对于变量对应的是变量的值，对象和数组对应的是内存位置的指针。</font></p>
<p><font style="color:rgb(51, 51, 51);">Java中使用变量必须实现声明；变量的定义和声明可以用一条语句实现。对于对象和数组需要进行实例化，通过定义将内存中的数据与对象（数组名）连接。</font></p>
<h2 id="lists"><a href="#lists" class="headerlink" title="lists"></a><font style="color:rgb(51, 51, 51);">lists</font></h2><blockquote>
<p>主要讲的是 LinkedList 的实现</p>
</blockquote>
<p><font style="color:rgb(51, 51, 51);">首先使用类构建一个单向的链表，每个数据单元保存找该单元的值和下一个单元位置的指针（内存地址）。之后引入sllist，这个类通过嵌套类的方法再原先链表的基础上加入一个中间层操作链表，方便用户调用定义，参数（之前的链表每次加入新的值都需要加入一个指针指向原来首个单元的地址）。并且通过将来的类以及一些变量设置为private，形成一个黑盒，从而避免用户访问而造成错误。</font></p>
<p><font style="color:rgb(51, 51, 51);">通过递归或者是迭代来实现get，size以及addfirst，addlast等方法。但无论是使用使用迭代还是递归有的方法都需要遍历整个链表来实现，这会使得计算的速度随着链表数据的增多而线性增加。然而显然对于这些方法显然有更好的实现方式。对于size方法可以在中间层维护一个size变量每次链表数据长度产生变化便进行记录。虽然其他方法时间会略微延长（几乎可以忽略），但使得size方法的速度大大加快（只用返回一个数就可以）。</font></p>
<p><font style="color:rgb(51, 51, 51);">对于addlast方法根据原来链表书是否有数据需要分情况讨论？在这里可以引入一个</font><strong><font style="color:rgb(51, 51, 51);">哨兵节点</font></strong><font style="color:rgb(51, 51, 51);">使得所有的方法统一。在编写代码中要尽可能地减少特殊情况，less is more 。哨兵节点永远位于链表地最前端，并指向第一个数据哨兵节点内地值可以忽略，添加数据都从器后面添加。</font></p>
<p><font style="color:rgb(51, 51, 51);">要</font><strong><font style="color:rgb(51, 51, 51);">加快addlast方法的访问速度可以使用结合前面的sllist双向链表</font></strong><font style="color:rgb(51, 51, 51);">，为了避免bug可以在尾部再添加一个哨兵节点（它一直位于最后）。</font><strong><font style="color:rgb(51, 51, 51);">进一步节点只用一个哨兵节点即使开头也是结尾，是整个双向链表构成一个环形。</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">为了实现链表的</font><strong><font style="color:rgb(51, 51, 51);">泛型</font></strong><font style="color:rgb(51, 51, 51);">功能，可以在定义类时在类名字后面加入&lt; &gt;其中写入默认储存的类型。在实际声明时可以在进行指定（实例化的地方可写可不写）。需要注意的是: </font><strong><font style="color:rgb(51, 51, 51);">泛型仅适用于引用类型。</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">最后结合数组来实现这些上面类的方法。同时介绍了二维数组。二维数组中实际时是由多个一维数组组成的。数组名指向的时一个储存着各个一维数组的地址。这些一维数组的地址才指向数据。接着介绍了数组和类调用参数和成员的区别。</font></p>
<p><font style="color:rgb(51, 51, 51);">结合数组使用中间层（bureaucracy）构建</font><strong><font style="color:rgb(51, 51, 51);">Alist</font></strong><font style="color:rgb(51, 51, 51);">;设置固定的数组长度，</font><strong><font style="color:rgb(51, 51, 51);">删除末尾的数据只需要将保存的size值减少不去索引末尾的值即可。从用户层面看就是删去了。</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">当内部数组储存的数据慢了之后要再添加数据就需要新建更长的数组复制原数据以及加入新数据。但这种如果加一个数据就延长一次当添加数据量大（现实中这种情况还是存在的）时需要多次创建新的数组，使得速度下降。每次数组慢时直接多增加几个格子并不能完全解决问题。实际解决方案是</font><strong><font style="color:rgb(51, 51, 51);">在原数据长度的基础上倍增数组长度</font></strong><font style="color:rgb(51, 51, 51);">。</font></p>
<p>泛型方法与sllist相似，但是定义数据时需要使用不同语法或者是Java 不允许我们创建一个泛型对象数组）在删除数据是因为泛型后如果仅减少size值但原格子中数据不删除的的话会造成内存的浪费，因此需要替换为null。</p>
<h2 id="测试"><a href="#测试" class="headerlink" title="测试"></a><font style="color:rgb(51, 51, 51);">测试</font></h2><p><font style="color:rgb(51, 51, 51);">在现实中面对大型的工程文件一次debug是需要花费很长的时间的，但可以通过编写测试代码对各个单元（就是其中的各个方法）进行测试。使用Java中的JUnit库可以减少许多的比较之类的重复代码。结合这个库不需要再测试文件中加入主方法，只需要再每个测试方法前加入@org.junit.Test。运行与运行代码略微有些区别。与python类似，通过import语句看以减少调用函数时重复语句的书写。</font></p>
<p><font style="color:rgb(51, 51, 51);">课程中结合选择排序及其测试进行展开。选择排序每次都寻找最小值并于开头交换最后完成排序（概略）。结合测试产生了一种编写程序的方法，测试驱动开发（TDD）。先写测试代码再写各个环节的函数通过测试代码测试程序不断改进，这个方法有利有弊。</font></p>
<h2 id="继承和实现"><a href="#继承和实现" class="headerlink" title="继承和实现"></a><font style="color:rgb(51, 51, 51);">继承和实现</font></h2><p><strong><font style="color:rgb(51, 51, 51);">重载指的是同一个方法名但输入输出参数不同。</font></strong><font style="color:rgb(51, 51, 51);">通过interface可以避免多次重载（overload）。</font><strong><font style="color:rgb(51, 51, 51);">interface 命名作为上位词，其中包括了各子类（下位词）共同拥有的方法，只需表示方法不需要实现。在interface实现方法并加入default关键词。在子类中实现在这些方法时可以在方法上加入@Override标签。</font></strong><font style="color:rgb(51, 51, 51);">（重写标识，便于寻找bug）当子类中没有该实现方法时便使用interface中的实现。静态类型（编译时类型）是变量声明时确定的，实例化时决定变量的动态类型（运行时类型）。动态类型决定动态方法的选择。这种方法的动态选择实际比较复杂， 大概就是现在对象下的方法中寻找，寻找参数最准确的（下位词参数比上位词精准）。</font></p>
<p><font style="color:rgb(51, 51, 51);">子类可以通过</font><strong><font style="color:rgb(51, 51, 51);">extend</font></strong><font style="color:rgb(51, 51, 51);">关键字继承其上级类的方法，同时可以在子类中重写方法@override以及定义新方法。extend表示子类与上类之间地关系是</font><strong><font style="color:rgb(51, 51, 51);">is a</font></strong><font style="color:rgb(51, 51, 51);">。通过extend可以继承上一级类中的所有方法、实例和静态变量以及嵌套类（不包括私有），但类的构造方法并不会继承。实现子类的构造方法时会自动地隐式地继承其上级类地构造方法（也可以显式地添加super语句）。但如果要向上级传入参数则比喻通过super()语句。</font></p>
<h5 id="接口继承与类继承之间的关系"><a href="#接口继承与类继承之间的关系" class="headerlink" title="接口继承与类继承之间的关系"></a><font style="color:rgb(51, 51, 51);">接口继承与类继承之间的关系</font></h5><p><strong><font style="color:rgb(51, 51, 51);">接口继承 has a,类继承 is a</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">类继承：核心是代码复用 + 类型归属。子类继承父类的属性和方法（非 private），体现 “is-a” 的关系（比如 Dog is a Animal）。</font></p>
<p><font style="color:rgb(51, 51, 51);">接口继承：核心是行为约定 + 多态扩展。接口只定义方法签名（JDK8 + 支持默认方法 &#x2F; 静态方法），不关注实现，体现 “can-do” 的关系（比如 Dog can do Runnable）。</font></p>
<p><strong><font style="color:rgb(51, 51, 51);">动态类型与静态类型</font></strong></p>
<p><strong><font style="color:rgb(51, 51, 51);">静态类型（Static Type）</font></strong><font style="color:rgb(51, 51, 51);">：也叫编译时类型，是变量在声明时被显式指定的类型，这个类型在编译阶段就确定，且无法改变。编译阶段就会根据静态类型检查方法 &#x2F; 属性是否存在，不存在则直接报错。</font></p>
<p><strong><font style="color:rgb(51, 51, 51);">动态类型（Dynamic Type）</font></strong><font style="color:rgb(51, 51, 51);">：也叫运行时类型，是变量实际指向的对象的类型，这个类型只有在程序运行时才确定。</font></p>
<pre><code class="language-plain">// 声明变量时，明确指定的类型就是「静态类型」—— Animal
Animal animal = new Dog();
//                ↑↑↑↑
//                实际new出来的对象类型是「动态类型」—— Dog
</code></pre>
<p><font style="color:rgb(51, 51, 51);">当 Java 运行一个被覆盖的方法时，它会在其动态类型中搜索适当的方法签名并运行它。</font></p>
<p><strong><font style="color:rgb(51, 51, 51);">对象类</font></strong></p>
<p><strong><font style="color:rgb(51, 51, 51);">java中所有的类都是对象类的子类。所以每个类都继承对象类的equal等等方法。</font></strong></p>
<p><font style="color:rgb(51, 51, 51);">而有的类也是接口地实际实现。封装：下层地实现对上层不可见，上层调用下层地类或者方法是不需要关注它是如何实现的，减少大量工程的复杂度。但是</font><strong><font style="color:rgb(51, 51, 51);">继承有时会打破封装</font></strong><font style="color:rgb(51, 51, 51);">产生bug。</font></p>
<p><font style="color:rgb(51, 51, 51);">new右边的类代表了变量的动态类型（运行时类型）而等号的左边或声明语句定义了类的静态类型（编译时类型）。编译器检查时会看编译时类型而动态方法选择会看变量的动态类型。子类动态类型可以赋予给父类但不可以反过来。</font></p>
<p><font style="color:rgb(51, 51, 51);">通过（）中定义类型可以使得该语句跳过类型检查。但这并不会改变原变量的类型（动态或静态），只是让编译器将后面的作为括号中的类。</font></p>
<p><font style="color:rgb(51, 51, 51);">Java中需要调用通过重写接口方法类实现高级函数（将函数作为参数）。</font></p>
<p><strong><font style="color:rgb(51, 51, 51);">多态性 Polymorphism</font></strong></p>
<p><strong><font style="color:rgb(51, 51, 51);">「一个接口，多种实现」</font></strong><font style="color:rgb(51, 51, 51);"> 或者 </font><strong><font style="color:rgb(51, 51, 51);">「调用同一个方法，不同对象有不同的表现」</font></strong><font style="color:rgb(51, 51, 51);">。</font></p>
<p><font style="color:rgb(51, 51, 51);">从如何泛型max函数讲起。通过设置定义接口在每个类中继承并实现比较的方法。也可以通过Java中自带的一个接口Comparable</font><font style="color:rgb(167, 167, 167);"><T></font><font style="color:rgb(51, 51, 51);">，但具有泛型的功能。同时在Java中还有一个Comparator的类可以实现按类特定的属性排序。通过在类中定义Comparator子类来实现比较方法。</font></p>
<p><font style="color:rgb(51, 51, 51);">后边讲了Java中的图和集合。相比于python中定义字典和集合只需要使用{ }即可。在Java中则需要准确定义集合或是字典键值对应的类型，同时需要选择其中的实现方式。语法相对复杂但也正因如此自定义程度更高，也能够根据需要选择合适的实现方式，提高效率。抽象数据类型与介于接口和具体的类之间，在抽象数据类型中的方法默认需要书写其实现通过abstract语句可以不用编写实现，方法可以是私有的但同样不能被实例化。最后介绍了引用Java中的包。</font></p>
<h2 id="泛型与包装"><a href="#泛型与包装" class="headerlink" title="泛型与包装"></a><font style="color:rgb(51, 51, 51);">泛型与包装</font></h2><p><strong><font style="color:rgb(51, 51, 51);">java中的每个基本类型的变量都对应一个包装类（wrapper calss）也称为引用类</font></strong><font style="color:rgb(51, 51, 51);">，比如在对于一些泛型的类和方法我们在使用的时候会根据需要定义其类型（如Integer）。这些就是包装类。两者其实可以换转换。Java语法可以自动将基本类转换为引用类（int→Integer）也可以将引用类转换为基本类。但对于数组是无法自动包装和解包装。但实际上使用引用类会占用更多的内存，两者之间的转换会影响性能。</font></p>
<p><font style="color:rgb(51, 51, 51);">同时Java也会自动拓宽（Widening）根据情况需要原变量。比如将一个int的变量输入给函数，但该函数需要的是double型。但反过来就需要手动转换。</font></p>
<p><font style="color:rgb(51, 51, 51);">可变的数据类型诸如之前定义的数组是可以使用方法对其进行更改。在Java中使用final关键词可以定义恒定值，无法被修改。但对于类等数据，在其声明时加入关键词智能保证其对应的变量名内存地址不变。</font></p>
<p><font style="color:rgb(51, 51, 51);">后面构建的map类型，类似于字典具有键值对应的关系。但对于其泛型类型，由于键和值的组合对应在编写map中的一些方法时需要进行一些改变。例如：</font></p>
<pre><code class="language-plain">public static &lt;K,V&gt; V get(Map61B&lt;K,V&gt; map, K key) {
    if map.containsKey(key) {
        return map.get(key);
    }
    return null;
}
</code></pre>
<p><font style="color:rgb(51, 51, 51);">同时为了比较键的需要，需要对应的泛型带有比较方法。</font></p>
<p>public static &lt;K extends Comparable<K>, V&gt; K maxKey(Map61B&lt;K, V&gt; map) {…}</p>
<h2 id="异常，迭代与对象方法"><a href="#异常，迭代与对象方法" class="headerlink" title="异常，迭代与对象方法"></a><font style="color:rgb(51, 51, 51);">异常，迭代与对象方法</font></h2><p><font style="color:rgb(51, 51, 51);">使用import语句可以调用Java中提供的数组接口及其对应的实现，同样也包括集合。使用throw关键词可以返回带有错误信息的语句并使得程序停止运行（不过课程里面的代码使用throw更多一些）。使用try catch语句也可以实现一场捕获。</font></p>
<p><font style="color:rgb(51, 51, 51);">要对用户构造的类实现类似for (String city : s)的迭代操作。借助迭代器的接口在类实现时嵌套对应迭代器类的实现来实现上述功能。相关接口包括</font></p>
<pre><code class="language-plain">public interface Iterable&lt;T&gt; {
    Iterator&lt;T&gt; iterator();
}

public interface Iterator&lt;T&gt; {
    boolean hasNext();
    T next();
}
</code></pre>
<p><font style="color:rgb(51, 51, 51);">使用</font></p>
<pre><code class="language-plain">public Iterator&lt;T&gt; iterator() {
        return new ArraySetIterator();
    }

    private class ArraySetIterator implements Iterator&lt;T&gt; {
        private int wizPos;

        public ArraySetIterator() {
            wizPos = 0;
        }

        public boolean hasNext() {
            return wizPos &lt; size;
        }

        public T next() {
            T returnItem = items[wizPos];
            wizPos += 1;
            return returnItem;
        }
</code></pre>
<p><font style="color:rgb(51, 51, 51);">所有的类都是继承对象类的方法(Object Methods)，但对于自定义的一些类（数组等）其继承的方法不一定适用，可以通过一些重构来实现。</font></p>
<h2 id="包，访问控制及特性"><a href="#包，访问控制及特性" class="headerlink" title="包，访问控制及特性"></a><font style="color:rgb(51, 51, 51);">包，访问控制及特性</font></h2><p><font style="color:rgb(51, 51, 51);">包是组织一系列类和接口的命名空间。声明和调用类时需要完整的包名和类名字可以通过在代码开头添加import语句来省略。通过intelliJ可以直接创建包。报名字中的.符号的分割会以文件目录的形成创建层次文件夹。（将.看作\）；</font></p>
<p><font style="color:rgb(51, 51, 51);">如果在编写类时没有在代码开头写明包，则会自动归类到默认包中默认包中的类无法被在其他包中调用。JAR文件是一种类似压缩包的东西，可以将包中的Java文件打包到一块。</font></p>
<h3 id="访问控制"><a href="#访问控制" class="headerlink" title="访问控制"></a><font style="color:rgb(51, 51, 51);">访问控制</font></h3><p><img loading="lazy" src="https://cdn.nlark.com/yuque/0/2026/png/40667138/1770296441397-0cb1b3b4-f37d-4504-9bc9-efde0b48ed81.png"></p>
<p><font style="color:rgb(119, 119, 119);">回过头来再看自己学习的时候做的笔记，虽然确实有些细节已经记的不太清楚，但却不知不觉发现已经走了很远了。</font></p>
<h2 id="引用及资源"><a href="#引用及资源" class="headerlink" title="引用及资源"></a><font style="color:rgb(51, 51, 51);">引用及资源</font></h2><ul>
<li><p><a href="https://joshhug.gitbooks.io/hug61b/content/"><font style="color:rgb(65, 131, 196);">课程阅读材料</font></a></p>
</li>
<li><p><a href="https://sp18.datastructur.es/"><font style="color:rgb(65, 131, 196);">CS61B Sp18课程表</font></a></p>
</li>
<li><p><a href="https://csdiy.wiki/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E7%AE%97%E6%B3%95/CS61B/"><font style="color:rgb(65, 131, 196);">CS自学指南对CS61B的介绍</font></a></p>
</li>
<li><p><a href="https://www.bilibili.com/video/BV1qt411W7dh"><font style="color:rgb(65, 131, 196);">bilibili英文字幕搬运视频</font></a></p>
</li>
<li><p><a href="https://www.mcatin.com/course/15"><font style="color:rgb(65, 131, 196);">mcatin中文字幕视频</font></a></p>
</li>
<li><p><a href="https://qiushao-e.github.io/2024/08/19/CS61B_Notes/#Week-1"><font style="color:rgb(65, 131, 196);">CS61B 笔记 - Home</font></a></p>
</li>
</ul>
]]></description>
    </item>
    <item>
      <title>H5与JS总结</title>
      <link>https://raindrop2508.github.io/2026/02/26/tech/H5%E4%B8%8EJS%E6%80%BB%E7%BB%93/</link>
      <guid>https://raindrop2508.github.io/2026/02/26/tech/H5%E4%B8%8EJS%E6%80%BB%E7%BB%93/</guid>
      <pubDate>2 月 26 日，2026</pubDate>
      <description><![CDATA[<h2 id="HTML5"><a href="#HTML5" class="headerlink" title="HTML5"></a>HTML5</h2><p>来源：<a href="https://developer.mozilla.org/zh-CN/docs/Learn/Getting_started_with_the_web/HTML_basics">HTML 基础 - 学习 Web 开发 | MDN (mozilla.org)</a></p>
<p>HTML 不是一门编程语言，而是一种用于定义内容结构的标记语言。HTML 由一系列的元素组成，这些元素可以用来包围不同部分的内容，使其以某种方式呈现或者工作。</p>
<p><img loading="lazy" src="https://img-blog.csdnimg.cn/img_convert/a5111407d777774ff3a3975185bf610e.png" alt="image"></p>
<p>HTML的主要元素包括：开始标签（Opening tag），结束标签（Closing tag），内容（Content）和元素（Element）。元素也可以有属性（Attribute）。</p>
<p>属性应该包含：</p>
<ol>
<li><p>在属性与元素名称（或上一个属性，如果有超过一个属性的话）之间的空格符。</p>
</li>
<li><p>属性的名称，并接上一个等号。</p>
</li>
<li><p>由引号所包围的属性值。</p>
</li>
</ol>
<p>将一个元素置于其他元素之中——称作嵌套。</p>
<p>不包含任何内容的元素称为空元素。比如 <img loading="lazy"> 元素。</p>
<pre><code class="language-html">&lt;img src=&quot;images/firefox-icon.png&quot; alt=&quot;My test image&quot; /&gt;
</code></pre>
<p>图像元素不需要通过内容来产生效果，它的作用是向其所在的位置嵌入一个图像。但图像元素可以添加<code>alt</code>作为图像注释，在图像加载不出来的时候使用。</p>
<p>链接非常重要 — 它们赋予 Web 网络属性。要植入一个链接，我们需要使用一个简单的元素 — <code>&lt;a&gt;</code> — a 是 “anchor” （锚）的缩写。</p>
<pre><code>&lt;p&gt;A link to my &lt;a href=&quot;https://www.mozilla.org/&quot; title=&quot;The Mozilla homepage&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;favorite website&lt;/a&gt;.&lt;/p&gt;
</code></pre>
<h3 id="剖析HTML-文档"><a href="#剖析HTML-文档" class="headerlink" title="剖析HTML 文档"></a>剖析HTML 文档</h3><pre><code class="language-html">&lt;!doctype html&gt;  声明类型文档
&lt;html lang=&quot;zh-CN&quot;&gt;   HTML元素，又称为根元素
  &lt;head&gt;        head元素，包含了所有你想包含在 HTML 页面中但不在 HTML 页面中显示的内容。
    &lt;meta charset=&quot;utf-8&quot; /&gt;        meta元素，代表了不能由其他 HTML 元相关元素表示的元数据
    &lt;title&gt;我的测试站点&lt;/title&gt;   页面标题
  &lt;/head&gt;
  &lt;body&gt;  body元素：包含了访问页面时所有显示在页面上的内容
    &lt;p&gt;这是我的页面&lt;/p&gt;  
  &lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p>在 HTML 元素的内容中使用多少空格（包括一个或多个空白字符或换行），当渲染这些代码的时候，HTML 解释器会将连续出现的空白字符减少为一个单独的空格符。</p>
<h3 id="描述列表"><a href="#描述列表" class="headerlink" title="描述列表"></a>描述列表</h3><p>描述列表使用与其他列表类型不同的闭合标签——dl；此外，每一项都用 dt（description term）元素闭合。每个描述都用 dd（description definition）元素闭合。</p>
<h2 id="JavaScript"><a href="#JavaScript" class="headerlink" title="JavaScript"></a>JavaScript</h2><h2 id="JavaScript-1"><a href="#JavaScript-1" class="headerlink" title="JavaScript"></a>JavaScript</h2><p>[JavaScript 基础 - 学习 Web 开发 | MDN](<a href="https://developer.mozilla.org/zh-CN/docs/Learn/Getting_started_with_the_web/JavaScript_basics">JavaScript 基础 - 学习 Web 开发 | MDN (mozilla.org)</a>)</p>
<p>JavaScript（缩写：JS）是一门完备的动态编程语言。当应用于 HTML 文档时，可为网站提供动态交互特性。</p>
<p>HTML引用JS脚本：</p>
<pre><code class="language-html">&lt;script src=&quot;scripts/main.js&quot; defer&gt;&lt;/script&gt;
</code></pre>
<p><strong>变量</strong> 是存储值的容器。要声明一个变量，先输入关键字 <code>let</code> 或 <code>var</code>，然后输入合适的名称。</p>
<h3 id="条件语句"><a href="#条件语句" class="headerlink" title="条件语句"></a>条件语句</h3><pre><code class="language-javascript">let iceCream = &quot;chocolate&quot;;
if (iceCream === &quot;chocolate&quot;) {
  alert(&quot;我最喜欢巧克力冰淇淋了。&quot;);
} else {
  alert(&quot;但是巧克力才是我的最爱呀……&quot;);
}
</code></pre>
<h4 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h4><pre><code class="language-javascript">function multiply(num1, num2) {
  let result = num1 * num2;
  return result;
}
</code></pre>
<p><strong>事件</strong>能为网页添加真实的交互能力。它可以捕捉浏览器操作并运行一些代码做为响应。最简单的事件是点击事件，鼠标的点击操作会触发该事件。</p>
<pre><code class="language-javascript">document.querySelector(&quot;html&quot;).addEventListener(&quot;click&quot;, function () {
  alert(&quot;别戳我，我怕疼。&quot;);
});
document.querySelector(&quot;html&quot;).addEventListener(&quot;click&quot;, () =&gt; {
  alert(&quot;别戳我，我怕疼。&quot;);
});
// 匿名函数的两种写法
</code></pre>
<h2 id="JavaScript-2"><a href="#JavaScript-2" class="headerlink" title="JavaScript"></a>JavaScript</h2><h3 id="JS数组"><a href="#JS数组" class="headerlink" title="JS数组"></a>JS数组</h3><p><code>Push</code>方法:改变原始数组，并且可以接受多个参数，每个参数都会作为一个单独的元素被添加到数组的末尾。</p>
<p><code>concat</code>方法是将两个或多个数组拼接并返回一个新的数组。</p>
<h3 id="JS字符串"><a href="#JS字符串" class="headerlink" title="JS字符串"></a>JS字符串</h3><p>在JavaScript中，你可以使用反引号 ` 或者单引号&#39; 和双引号 &quot; 来创建字符。反引号允许在字符串内直接嵌入变量和表达式，这被称为模板字符串（Template Literals）。单引号用来创建普通的字符串。如果你要在字符串中插入变量或表达式的值，你需要使用字符串拼接操作符 <code>+</code>。</p>
<p><a href="https://segmentfault.com/a/1190000044532720">前端本地开发，URL如何设置？ - 个人文章 - SegmentFault 思否</a></p>
<h3 id="前端请求中body和query传参的区别"><a href="#前端请求中body和query传参的区别" class="headerlink" title="前端请求中body和query传参的区别"></a>前端请求中body和query传参的区别</h3><p>通常情况下，Get请求只能传Query参数，POST可以传Query和Body（常用）两种形式的参数。Query代表请求的参数，URL以<code>?key=value</code>的形式呈现。Query的值只能是一个字符串，不能传递对象类型的参数，如果参数中涉及到了传递对象，就要选择Body传参，一般json等都是Body传参。</p>
<p><strong>Get传Query参数</strong></p>
<pre><code class="language-javascript">// 其他文件中需要传递的参数
let queryParams = {
    project_id: ID
};

// get请求
export function getIDSearchProject(params) {
	return http.get(&#39;api路径&#39;, {params});
}
// 使用方法
getIDSearchProject(queryParams)
</code></pre>
<p><strong>POST传Body参数</strong></p>
<pre><code class="language-javascript">// 其他文件中需要传递的参数
let queryParams = {
    project_id: ID
};

// post请求
export function getSearchProject(param) {
	return http.post(&#39;api路径&#39;, param);
}
// 使用方法
getSearchProject(queryParams)
</code></pre>
<h3 id="不同页面间的数据传递"><a href="#不同页面间的数据传递" class="headerlink" title="不同页面间的数据传递"></a>不同页面间的数据传递</h3><h4 id="通过URL跳转"><a href="#通过URL跳转" class="headerlink" title="通过URL跳转"></a>通过URL跳转</h4><p><strong>跳转页面</strong></p>
<p>对于大部分页面，只需要在跳转URL后面加上参数即可。其中，URL也可使用反引号的形式来表示。</p>
<pre><code class="language-javascript">    gotoHomeProject(id){
        uni.navigateTo({
            url: &#39;/pages/home/index?id=&#39;+ id
            // url: `/pages/home/index?id=${id}` // 使用反引号的另一种写法
        });
    }
</code></pre>
<p>然而，在uni-app中，使用uni.navigateTo无法跳转至tabBar页面。而使用uni.switchTabs跳转时不能携带参数。要在uni-app中实现带参数跳转至tabBar可以使用uni.reLaunch来实现。但需要注意的是该方法会关闭所有页面。</p>
<pre><code class="language-javascript">		gotoHomeProject(id){
			uni.reLaunch({ //关闭所有页面，打开到应用内的某个页面。
				url: &#39;/pages/tabBar/home/index?id=&#39;+ id
			});
		}
</code></pre>
<p><strong>接收页面</strong></p>
<p>要实现接收，只需在onload中将url中所携带的参数赋值给页面中的参数。</p>
<pre><code class="language-javascript">	onLoad(option){
		if(JSON.stringify(option)!=&#39;{}&#39;){
			this.projectId = option.id;
			}
	}
</code></pre>
<h3 id="异步JS"><a href="#异步JS" class="headerlink" title="异步JS"></a>异步JS</h3><p>在同步程序中，浏览器是按照书写代码的顺序一步一步执行。异步可以使程序在执行一个可能长期运行的任务的同时继续对其他事件做出反应而不必等待该任务完成。若使用回调函数进行异步，面对嵌套回调时会造成代码难以阅读和调试的问题。JavaScript 中异步编程主要通过Promise实现。</p>
<h4 id="Promise"><a href="#Promise" class="headerlink" title="Promise"></a>Promise</h4><p>在基于 Promise 的 API 中，异步函数会启动操作并返回一个 <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Promise"><code>Promise</code></a> 对象。然后，可以将处理函数附加到 Promise 对象上，当操作完成时（成功或失败），这些处理函数将被执行。</p>
<p>在控制台中，<code>Promise { &lt;state&gt;: &quot;pending&quot; }</code>表示有一个 <code>Promise</code> 对象，它有一个 <code>state</code>属性，值是 <code>&quot;pending&quot;</code>。<code>&quot;pending&quot;</code> 状态意味着操作仍在进行中。</p>
<p><strong>链式使用Promise</strong></p>
<p>为了支持错误处理，<code>Promise</code> 对象提供了一个 <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Promise/catch"><code>catch()</code></a> 方法。这很像 <code>then()</code>：你调用它并传入一个处理函数。然后，当异步操作<em>成功</em>时，传递给 <code>then()</code> 的处理函数被调用，而当异步操作<em>失败</em>时，传递给 <code>catch()</code> 的处理函数被调用。</p>
<p>如果将 <code>catch()</code> 添加到 Promise 链的末尾，它就可以在任何异步函数失败时被调用。于是，我们就可以将一个操作实现为几个连续的异步函数调用，并在一个地方处理所有错误。</p>
<pre><code class="language-javascript">// 先获取id并通过用户id获取信息
method() {
    // 调用接口
    this.$api.common.getmethod().then((res) =&gt; {
        if (res.length &lt; 1) {
            throw new Error(`。。。。`);
        }
        uni.setStorageSync(&#39;Id&#39;, res[0].id);
        return res[0].id;
        }).then((id) =&gt; {

        this.getInfoById(id);

    }).catch((err) =&gt; {
        this.$u.toast(err.msg);
    });
},
</code></pre>
<h4 id="workers"><a href="#workers" class="headerlink" title="workers"></a>workers</h4><p>通过workers可以实现多线程。</p>
<p>有三种不同类型的 workers：</p>
<ul>
<li>dedicated workers</li>
<li>shared workers</li>
<li>service workers</li>
</ul>
<h3 id="钩子（hook）"><a href="#钩子（hook）" class="headerlink" title="钩子（hook）"></a>钩子（hook）</h3><p><a href="https://segmentfault.com/a/1190000041894663">JavaScript中的钩子(钩子机制\钩子函数\hook)是什么？ - 个人文章 - SegmentFault 思否</a></p>
<p>钩子函数是在一个<strong>事件触发</strong>的时候，在系统级捕获到了他，然后做一些操作。一段用以处理系统消息的程序，用以处理系统消息的程序，是说钩子函数是用于处理系统消息的。回调函数是留了个函数，但是这个函数不是立即执行，而是绑定了个事件。当事件触发时，自动执行函数，而非必须执行。</p>
<h1 id="CSS布局"><a href="#CSS布局" class="headerlink" title="CSS布局"></a>CSS布局</h1><p><a href="https://developer.mozilla.org/zh-CN/docs/Learn/CSS/Building_blocks/The_box_model">盒模型 - 学习 Web 开发 | MDN (mozilla.org)</a></p>
<p>在 CSS 中，一般分为<strong>区块盒子</strong>（block boxes）和<strong>行内盒子</strong>（inline boxes）。类型指的是盒子在页面流中的行为方式以及与页面上其他盒子的关系。盒子有<strong>内部显示</strong>（inner display type）和<strong>外部显示</strong>（outer display type）两种类型。</p>
<h3 id="外部显示类型"><a href="#外部显示类型" class="headerlink" title="外部显示类型"></a>外部显示类型</h3><p><strong>block</strong></p>
<ul>
<li><p>盒子会产生换行。</p>
</li>
<li><p><a href="https://developer.mozilla.org/zh-CN/docs/Web/CSS/width"><code>width</code></a> 和 <a href="https://developer.mozilla.org/zh-CN/docs/Web/CSS/height"><code>height</code></a> 属性可以发挥作用。</p>
</li>
<li><p>如果未指定 <a href="https://developer.mozilla.org/zh-CN/docs/Web/CSS/width"><code>width</code></a>，方框将沿行向扩展，以填充其容器中的可用空间。在大多数情况下，盒子会变得与其容器一样宽，占据可用空间的 100%。</p>
</li>
</ul>
<p><strong>inline</strong></p>
<ul>
<li>盒子不会产生换行。</li>
<li><a href="https://developer.mozilla.org/zh-CN/docs/Web/CSS/width"><code>width</code></a> 和 <a href="https://developer.mozilla.org/zh-CN/docs/Web/CSS/height"><code>height</code></a> 属性将不起作用。</li>
<li>垂直方向的内边距、外边距以及边框会被应用但是不会把其他处于 <code>inline</code> 状态的盒子推开。</li>
</ul>
<h3 id="内部显示类型"><a href="#内部显示类型" class="headerlink" title="内部显示类型"></a>内部显示类型</h3><p>区块和行内布局是网络上的默认行为方式。默认情况下，在没有任何其他指令的情况下，方框内的元素也会以<a href="https://developer.mozilla.org/zh-CN/docs/Learn/CSS/CSS_layout/Normal_Flow"><strong>标准流</strong></a>的方式布局，并表现为区块或行内盒子。</p>
<p>常用的<code>display: flex;</code>，该元素仍将使用外部显示类型 <code>block</code> 但内部显示类型将变为 <code>flex</code>。该方框的任何直接子代都将成为弹性（flex）项，并按照<a href="https://developer.mozilla.org/zh-CN/docs/Learn/CSS/CSS_layout/Flexbox">弹性盒子</a>规范执行。</p>
<h3 id="盒模型的各个部分"><a href="#盒模型的各个部分" class="headerlink" title="盒模型的各个部分"></a>盒模型的各个部分</h3><p><img loading="lazy" src="https://img-blog.csdnimg.cn/img_convert/57c08d4d9e27e50059acefebda330ebb.png" alt="盒模型示意图"></p>
<h3 id="常规流布局"><a href="#常规流布局" class="headerlink" title="常规流布局"></a>常规流布局</h3><p>默认情况下，一个<a href="https://developer.mozilla.org/zh-CN/docs/Glossary/Block-level_content">块级元素</a>会填充其父元素所有的行向空间，并沿着其块向伸长以容纳其内容。<a href="https://developer.mozilla.org/zh-CN/docs/Glossary/Inline-level_content">行级元素</a>的大小就是其本身的大小。你可以为某些 <a href="https://developer.mozilla.org/zh-CN/docs/Web/CSS/display"><code>display</code></a> 属性值默认为 <code>inline</code> 的元素。 <code>display</code> 仍将保持为 <code>inline</code>。</p>
<h3 id="弹性盒子"><a href="#弹性盒子" class="headerlink" title="弹性盒子"></a><a href="https://developer.mozilla.org/zh-CN/docs/Learn/CSS/CSS_layout/Introduction#%E5%BC%B9%E6%80%A7%E7%9B%92%E5%AD%90">弹性盒子</a></h3><p>Flexbox 是 CSS 弹性盒子布局模块（<a href="https://developer.mozilla.org/zh-CN/docs/Web/CSS/CSS_flexible_box_layout">Flexible Box Layout</a> Module）的缩写，它被专门设计出来用于创建横向或是纵向的一维页面布局。要使用 flexbox，只需要在想要进行 flex 布局的父元素上应用<code>display: flex</code> ，所有直接子元素都将会按照 flex 进行布局。</p>
<h3 id="Grid-布局"><a href="#Grid-布局" class="headerlink" title="Grid 布局"></a><a href="https://developer.mozilla.org/zh-CN/docs/Learn/CSS/CSS_layout/Introduction#grid_%E5%B8%83%E5%B1%80">Grid 布局</a></h3><p>Flexbox 用于设计横向或纵向的布局，而 Grid 布局则被设计用于同时在两个维度上把元素按行和列排列整齐。使用<code>display: grid</code>实现。</p>
<h3 id="定位技术"><a href="#定位技术" class="headerlink" title="定位技术"></a><a href="https://developer.mozilla.org/zh-CN/docs/Learn/CSS/CSS_layout/Introduction#%E5%AE%9A%E4%BD%8D%E6%8A%80%E6%9C%AF">定位技术</a></h3><p>定位 (positioning) 能够让我们把一个元素从它原本在正常布局流 (normal flow) 中应该在的位置移动到另一个位置</p>
]]></description>
    </item>
    <item>
      <title>Vue2总结</title>
      <link>https://raindrop2508.github.io/2026/02/26/tech/Vue2%E6%80%BB%E7%BB%93/</link>
      <guid>https://raindrop2508.github.io/2026/02/26/tech/Vue2%E6%80%BB%E7%BB%93/</guid>
      <pubDate>2 月 26 日，2026</pubDate>
      <description><![CDATA[<p>Vue 通过组件允许创建可重用块标记。大多数时候，Vue 组件是使用一个特殊的 HTML 模板的语法写的。当需要比 HTML 语法允许的更多的控制时，可以编写 JSX 或纯 JavaScript 函数来定义组件。一个 Vue 应用由一个通过 <code>new Vue</code> 创建的<strong>根 Vue 实例</strong>，以及可选的嵌套的、可复用的组件树组成。</p>
<p>template是一级节点，用于写tag组件，script和style是并列的一级节点，也就是有3个一级节点。</p>
<ul>
<li><p><a href="https://developer.mozilla.org/zh-CN/docs/Glossary/HTML">HTML</a> 是一种标记语言，用来结构化我们的网页内容并赋予内容含义，例如定义段落、标题和数据表，或在页面中嵌入图片和视频。</p>
</li>
<li><p><a href="https://developer.mozilla.org/zh-CN/docs/Glossary/CSS">CSS</a> 是一种样式规则语言，可将样式应用于 HTML 内容，例如设置背景颜色和字体，在多个列中布局内容。</p>
</li>
<li><p><a href="https://developer.mozilla.org/zh-CN/docs/Glossary/JavaScript">JavaScript</a> 是一种脚本语言，可以用来创建动态更新的内容，控制多媒体，制作图像动画，还有很多。</p>
</li>
</ul>
<h3 id="DOM"><a href="#DOM" class="headerlink" title="DOM"></a>DOM</h3><p>文档对象模型（Document Object model，简称DOM）是针对HTML和XML文档的一个API（应用程序编程接口）。文档对象模型（DOM）通过将文档的结构（例如表示网页的 HTML）以对象的形式存储在内存中，将网页与脚本或编程语言连接起来。DOM 使用逻辑树的形式来表示文档。树的每个分支末端都是一个节点，每个节点都包含对象。DOM 方法允许以编程方式来访问树。借助这些方法，你可以改变文档的结构、样式或内容。</p>
<h3 id="指令"><a href="#指令" class="headerlink" title="指令"></a>指令</h3><p>指令 (Directives) 是带有 <code>v-</code> 前缀的特殊 attribute。指令 attribute 的值预期是<strong>单个 JavaScript 表达式</strong> (<code>v-for</code> 是例外情况）。</p>
<pre><code class="language-html">&lt;!-- 完整语法 --&gt;
&lt;a v-on:click=&quot;doSomething&quot;&gt;...&lt;/a&gt;

&lt;!-- 缩写 --&gt;
&lt;a @click=&quot;doSomething&quot;&gt;...&lt;/a&gt;

&lt;!-- 完整语法 --&gt;
&lt;a v-bind:href=&quot;url&quot;&gt;...&lt;/a&gt;

&lt;!-- 缩写 --&gt;
&lt;a :href=&quot;url&quot;&gt;...&lt;/a&gt;
</code></pre>
<h3 id="计算属性与方法属性"><a href="#计算属性与方法属性" class="headerlink" title="计算属性与方法属性"></a>计算属性与方法属性</h3><p><strong>计算属性是基于它们的响应式依赖进行缓存的</strong>。只在相关响应式依赖发生改变时它们才会重新求值。相比之下，每当触发重新渲染时，调用方法将<strong>总会</strong>再次执行函数。因此，计算属性对于性能要求更低。</p>
<h4 id="getter和setter的含义"><a href="#getter和setter的含义" class="headerlink" title="getter和setter的含义"></a>getter和setter的含义</h4><blockquote>
<h4 id="Getter"><a href="#Getter" class="headerlink" title="Getter"></a>Getter</h4><ol>
<li><strong>含义</strong>:<ul>
<li>Getter 是计算属性的主要组成部分，它是一个函数，用于获取计算属性的值。</li>
<li>当你访问计算属性时，实际上是在调用 getter 方法。</li>
</ul>
</li>
<li><strong>用途</strong>:<ul>
<li>Getter 方法通常用于基于其他数据属性计算出一个新值。</li>
<li>Getter 方法可以访问组件的所有数据和其他计算属性。</li>
<li>当依赖的数据发生变化时，Vue.js 会自动调用 getter 方法来更新计算属性的值。</li>
</ul>
</li>
</ol>
<h4 id="Setter"><a href="#Setter" class="headerlink" title="Setter"></a>Setter</h4><ol>
<li><strong>含义</strong>:<ul>
<li>Setter 是一个可选的部分，它也是一个函数，用于设置计算属性的值。</li>
<li>当你试图改变一个只读的计算属性时，Vue.js 会自动调用 setter 方法。</li>
</ul>
</li>
<li><strong>用途</strong>:<ul>
<li>Setter 方法通常用于更新其他数据属性的值，而不是直接改变计算属性本身。</li>
<li>当计算属性被赋值时，Vue.js 会调用 setter 方法。</li>
</ul>
</li>
</ol>
</blockquote>
<h3 id="箭头函数表达式"><a href="#箭头函数表达式" class="headerlink" title="箭头函数表达式&#x3D;&gt;"></a>箭头函数表达式&#x3D;&gt;</h3><pre><code>() =&gt; {
  statements
}

param =&gt; {
  statements
}
(param1, paramN) =&gt; {
  statements
}
</code></pre>
<p>箭头函数可以是 async 的，方法是在表达式前加上 async 关键字。</p>
<pre><code>async param =&gt; expression
async (param1, param2, ...paramN) =&gt; {
  statements
}
</code></pre>
<h3 id="异步操作和Async-Await"><a href="#异步操作和Async-Await" class="headerlink" title="异步操作和Async&#x2F;Await"></a>异步操作和Async&#x2F;Await</h3><h4 id="promise"><a href="#promise" class="headerlink" title="promise"></a>promise</h4><p>Promise 是现代 JavaScript 中异步编程的基础。它是一个由异步函数返回的对象，可以指示操作当前所处的状态。在 Promise 返回给调用者的时候，操作往往还没有完成，但 Promise 对象提供了方法来处理操作最终的成功或失败。</p>
<p>Promise 的优雅之处在于 <em><code>then()</code> 本身也会返回一个 Promise，这个 Promise 将指示 <code>then()</code> 中调用的异步函数的完成状态</em>。由此可以链式使用promise。</p>
<p>promise有**待定（pending）、已兑现（fulfilled）和已拒绝（rejected）**三种状态。</p>
<h4 id="Async和Await"><a href="#Async和Await" class="headerlink" title="Async和Await"></a>Async和Await</h4><p>在一个函数的开头添加 <code>async</code>，就可以使其成为一个异步函数。</p>
<p>在 uni-app 中，<code>async</code> 和 <code>await</code> 的底层逻辑与原理与其他 JavaScript 环境相同。</p>
<p>在Vue组件的生命周期中，有一些钩子函数是异步的，比如created和mounted。这些钩子函数允许你在组件被创建或挂载到DOM之后执行异步操作。</p>
<pre><code class="language-javascript">this.$api.common.login(this.loginForm).then(async res =&gt; {   // async 异步  箭头函数去除掉function关键字，在形参列表和函数体之间使用=&gt;连接
    if (res.code == 200) {
    // 存储token和账号
    this.saveToken(res.token);
    this.saveAccount();
    // 获取用户信息
    let profile = await this.getProfile();   // 等待异步完成
    uni.$u.vuex(&#39;profile&#39;, profile);
    // 跳转主页
    uni.switchTab({
    url: &#39;/pages/tabBar/home/index&#39;
    });
</code></pre>
<p><code>async</code> 函数是一个特殊的函数，它总是返回一个 Promise。即使你不显式地返回一个 Promise，<code>async</code> 函数也会返回一个已解决的 Promise，其值为 <code>undefined</code>。</p>
<p><code>await</code> 关键字只能在 <code>async</code> 函数内部使用。它用于等待一个 Promise 的结果。当 <code>await</code> 一个 Promise 时，JavaScript 引擎会暂停执行 <code>async</code> 函数，直到 Promise 解决或被拒绝。</p>
<p><code>async</code> 和 <code>await</code> 的底层原理基于 Promise。Promise 是 JavaScript 中处理异步操作的标准方式之一，它有三种状态：pending（挂起）、fulfilled（已解决）和 rejected（已拒绝）。Promise 对象代表了一个最终可能会完成或失败的异步操作，并附带了完成或失败时的值。</p>
<p><code>await</code> 关键字用于等待一个 Promise 的结果。当你在 <code>async</code> 函数中使用 <code>await</code> 关键字时，JavaScript 引擎会暂停执行 <code>async</code> 函数，直到 Promise 被解决或被拒绝。如果 Promise 被解决，<code>await</code> 表达式的值将是解决值；如果 Promise 被拒绝，<code>await</code> 表达式会抛出一个错误，这可以通过 <code>try...catch</code> 语句来处理。</p>
<h3 id="插槽（slot）"><a href="#插槽（slot）" class="headerlink" title="插槽（slot）"></a>插槽（slot）</h3><pre><code class="language-html">    &lt;!-- 根据不同条件渲染图片 --&gt;
    &lt;u--image v-if=&quot;device.imgUrl&quot; :src=&quot;device.imageUrl&quot; radius=&quot;10&quot; mode=&quot;aspectFill&quot;
    width=&quot;70&quot; height=&quot;70&quot;&gt;
    &lt;!-- 插槽（slot）显示不同状态时显示的图像 --&gt;
    &lt;view slot=&quot;error&quot; style=&quot;font-size: 12px;&quot;&gt;加载失败&lt;/view&gt;
    &lt;template v-slot:loading&gt;
    &lt;u-loading-icon&gt;&lt;/u-loading-icon&gt;
    &lt;/template&gt;
</code></pre>
<p>slot元素是——Web 组件技术套件的一部分——它是一个在 web 组件内部的占位符，你可以使用自己的标记来填充该占位符，从而创建单独的 DOM 树并将其一起呈现。</p>
<h3 id="template中：的使用"><a href="#template中：的使用" class="headerlink" title="template中：的使用"></a>template中<code>：</code>的使用</h3><p><a href="https://blog.csdn.net/qq_28369007/article/details/140122483">Vue.js 中属性绑定的详细解析：冒号 `:` 和非冒号的区别_vue 属性 冒号-CSDN博客</a></p>
<p><code>v-bind</code> attribute 被称为指令。指令带有前缀 <code>v-</code>，以表示它们是 Vue 提供的特殊 attribute。</p>
<p>在设置参数时是否添加<code>：</code>主要取决于是在设置HTML属性（attributes）还是Vue特有的指令。它们会在渲染的 DOM 上应用特殊的响应式行为。</p>
<h4 id="不需要-的情况"><a href="#不需要-的情况" class="headerlink" title="不需要:的情况"></a>不需要<code>:</code>的情况</h4><p>静态HTML属性：当你直接在元素上写属性时，如<code>&lt;div id=&quot;app&quot;&gt;</code>，这里的<code>id=&quot;app&quot;</code>是一个静态的HTML属性，其值在编译时就已经确定，不会随着Vue实例中的数据变化而变化。</p>
<p>Vue指令但非属性绑定：对于某些Vue指令，如<code>v-model</code>、<code>v-if</code>、<code>v-for</code>等，它们不是用于绑定HTML属性的，而是Vue特有的模板指令，用于实现数据绑定、条件渲染、列表渲染等功能。这些指令不需要<code>:</code>前缀，因为它们不是HTML属性的绑定。</p>
<h4 id="需要-的情况"><a href="#需要-的情况" class="headerlink" title="需要:的情况"></a>需要<code>:</code>的情况</h4><p>动态HTML属性绑定：当你想要根据Vue实例中的数据动态地设置HTML元素的属性时，就需要使用<code>v-bind</code>指令。为了简化，Vue允许使用<code>:</code>作为<code>v-bind</code>的简写。例如，<code>&lt;div :id=&quot;dynamicId&quot;&gt;</code>中的<code>:id=&quot;dynamicId&quot;</code>表示<code>id</code>属性的值将绑定到Vue实例的<code>dynamicId</code>数据属性上。如果<code>dynamicId</code>的值发生变化，那么<code>&lt;div&gt;</code>的<code>id</code>属性也会相应地更新。</p>
<pre><code class="language-html">&lt;!-- 完整语法 --&gt;
&lt;a v-on:click=&quot;doSomething&quot;&gt;...&lt;/a&gt;

&lt;!-- 缩写 --&gt;
&lt;a @click=&quot;doSomething&quot;&gt;...&lt;/a&gt;
</code></pre>
<p>动态绑定和静态绑定在性能上有所差异。动态绑定会增加一定的渲染开销，因为 Vue.js 需要监视表达式的变化并进行响应式更新。静态绑定则不会有这样的性能开销，因为它们是静态的值。</p>
<h3 id="免密码登录相关"><a href="#免密码登录相关" class="headerlink" title="免密码登录相关"></a>免密码登录相关</h3><p>从本地缓存中读取token值，并将其存储到Vuex状态管理器中，以便在需要的地方可以方便地访问此token值。</p>
<pre><code>getToken () {
    // 本地缓存获取token
    this.token = uni.getStorageSync(&#39;token&#39;);
    // vuex存储token
    uni.$u.vuex(&#39;vuex_token&#39;, this.token);
}
</code></pre>
<h4 id="token、session和cookie三者之间的区别"><a href="#token、session和cookie三者之间的区别" class="headerlink" title="token、session和cookie三者之间的区别"></a>token、session和cookie三者之间的区别</h4><ul>
<li><p><strong>安全性</strong>：Token 通常比 Cookie 更安全，因为它包含了加密签名，并且可以设计为不持久化存储。而 Cookie 存储在客户端，更容易受到攻击。</p>
</li>
<li><p><strong>可扩展性</strong>：Token 机制更适合于无状态架构和分布式系统，因为它不需要服务器维护会话状态。</p>
</li>
<li><p><strong>跨域问题</strong>：Token 可以轻松解决跨域问题，而传统的 Session Cookie 受到同源策略的限制。</p>
</li>
</ul>
<h3 id="this-refs-form"><a href="#this-refs-form" class="headerlink" title="this.$refs.form"></a>this.$refs.form</h3><p><code>this.$refs.form</code> 是一个引用，指向了模板中带有 <code>ref=&quot;form&quot;</code> 的表单元素或组件。</p>
<p>在 Vue 组件的模板中使用 <code>ref</code> 属性时，Vue 会在该组件实例的 <code>$refs</code> 对象中创建一个对应的属性，该属性的值就是对应的子组件实例或 DOM 元素。</p>
<p>注意：<code>$refs</code> 的值在组件渲染完成之前是 <code>undefined</code>。因此，你不能在 <code>created</code> 钩子中直接访问 <code>$refs</code>，而应该在 <code>mounted</code> 钩子或之后的生命周期中访问。</p>
<h4 id="页面结构"><a href="#页面结构" class="headerlink" title="页面结构"></a>页面结构</h4><p><a href="https://uniapp.dcloud.net.cn/tutorial/page.html">页面 | uni-app官网 (dcloud.net.cn)</a></p>
<p>一个页面内，有3个根节点标签：</p>
<ul>
<li>模板组件区 <code>&lt;template&gt;</code></li>
<li>脚本区 <code>&lt;script&gt;</code></li>
<li>样式区 <code>&lt;style&gt;</code></li>
</ul>
<p>在 vue 文件中，<code>template</code>、<code>script</code>、<code>style</code> 这3个是平级关系。<strong>在vue2中，<code>template</code> 的二级节点只能有一个节点</strong>，一般是在一个根 <code>view</code> 下继续写页面组件。但<strong>在vue3中，template可以有多个二级节点，省去一个层级</strong>。</p>
<p>在脚本区中，开发者应谨慎编写 <code>export default {}</code> 外面的代码，这里的代码有2个注意事项：</p>
<ol>
<li>影响应用性能。这部分代码在应用启动时执行，而不是页面加载。如果这里的代码写的太复杂，会影响应用启动速度，占用更多内存。</li>
<li>不跟随组件、页面关闭而回收。在外层的静态变量不会跟随页面关闭而回收。如果必要你需要手动处理。比如 <code>beforeDestroy</code> 或 <code>destroyed</code> 生命周期进行处理。</li>
</ol>
<h4 id="export"><a href="#export" class="headerlink" title="export"></a>export</h4><p>在ES6（<a href="https://www.bookstack.cn/read/es6-3rd/sidebar.md">ECMAScript 6</a>）中，export与export default均可用于导出变量(含常量)、函数、类、文件、模块等，然后在其它文件或模块中通过import 变量(含常量)|函数|类|文件|模块名的方式，将其导入，以便能够对其进行使用。export可以有多个，然而export default只能有一个。</p>
<h4 id="install"><a href="#install" class="headerlink" title="install"></a>install</h4><p>在 Vue.js 中，使用 <code>const install = (Vue, vm) =&gt; { ... }</code> 和 <code>export default { install }</code> 是一种常见的模式来定义和导出 Vue 插件。这种方法允许创建一个可以在 Vue 应用程序中使用的插件。</p>
<p><strong><code>install</code> 函数</strong>通常接收两个参数：</p>
<ul>
<li><code>Vue</code>:Vue 构造函数，可以用来扩展 Vue 的原型、注册全局组件、指令等。</li>
<li><code>vm</code>:可选参数，通常用于访问 Vue 实例的上下文，但在 Vue 3 中已被废弃。</li>
</ul>
<p>在函数体中，可以执行各种操作来配置 Vue 或者注册自定义行为。当你在其他地方导入(比如<code>main.js</code>)这个模块时，可以通过 <code>Vue.use()</code> 方法来安装这个插件。</p>
<h4 id="prototype"><a href="#prototype" class="headerlink" title="prototype"></a>prototype</h4><p>在 JavaScript 中，原型是一种特殊的机制，用于实现对象之间的继承和共享属性及方法。每个 JavaScript 对象都有一个内部属性 <code>[[[Prototype]]]</code>，它指向另一个对象，这个对象被称为<strong>原型对象</strong>。当试图访问一个对象的属性或方法时，如果该对象本身没有这个属性或方法，JavaScript 引擎会<strong>沿着原型链向上查找，直到找到这个属性或方法为止</strong>。Java 作为一种面向对象的编程语言，也有一种类似于 JavaScript 中原型机制的概念，但它采用的是类继承和接口实现的方式。Java 中并没有直接与 JavaScript 的 <code>prototype</code> 相对应的概念，但我们可以探讨 Java 中的一些机制，它们在某种程度上实现了类似的功能。</p>
<p><code>prototype</code> 是一个特殊的属性，它存在于所有函数对象上。当创建一个函数时，这个函数会自动获得一个 <code>prototype</code> 属性。这个 <code>prototype</code> 属性是一个对象，它的用途是作为新创建对象的原型。当你使用构造函数创建对象时，新创建的对象会继承 <code>prototype</code> 对象上的属性和方法。</p>
<p>在 Vue.js 中，可以利用 JavaScript 的原型机制来<strong>扩展 Vue 的原型，使得所有的 Vue 实例可以共享某些属性和方法</strong>。这通常通过将属性或方法添加到 <code>Vue.prototype</code> 来实现。</p>
<pre><code class="language-javascript">Vue.prototype.$api = {
    // 导入模块
    common,
    scene,
    group,
    deviceUser,
    deviceLog,
    device,
    deviceJob,
    account,
    energyConsumptionAnalysis
};
</code></pre>
<p>上述代码，向 Vue 的原型上添加了一个名为 <code>$api</code> 的属性，并将自定义的模块加入之中。之后，可以在Vue 的任何实例中都可以通过 <code>$api</code> 访问这些方法。</p>
<pre><code class="language-javascript"> this.$api.common.xxx()   // 访问自定的方法
</code></pre>
<h4 id="Props"><a href="#Props" class="headerlink" title="Props"></a>Props</h4><p><a href="https://uniapp.dcloud.net.cn/tutorial/vue-components.html#props">uni-app官网 (dcloud.net.cn)</a></p>
<p><a href="https://cn.vuejs.org/guide/essentials/component-basics#passing-props">组件基础 | Vue.js (vuejs.org)</a></p>
<p><code>props</code> 可以是数组或对象，用于接收来自父组件的数据。Props 可以在子组件上声明注册,在父组件定义其中声明参数的值，父级 prop 的更新会向下流动到子组件中，但不能反向。<code>props</code> 可以是简单的数组，或者使用对象作为替代，对象允许配置高级选项，如类型检测、自定义验证和设置默认值。</p>
<h4 id="ref"><a href="#ref" class="headerlink" title="ref"></a>ref</h4><p><a href="https://uniapp.dcloud.net.cn/tutorial/vue-components.html#ref">uni-app官网 (dcloud.net.cn)</a></p>
<p>用来<strong>给元素或子组件注册引用信息</strong>，引用信息将会注册在父组件的 <code>$refs</code> 对象上。</p>
<pre><code class="language-html">&lt;!-- index 父组件页面 --&gt;
&lt;template&gt;
	&lt;view&gt;
		&lt;base-input ref=&quot;usernameInput&quot;&gt;&lt;/base-input&gt;
		&lt;button type=&quot;default&quot; @click=&quot;getFocus&quot;&gt;获取焦点&lt;/button&gt; 
	&lt;/view&gt;
&lt;/template&gt;
&lt;script&gt;
	export default {
		methods:{
			getFocus(){
				//通过组件定义的ref调用focus方法
				this.$refs.usernameInput.focus()
			}
		}
	}
&lt;/script&gt;
</code></pre>
<h4 id="vuex"><a href="#vuex" class="headerlink" title="vuex"></a>vuex</h4><p>Vuex 是一个专为 Vue.js 应用程序开发的状态管理模式。它采用集中式存储管理应用的所有组件的状态，并以相应的规则保证状态以一种可预测的方式发生变化。</p>
<p><strong>Vuex与全局变量区别</strong></p>
<table>
<thead>
<tr>
<th>vuex</th>
<th>全局变量</th>
</tr>
</thead>
<tbody><tr>
<td>不能直接改变store里面的变量，由统一的方法修改数据</td>
<td>可以任意修改</td>
</tr>
<tr>
<td>每个组件可以根据自己vuex的变量名引用不受影响</td>
<td>全局变量可能操作命名污染</td>
</tr>
<tr>
<td>解决了多组件之间通信的问题</td>
<td>跨页面数据共享</td>
</tr>
<tr>
<td>适用于多模块、业务关系复杂的中大型项目</td>
<td>适用于demo或者小型项目</td>
</tr>
</tbody></table>
<p>每一个 <code>Vuex</code> 应用的核心就是 <code>store</code>（仓库），它包含着你的应用中大部分的状态 (<code>state</code>)。</p>
<p>状态管理有5个核心：<code>state</code>、<code>getter</code>、<code>mutation</code>、<code>action</code>、<code>module</code>。</p>
]]></description>
    </item>
    <item>
      <title>MQTT3.1.1协议解析</title>
      <link>https://raindrop2508.github.io/2026/02/26/tech/MQTT3.1.1%E5%8D%8F%E8%AE%AE%E8%A7%A3%E6%9E%90/</link>
      <guid>https://raindrop2508.github.io/2026/02/26/tech/MQTT3.1.1%E5%8D%8F%E8%AE%AE%E8%A7%A3%E6%9E%90/</guid>
      <pubDate>2 月 26 日，2026</pubDate>
      <description><![CDATA[<p><a href="https://www.yuque.com/attachments/yuque/0/2025/pdf/40667138/1760532241226-0b0592ba-9e07-4972-891f-b23c52e2e618.pdf">mqtt-v3.1.1-errata01-os-complete.pdf</a></p>
<p><a href="https://mcxiaoke.gitbooks.io/mqtt-cn/content/mqtt/01-Introduction.html">第一章 - MQTT介绍 · MQTT协议中文版</a></p>
<p><strong><font style="color:rgb(51, 51, 51);">应用消息 Application Message</font></strong><font style="color:rgb(51, 51, 51);"> MQTT协议通过网络传输应用数据。应用消息通过MQTT传输时，它们有关联的服务质量（QoS）和主题（Topic）。</font></p>
<p><strong><font style="color:rgb(51, 51, 51);">客户端 Client：</font></strong><font style="color:rgb(51, 51, 51);">发布消息，订阅请求。</font></p>
<p><strong><font style="color:rgb(51, 51, 51);">服务器 Server：</font></strong><font style="color:rgb(51, 51, 51);">接收、发布消息，处理订阅。</font></p>
<p><font style="color:rgb(51, 51, 51);">订阅包含一个主题过滤器（Topic Filter）和一个最大的服务质量（QoS）等级。</font></p>
<h1 id="控制报文格式"><a href="#控制报文格式" class="headerlink" title="控制报文格式"></a><font style="color:rgb(51, 51, 51);">控制报文格式</font></h1><p>MQTT 协议通过交换预定义的 MQTT 控制报文来通信。</p>
<p><strong>控制报文主要包括：固定报头、可变报头（部分包含）和有效载荷（部分包含）。</strong></p>
<h3 id="控制报文类型"><a href="#控制报文类型" class="headerlink" title="控制报文类型"></a>控制报文类型</h3><table>
<thead>
<tr>
<th><strong><font style="color:rgb(51, 51, 51);">名字</font></strong></th>
<th><strong><font style="color:rgb(51, 51, 51);">值</font></strong></th>
<th><strong><font style="color:rgb(51, 51, 51);">报文流动方向</font></strong></th>
<th><strong><font style="color:rgb(51, 51, 51);">描述</font></strong></th>
</tr>
</thead>
<tbody><tr>
<td><font style="color:rgb(51, 51, 51);">Reserved</font></td>
<td><font style="color:rgb(51, 51, 51);">0</font></td>
<td><font style="color:rgb(51, 51, 51);">禁止</font></td>
<td><font style="color:rgb(51, 51, 51);">保留</font></td>
</tr>
<tr>
<td><font style="color:rgb(51, 51, 51);">CONNECT</font></td>
<td><font style="color:rgb(51, 51, 51);">1</font></td>
<td><font style="color:rgb(51, 51, 51);">客户端到服务端</font></td>
<td><font style="color:rgb(51, 51, 51);">客户端请求连接服务端</font></td>
</tr>
<tr>
<td><font style="color:rgb(51, 51, 51);">CONNACK</font></td>
<td><font style="color:rgb(51, 51, 51);">2</font></td>
<td><font style="color:rgb(51, 51, 51);">服务端到客户端</font></td>
<td><font style="color:rgb(51, 51, 51);">连接报文确认</font></td>
</tr>
<tr>
<td><font style="color:rgb(51, 51, 51);">PUBLISH</font></td>
<td><font style="color:rgb(51, 51, 51);">3</font></td>
<td><font style="color:rgb(51, 51, 51);">两个方向都允许</font></td>
<td><font style="color:rgb(51, 51, 51);">发布消息</font></td>
</tr>
<tr>
<td><font style="color:rgb(51, 51, 51);">PUBACK</font></td>
<td><font style="color:rgb(51, 51, 51);">4</font></td>
<td><font style="color:rgb(51, 51, 51);">两个方向都允许</font></td>
<td><font style="color:rgb(51, 51, 51);">QoS 1消息发布收到确认</font></td>
</tr>
<tr>
<td><font style="color:rgb(51, 51, 51);">PUBREC</font></td>
<td><font style="color:rgb(51, 51, 51);">5</font></td>
<td><font style="color:rgb(51, 51, 51);">两个方向都允许</font></td>
<td><font style="color:rgb(51, 51, 51);">发布收到（保证交付第一步）</font></td>
</tr>
<tr>
<td><font style="color:rgb(51, 51, 51);">PUBREL</font></td>
<td><font style="color:rgb(51, 51, 51);">6</font></td>
<td><font style="color:rgb(51, 51, 51);">两个方向都允许</font></td>
<td><font style="color:rgb(51, 51, 51);">发布释放（保证交付第二步）</font></td>
</tr>
<tr>
<td><font style="color:rgb(51, 51, 51);">PUBCOMP</font></td>
<td><font style="color:rgb(51, 51, 51);">7</font></td>
<td><font style="color:rgb(51, 51, 51);">两个方向都允许</font></td>
<td><font style="color:rgb(51, 51, 51);">QoS 2消息发布完成（保证交互第三步）</font></td>
</tr>
<tr>
<td><font style="color:rgb(51, 51, 51);">SUBSCRIBE</font></td>
<td><font style="color:rgb(51, 51, 51);">8</font></td>
<td><font style="color:rgb(51, 51, 51);">客户端到服务端</font></td>
<td><font style="color:rgb(51, 51, 51);">客户端订阅请求</font></td>
</tr>
<tr>
<td><font style="color:rgb(51, 51, 51);">SUBACK</font></td>
<td><font style="color:rgb(51, 51, 51);">9</font></td>
<td><font style="color:rgb(51, 51, 51);">服务端到客户端</font></td>
<td><font style="color:rgb(51, 51, 51);">订阅请求报文确认</font></td>
</tr>
<tr>
<td><font style="color:rgb(51, 51, 51);">UNSUBSCRIBE</font></td>
<td><font style="color:rgb(51, 51, 51);">10</font></td>
<td><font style="color:rgb(51, 51, 51);">客户端到服务端</font></td>
<td><font style="color:rgb(51, 51, 51);">客户端取消订阅请求</font></td>
</tr>
<tr>
<td><font style="color:rgb(51, 51, 51);">UNSUBACK</font></td>
<td><font style="color:rgb(51, 51, 51);">11</font></td>
<td><font style="color:rgb(51, 51, 51);">服务端到客户端</font></td>
<td><font style="color:rgb(51, 51, 51);">取消订阅报文确认</font></td>
</tr>
<tr>
<td><font style="color:rgb(51, 51, 51);">PINGREQ</font></td>
<td><font style="color:rgb(51, 51, 51);">12</font></td>
<td><font style="color:rgb(51, 51, 51);">客户端到服务端</font></td>
<td><font style="color:rgb(51, 51, 51);">心跳请求</font></td>
</tr>
<tr>
<td><font style="color:rgb(51, 51, 51);">PINGRESP</font></td>
<td><font style="color:rgb(51, 51, 51);">13</font></td>
<td><font style="color:rgb(51, 51, 51);">服务端到客户端</font></td>
<td><font style="color:rgb(51, 51, 51);">心跳响应</font></td>
</tr>
<tr>
<td><font style="color:rgb(51, 51, 51);">DISCONNECT</font></td>
<td><font style="color:rgb(51, 51, 51);">14</font></td>
<td><font style="color:rgb(51, 51, 51);">客户端到服务端</font></td>
<td><font style="color:rgb(51, 51, 51);">客户端断开连接</font></td>
</tr>
<tr>
<td><font style="color:rgb(51, 51, 51);">Reserved</font></td>
<td><font style="color:rgb(51, 51, 51);">15</font></td>
<td><font style="color:rgb(51, 51, 51);">禁止</font></td>
<td><font style="color:rgb(51, 51, 51);">保留</font></td>
</tr>
</tbody></table>
<h2 id="固定报头"><a href="#固定报头" class="headerlink" title="固定报头"></a>固定报头</h2><p>固定报头为两个字节。</p>
<ul>
<li>第一个字节：<strong>高四位为控制报文类型，而低四位为对应控制报文类型的标志位</strong></li>
<li>第二个字节（剩余长度）：<font style="color:rgb(0, 0, 0);">表示当前报文剩余部分的字节数，包括可变报头和负载的数据。剩余长度不包括用于编码剩余长度字段本身的字节数。</font></li>
</ul>
<h2 id="可变报头"><a href="#可变报头" class="headerlink" title="可变报头"></a>可变报头</h2><p>位于固定报头与负载之间。<font style="color:rgb(51, 51, 51);">很多控制报文的可变报头部分包含</font><font style="color:rgb(0, 0, 0);">一个两字节的报文标识符字段</font><font style="color:rgb(51, 51, 51);">。</font></p>
<h2 id="有效载荷"><a href="#有效载荷" class="headerlink" title="有效载荷"></a>有效载荷</h2><p>位于报文最后。</p>
<h1 id="MQTT-运行流程"><a href="#MQTT-运行流程" class="headerlink" title="MQTT 运行流程"></a>MQTT 运行流程</h1><h2 id="客户端连接"><a href="#客户端连接" class="headerlink" title="客户端连接"></a>客户端连接</h2><p><strong>客户端 向 服务端 发送 connect 报文</strong>    </p>
<p>包含 客户端标识符（<strong>必须</strong>）、用户名、密码、协议级别以及遗嘱信息等</p>
<p><strong>服务端 向 客户端 发送 CONNACK 确认连接请求</strong></p>
<h2 id="订阅主题"><a href="#订阅主题" class="headerlink" title="订阅主题"></a>订阅主题</h2><p><font style="color:rgb(38, 38, 38);">客户端向服务端发送</font><strong><font style="color:rgb(38, 38, 38);"> SUBSCRIBE </font></strong><font style="color:rgb(38, 38, 38);">报文用于创建</font><strong><font style="color:rgb(38, 38, 38);">一个或多个订阅（支持通配符 ）</font></strong><font style="color:rgb(38, 38, 38);">。SUBSCRIBE 报文也</font><strong><font style="color:rgb(38, 38, 38);">（为每个订阅）指定了最大的QoS等级</font></strong><font style="color:rgb(38, 38, 38);">，服务端根据这个发送应用消息给客户端。 </font></p>
<p><font style="color:rgb(38, 38, 38);">SUBACK 用来进行订阅 确认。</font><code>&lt;font style=&quot;color:rgb(0, 0, 0);&quot;&gt;SUBACK&lt;/font&gt;</code><font style="color:rgb(0, 0, 0);">报文会为 </font><code>&lt;font style=&quot;color:rgb(0, 0, 0);&quot;&gt;SUBSCRIBE&lt;/font&gt;</code><font style="color:rgb(0, 0, 0);">报文中的</font><strong><font style="color:rgb(0, 0, 0);">每个主题过滤器返回一个授予的QoS等级</font></strong><font style="color:rgb(0, 0, 0);">（这个等级可能等于或小于客户端请求的最大QoS，但不会高于它）。</font></p>
<h2 id="PUBLISH-发送消息"><a href="#PUBLISH-发送消息" class="headerlink" title="PUBLISH 发送消息"></a>PUBLISH 发送消息</h2><p>客户端使用PUBLISH报文发送应用消息给服务端，目的是<strong>分发到其它订阅匹配的客户端</strong>。 </p>
<p> 服务端使用PUBLISH报文发送应用消息给<strong>每一个订阅匹配的客户端</strong>。 </p>
<p>可变报头中包含了 主题名 和 报文标识符，有效载荷中包含将要被发布的消息。</p>
<ul>
<li>QOS 为 0 时，无确认流程。</li>
<li>当 QOS 为 1 时，需要通过 PUBACK 进行发布确认</li>
<li>当 QOS 为 2 时，首先是通过 PUBREC 进行发布收到确认，之后使用 PUBREL 进行发布释放确认，最后使用 PUBCOMP 进行发布完成确认</li>
</ul>
<h2 id="取消订阅"><a href="#取消订阅" class="headerlink" title="取消订阅"></a>取消订阅</h2><p>使用 UNSUBSCRIBE 取消订阅</p>
<p><font style="color:rgb(38, 38, 38);">使用 UNSUBACK  确认收到UNSUBSCRIBE报文。</font></p>
<h1 id="断开连接"><a href="#断开连接" class="headerlink" title="断开连接"></a><font style="color:rgb(38, 38, 38);">断开连接</font></h1><p><font style="color:rgb(38, 38, 38);"> 客户端发送DISCONNECT报文，断开与服务端连接。</font></p>
<h1 id="控制报文"><a href="#控制报文" class="headerlink" title="控制报文"></a>控制报文</h1><h2 id="CONNECT-–-连接服务端"><a href="#CONNECT-–-连接服务端" class="headerlink" title="CONNECT – 连接服务端"></a><font style="color:rgb(51, 51, 51);">CONNECT – 连接服务端</font></h2><ul>
<li><font style="color:rgb(0, 0, 0);">客户端到服务端的网络连接建立后，客户端发送给服务端的第一个报文</font><strong><font style="color:rgb(0, 0, 0);">必须</font></strong><font style="color:rgb(0, 0, 0);">是CONNECT报文</font></li>
<li><font style="color:rgb(0, 0, 0);">在一个网络连接上，客户端只能发送一次CONNECT报文。</font></li>
<li><font style="color:rgb(0, 0, 0);">有效载荷包含一个或多个编码的字段。包括客户端的唯一标识符，Will主题，Will消息，用户名和密码。</font></li>
</ul>
<h3 id="固定报头-1"><a href="#固定报头-1" class="headerlink" title="固定报头"></a>固定报头</h3><p>第一字节： 报文类型 0001  低四位作为保留位</p>
<p><font style="color:rgb(51, 51, 51);">剩余长度等于可变报头的长度（10字节）加上有效载荷的长度。</font></p>
<h3 id="可变报头-1"><a href="#可变报头-1" class="headerlink" title="可变报头"></a><font style="color:rgb(51, 51, 51);">可变报头</font></h3><p><font style="color:rgb(0, 0, 0);">CONNECT报文的可变报头按下列次序包含四个字段：协议名（Protocol Name），协议级别（Protocol Level），连接标志（Connect Flags）和保持连接（Keep Alive）。</font></p>
<h4 id="协议名：表示-MQTT-的编码"><a href="#协议名：表示-MQTT-的编码" class="headerlink" title="协议名：表示 MQTT 的编码"></a>协议名：表示 MQTT 的编码</h4><h4 id="协议级别：表示协议版本"><a href="#协议级别：表示协议版本" class="headerlink" title="协议级别：表示协议版本"></a>协议级别：表示协议版本</h4><h4 id="连接标志-Connect-Flags"><a href="#连接标志-Connect-Flags" class="headerlink" title="连接标志 Connect Flags"></a><font style="color:rgb(51, 51, 51);">连接标志 Connect Flags</font></h4><p><img loading="lazy" src="https://i-blog.csdnimg.cn/img_convert/4313d4d9d3d79e469fadef4a2d8633d8.png"></p>
<ul>
<li><strong><font style="color:rgb(51, 51, 51);">清理会话 Clean Session</font></strong><font style="color:rgb(51, 51, 51);">为</font></li>
</ul>
<p><font style="color:rgb(51, 51, 51);">为 0 时：</font><font style="color:rgb(51, 51, 51);">服务端必须基于当前会话（使用客户端标识符识别）的状态恢复与客户端的通信。</font></p>
<p><font style="color:rgb(51, 51, 51);">为 1 时：断开之后，旧的会话会被丢弃。</font></p>
<p><font style="color:rgb(0, 0, 0);">要确保不丢失连接断开期间的消息，需要使用QoS 1或 QoS 2级别，同时将清理会话标志设置为0。</font></p>
<ul>
<li>**遗嘱标志：**服务端是否需要发布遗嘱消息</li>
<li>**遗嘱 QOS：**遗嘱消息QOS等级</li>
<li>**遗嘱保留：**是否将遗嘱消息作为保留消息发布</li>
<li>**用户名标志：**有效载荷中是否包含用户名</li>
<li>**密码标志：**有效载荷中是否包含密码</li>
</ul>
<h4 id="保持连接"><a href="#保持连接" class="headerlink" title="保持连接"></a>保持连接</h4><p><font style="color:rgb(51, 51, 51);">保持连接（Keep Alive）是一个以秒为单位的时间间隔。</font></p>
<p><font style="color:rgb(0, 0, 0);">指在客户端传输完成一个控制报文的时刻到发送下一个报文的时刻，两者之间</font><strong><font style="color:rgb(0, 0, 0);">允许空闲的最大时间间隔</font></strong><font style="color:rgb(0, 0, 0);">。期间如果没有任何其它的控制报文可以发送，客户端</font><strong><font style="color:rgb(0, 0, 0);">必须</font></strong><font style="color:rgb(0, 0, 0);">发送一个PINGREQ报文</font></p>
<p><font style="color:rgb(0, 0, 0);">如果保持连接的值非零，并且服务端在一点五倍的保持连接时间内没有收到客户端的控制报文，它</font><strong><font style="color:rgb(0, 0, 0);">必须</font></strong><font style="color:rgb(0, 0, 0);">断开客户端的网络连接，认为网络连接已断开。</font></p>
<p><font style="color:rgb(0, 0, 0);">客户端发送了PINGREQ报文之后，如果在合理的时间内仍没有收到PINGRESP报文，它</font><strong><font style="color:rgb(0, 0, 0);">应该</font></strong><font style="color:rgb(0, 0, 0);">关闭到服务端的网络连接。</font></p>
<p><font style="color:rgb(0, 0, 0);">保持连接的值为零表示关闭保持连接功能。</font></p>
<h3 id="有效载荷-1"><a href="#有效载荷-1" class="headerlink" title="有效载荷"></a><font style="color:rgb(0, 0, 0);">有效载荷</font></h3><p><font style="color:rgb(0, 0, 0);">CONNECT报文的有效载荷（payload）包含一个或多个以长度为前缀的字段，可变报头中的标志决定是否包含这些字段。如果包含的话，</font><strong><font style="color:rgb(0, 0, 0);">必须</font></strong><font style="color:rgb(0, 0, 0);">按这个顺序出现：客户端标识符，遗嘱主题，遗嘱消息，用户名，密码。</font></p>
<h4 id="客户端标识符"><a href="#客户端标识符" class="headerlink" title="客户端标识符"></a><font style="color:rgb(0, 0, 0);">客户端标识符</font></h4><p><font style="color:rgb(51, 51, 51);">服务端使用客户端标识符 (ClientId) 识别客户端。连接服务端的</font><font style="color:rgb(0, 0, 0);">每个客户端都有</font><strong><font style="color:rgb(0, 0, 0);">唯一</font></strong><font style="color:rgb(0, 0, 0);">的客户端标识符</font><font style="color:rgb(51, 51, 51);">（ClientId）。</font><font style="color:rgb(0, 0, 0);">客户端和服务端都必须使用ClientId识别两者之间的MQTT会话相关的状态。</font></p>
<p><font style="color:rgb(0, 0, 0);">客户端标识符 (ClientId)</font><font style="color:rgb(51, 51, 51);"> </font><strong><font style="color:rgb(0, 0, 0);">必须</font></strong><font style="color:rgb(0, 0, 0);">存在而且</font><strong><font style="color:rgb(0, 0, 0);">必须</font></strong><font style="color:rgb(0, 0, 0);">是CONNECT报文有效载荷的</font><strong><font style="color:rgb(0, 0, 0);">第一个字段。</font></strong></p>
<h4 id="遗嘱主题"><a href="#遗嘱主题" class="headerlink" title="遗嘱主题"></a><font style="color:rgb(0, 0, 0);">遗嘱主题</font></h4><p> 如果遗嘱标志被设置为1，有效载荷中的下一个字段是遗嘱主题（Will Topic）。  之后是遗嘱消息，根据是否含有标志包括了用户名和密码。</p>
<h3 id="响应"><a href="#响应" class="headerlink" title="响应"></a>响应</h3><p>服务器可以在同一个TCP端口或其他网络端点上支持多种协议  。</p>
<p>服务端必须发送返回码为零的CONNACK报文作为CONNECT报文的确认响应 。但客户端可以在收到CONNACK之前发送控制报文 。</p>
<h2 id="CONNACK-–-确认连接请求"><a href="#CONNACK-–-确认连接请求" class="headerlink" title="CONNACK – 确认连接请求"></a>CONNACK – 确认连接请求</h2><p> 服务端发送CONNACK报文响应从客户端收到的CONNECT报文。服务端发送给客户端的第一个报文必 须是CONNACK 。</p>
<h3 id="固定报头-2"><a href="#固定报头-2" class="headerlink" title="固定报头"></a>固定报头</h3><p><img loading="lazy" src="https://i-blog.csdnimg.cn/img_convert/2bc0bfa8a6e3753ab723647c08889a07.png"></p>
<h3 id="可变报头-2"><a href="#可变报头-2" class="headerlink" title="可变报头"></a>可变报头</h3><h4 id="连接确认标志"><a href="#连接确认标志" class="headerlink" title="连接确认标志"></a>连接确认标志</h4><p> 第0 (SP)位 是当前会话（Session Present）标志。  其他（１－７）位应为０</p>
<p> 如果服务端收到清理会话（CleanSession）标志为1的连接，除了将CONNACK报文中的返回码设置为0 之外，还必须将CONNACK报文中的当前会话设置（Session Present）标志为0  </p>
<blockquote>
<p>当服务端收到一个CleanSession为0的连接请求时，需要根据是否已经保存了该客户端（通过ClientId识别）的会话信息来决定CONNACK响应中的当前会话标志。如果之前有保存过这个客户端的会话信息，那么在CONNACK里就把当前会话标志设为1；如果没有保存过，则设为0。另外，不管哪种情况，都要确保CONNACK里的返回码是0。</p>
</blockquote>
<h4 id="连接返回码"><a href="#连接返回码" class="headerlink" title="连接返回码"></a>连接返回码</h4><p>一个无符号值,0 表示正常连接;非 0 值则表明连接被拒绝</p>
<h3 id="无有效载荷"><a href="#无有效载荷" class="headerlink" title="无有效载荷"></a>无有效载荷</h3><h2 id="Publish-发布消息"><a href="#Publish-发布消息" class="headerlink" title="Publish-发布消息"></a>Publish-发布消息</h2><h3 id="固定报头-3"><a href="#固定报头-3" class="headerlink" title="固定报头"></a>固定报头</h3><p><img loading="lazy" src="https://i-blog.csdnimg.cn/img_convert/295f8236ae99fe546106c5ae92ad2b24.png"></p>
<h4 id="重发标志-DUP-0-第一次请求发送-1-表示重发报文"><a href="#重发标志-DUP-0-第一次请求发送-1-表示重发报文" class="headerlink" title="重发标志 DUP: 0 第一次请求发送,1 表示重发报文."></a>重发标志 DUP: 0 第一次请求发送,1 表示重发报文.</h4><p> 发送（出站）的PUBLISH 报文与收到（入站）的 PUBLISH报文中的 DUP标志是独立设置的，它的值必须 单独的根据发送（出站）的PUBLISH报文是否是一个重发来确定 .</p>
<h4 id="服务质量等级-QOS"><a href="#服务质量等级-QOS" class="headerlink" title="服务质量等级  QOS"></a>服务质量等级  QOS</h4><p><img loading="lazy" src="https://i-blog.csdnimg.cn/img_convert/500600c740dc29b9ef3e6788b3f531c1.png"></p>
<h4 id="保留标志-RETAIN"><a href="#保留标志-RETAIN" class="headerlink" title="保留标志  RETAIN"></a>保留标志  RETAIN</h4><p> 如果客户端发给服务端的 PUBLISH 报文的保留（RETAIN）标志被设置为 1，服务端必须存储这个应用消 息和它的服务质量等级（QoS），以便它可以被分发给未来的主题名匹配的订阅者 </p>
<p> 一个 新的订阅建立时，对每个匹配的主题名，如果存在最近保留的消息，它必须被发送给这个订阅者 </p>
<p> 如果服务端收到一条保留（RETAIN）标志为 1的 QoS 0消息，它必须丢弃之前为那个主题保留 的任何消息。它应该将这个新的 QoS 0消息当作那个主题的新保留消息，但是任何时候都可以选择丢弃它 — 如果这种情况发生了，那个主题将没有保留消息</p>
<p> 服务端发送<strong>PUBLISH报文给客户端时</strong>，如果消息是作为客户端一个<strong>新订阅的结果</strong>发送，它必须将**报文的保 留标志设为1    **</p>
<h4 id="剩余长度字段"><a href="#剩余长度字段" class="headerlink" title="剩余长度字段"></a>剩余长度字段</h4><p> 可变报头的长度加上有效载荷的长度  </p>
<h3 id="可变报头-3"><a href="#可变报头-3" class="headerlink" title="可变报头"></a>可变报头</h3><p> 包含主题名和报文标识符。  </p>
<h4 id="主题名"><a href="#主题名" class="headerlink" title="主题名"></a>主题名</h4><p> 可变报头的第一个字段  </p>
<p> 主题名不能包含通配符  </p>
<h4 id="报文标识符"><a href="#报文标识符" class="headerlink" title="报文标识符"></a>报文标识符</h4><p>QOS 为 1 或 2 时存在</p>
<h3 id="有效载荷-2"><a href="#有效载荷-2" class="headerlink" title="有效载荷"></a>有效载荷</h3><p> 有效载荷包含将被发布的应用消息。数据的内容和格式是应用特定的。</p>
<h3 id="响应-1"><a href="#响应-1" class="headerlink" title="响应"></a>响应</h3><p>接收者与发布者的 QOS 等级应相同</p>
<h3 id="动作"><a href="#动作" class="headerlink" title="动作"></a>动作</h3><p> 客户端使用PUBLISH报文发送应用消息给服务端，目的是<strong>分发到其它订阅匹配的客户端</strong>。  </p>
<p> 服务端使用PUBLISH报文发送应用消息给<strong>每一个订阅匹配的客户端</strong>。  </p>
<p> 客户端使用带通配符的主题过滤器请求订阅时，客户端的订阅可能会<strong>重复</strong>，因此发布的消息可能会匹配多 个过滤器。对于这种情况，服务端必须将消息分发给<strong>所有订阅匹配的QoS等级最高的客户端</strong> .</p>
<p> 服务端之后可以按照订阅的QoS等级，分发消息的副本给每一个匹配的订阅者。  </p>
<h2 id="PUBACK-–发布确认"><a href="#PUBACK-–发布确认" class="headerlink" title="PUBACK –发布确认"></a>PUBACK –发布确认</h2><p> QoS 1等级的PUBLISH报文的响应。  </p>
<h3 id="固定报头-4"><a href="#固定报头-4" class="headerlink" title="固定报头"></a>固定报头</h3><p>MQTT 报文控制类型为 4</p>
<p>剩余长度字段为   2</p>
<h3 id="可变报头-4"><a href="#可变报头-4" class="headerlink" title="可变报头"></a>可变报头</h3><p> 包含等待确认的PUBLISH报文的报文标识符。  </p>
<h3 id="无有效载荷-1"><a href="#无有效载荷-1" class="headerlink" title="无有效载荷"></a>无有效载荷</h3><h2 id="PUBREC-–-发布收到（QoS-2，第一步）"><a href="#PUBREC-–-发布收到（QoS-2，第一步）" class="headerlink" title="PUBREC – 发布收到（QoS 2，第一步）"></a>PUBREC – 发布收到（QoS 2，第一步）</h2><p> QoS等级2的PUBLISH报文的响应。   它是QoS 2等级协议交换的第二个报文。  </p>
<p><strong><font style="color:rgb(38, 38, 38);">固定报头</font></strong><font style="color:rgb(38, 38, 38);"><br></font><font style="color:rgb(38, 38, 38);">MQTT 报文控制类型为 5<br></font><font style="color:rgb(38, 38, 38);">剩余长度字段为 2</font></p>
<h3 id="可变报头-5"><a href="#可变报头-5" class="headerlink" title="可变报头"></a><font style="color:rgb(38, 38, 38);">可变报头</font></h3><p><font style="color:rgb(38, 38, 38);">包含等待确认的PUBLISH报文的报文标识符。</font></p>
<p><strong><font style="color:rgb(38, 38, 38);">无有效载荷</font></strong></p>
<h2 id="PUBREL-–-发布释放（QoS-2，第二步）"><a href="#PUBREL-–-发布释放（QoS-2，第二步）" class="headerlink" title="PUBREL – 发布释放（QoS 2，第二步）  "></a><font style="color:rgb(38, 38, 38);">PUBREL – 发布释放（QoS 2，第二步）  </font></h2><p> 对PUBREC报文的响应。它是QoS 2等级协议交换的第三个报文。  </p>
<h3 id="固定报头-5"><a href="#固定报头-5" class="headerlink" title="固定报头"></a>固定报头</h3><p><font style="color:rgb(38, 38, 38);">MQTT 报文控制类型为 5</font></p>
<p><font style="color:rgb(38, 38, 38);">剩余长度字段为 2</font></p>
<h3 id="可变报头-6"><a href="#可变报头-6" class="headerlink" title="可变报头"></a><font style="color:rgb(38, 38, 38);">可变报头</font></h3><p> 与等待确认的PUBREC报文相同的报文标识符。  </p>
<p><strong><font style="color:rgb(38, 38, 38);">无有效载荷</font></strong></p>
<h2 id="PUBCOMP-–-发布完成（QoS-2，第三步）"><a href="#PUBCOMP-–-发布完成（QoS-2，第三步）" class="headerlink" title="PUBCOMP – 发布完成（QoS 2，第三步）  "></a><font style="color:rgb(38, 38, 38);">PUBCOMP – 发布完成（QoS 2，第三步）  </font></h2><p> 对PUBREL报文的响应。它是QoS 2等级协议交换的第四个也是最后一个报文。  </p>
<p><strong><font style="color:rgb(38, 38, 38);">固定报头</font></strong><font style="color:rgb(38, 38, 38);"><br></font><font style="color:rgb(38, 38, 38);">MQTT 报文控制类型为 7<br></font><font style="color:rgb(38, 38, 38);">剩余长度字段为 2<br></font><strong><font style="color:rgb(38, 38, 38);">可变报头</font></strong></p>
<p><font style="color:rgb(38, 38, 38);"> 与等待确认的PUBREC报文相同的报文标识符。<br></font><strong><font style="color:rgb(38, 38, 38);">无有效载荷</font></strong></p>
<h2 id="SUBSCRIBE-订阅主题"><a href="#SUBSCRIBE-订阅主题" class="headerlink" title=" SUBSCRIBE - 订阅主题  "></a><font style="color:rgb(38, 38, 38);"> SUBSCRIBE - 订阅主题  </font></h2><p> 客户端向服务端发送SUBSCRIBE报文用于创建<strong>一个或多个订阅</strong>。SUBSCRIBE 报文也**（为每个订阅）指定了最大的QoS等级**，服务端根据这个发送应用消息给客户端。  </p>
<p><strong><font style="color:rgb(38, 38, 38);">固定报头</font></strong><font style="color:rgb(38, 38, 38);"><br></font><font style="color:rgb(38, 38, 38);">MQTT 报文控制类型为 5<br></font><font style="color:rgb(38, 38, 38);">剩余长度字段为 可变报头 + 有效载荷 的长度</font></p>
<h3 id="可变报头-7"><a href="#可变报头-7" class="headerlink" title="可变报头"></a><font style="color:rgb(38, 38, 38);">可变报头</font></h3><p> 可变报头包含客户端标识符。  </p>
<h3 id="有效载荷-3"><a href="#有效载荷-3" class="headerlink" title="有效载荷"></a>有效载荷</h3><p> **主题过滤器 和 QoS等级字段组合 ** </p>
<p> 表示客户端想要订阅的主题。  <strong>服务端</strong>应该**支持 包含通配符（4.7.1节定义的）的主题过滤器。**如果服务端选择不支持包含通配符的主题过滤器，必须拒绝 任何包含通配符过滤器的订阅请求  。</p>
<p> 每一个过滤器后面跟着一个字节，这个字节被叫做 服 务质量要求（Requested QoS）。它给出了服务端向客户端发送应用消息所允许的最大QoS等级。 有效载荷必须包含至少一对主题过滤器 和 QoS等级字段组合。</p>
<p>没有有效载荷的 SUBSCRIBE报文是违反协议的  。  </p>
<h3 id="响应-2"><a href="#响应-2" class="headerlink" title="响应"></a>响应</h3><p> 服务端收到客户端发送的一个SUBSCRIBE报文时，必须使用SUBACK报文响应  , SUBACK报文必须和等待确认的SUBSCRIBE报文有相同的报文标识符 </p>
<p> 新订阅的主题过滤器和之前订阅的相同，但是它的最大QoS值可以不 同。与这个主题过滤器匹配的任何现存的保留消息必须被重发，但是发布流程不能中断 。</p>
<p> 如果服务端收到包含多个主题过滤器的SUBSCRIBE报文，它必须如同收到了一系列的多个SUBSCRIBE 报文一样处理那个，除了需要将它们的响应合并到一个单独的SUBACK报文发送     SUBACK  <strong>响应可以合并</strong></p>
<p> SUBACK报文对每一对主题过滤器 和QoS等级都必须包含一个返回码。  </p>
<p> 服务端可以授予比 订阅者要求的低一些的QoS等级。为响应订阅而发出的消息的有效载荷的QoS必须是原始发布消息的 QoS和服务端授予的QoS两者中的最小值。  </p>
<p>** 确定消息分发时可能的最大QoS等级是发布者的责任，而订阅者可以 要求服务端降低QoS到更适合它的等级。  **</p>
<h2 id="SUBACK-–-订阅确认"><a href="#SUBACK-–-订阅确认" class="headerlink" title="SUBACK – 订阅确认"></a>SUBACK – 订阅确认</h2><p> 用于确认它已收到并且正在处理SUBSCRIBE报文。   包含一个返回码清单，它们指定了SUBSCRIBE请求的每个订阅被授予的最大QoS等级。  </p>
<p><strong><font style="color:rgb(38, 38, 38);">固定报头</font></strong></p>
<p><font style="color:rgb(38, 38, 38);">MQTT 报文控制类型为 5 </font></p>
<p><font style="color:rgb(38, 38, 38);">剩余长度字段为 可变报头 + 有效载荷 的长度</font></p>
<h3 id="可变报头-8"><a href="#可变报头-8" class="headerlink" title="可变报头"></a><font style="color:rgb(38, 38, 38);">可变报头</font></h3><p> 包含等待确认的SUBSCRIBE报文的报文标识符。  </p>
<h3 id="有效载荷-4"><a href="#有效载荷-4" class="headerlink" title="有效载荷"></a>有效载荷</h3><p> 包含一个返回码清单。每个返回码对应等待确认的SUBSCRIBE报文中的一个主题过滤器。返回 码的顺序必须和SUBSCRIBE报文中主题过滤器的顺序相同  </p>
<p> 允许的返回码值： </p>
<p>0x00 - 最大QoS 0 </p>
<p>0x01 - 成功 – 最大QoS 1 </p>
<p>0x02 - 成功 – 最大 QoS 2 </p>
<p>0x80 - Failure 失败  </p>
<h2 id="UNSUBSCRIBE-–取消订阅"><a href="#UNSUBSCRIBE-–取消订阅" class="headerlink" title="UNSUBSCRIBE –取消订阅"></a>UNSUBSCRIBE –取消订阅</h2><p>用于取消订阅主题</p>
<h3 id="固定报头-6"><a href="#固定报头-6" class="headerlink" title="固定报头"></a>固定报头</h3><p>控制报文类型 10</p>
<p>保留位  0，0，1，0</p>
<p>**剩余长度字段     **<font style="color:rgb(38, 38, 38);">可变报头 + 有效载荷 的长度</font></p>
<h3 id="可变报头-9"><a href="#可变报头-9" class="headerlink" title="可变报头"></a><font style="color:rgb(38, 38, 38);">可变报头</font></h3><h3 id="有效载荷-5"><a href="#有效载荷-5" class="headerlink" title="有效载荷"></a>有效载荷</h3><p>包含客户端想要取消订阅的主体过考虑列表。</p>
<h3 id="Response-响应"><a href="#Response-响应" class="headerlink" title="Response 响应"></a>Response 响应</h3><p> 如果服务端删除了一个订阅：  </p>
<ul>
<li>停止分发任何新消息给这个客户端  </li>
<li>完成分发任何已经开始往客户端发送的QoS 1和QoS 2的消息  </li>
<li>继续发送任何现存的准备分发给客户端的缓存消息。</li>
</ul>
<p> 服务端必须发送UNSUBACK报文响应客户端的UNSUBSCRIBE请求。  </p>
<h2 id="UNSUBACK-–-取消订阅确认"><a href="#UNSUBACK-–-取消订阅确认" class="headerlink" title="UNSUBACK – 取消订阅确认"></a>UNSUBACK – 取消订阅确认</h2><p> 用于确认收到UNSUBSCRIBE报文。  </p>
<h3 id="固定报头-7"><a href="#固定报头-7" class="headerlink" title="固定报头"></a>固定报头</h3><p>控制报文类型 11 </p>
<p>剩余长度 2</p>
<h3 id="可变报头-10"><a href="#可变报头-10" class="headerlink" title="可变报头"></a>可变报头</h3><h3 id="有效载荷-6"><a href="#有效载荷-6" class="headerlink" title="有效载荷"></a>有效载荷</h3><p>无有效载荷</p>
<h2 id="PINGREQ-–-心跳请求"><a href="#PINGREQ-–-心跳请求" class="headerlink" title="PINGREQ – 心跳请求"></a>PINGREQ – 心跳请求</h2><ul>
<li>告知服务端客户端还活着。  </li>
<li>请求服务端发送 响应确认它还活着。  </li>
<li>使用网络以确认网络连接没有断开。</li>
</ul>
<h3 id="固定报头-8"><a href="#固定报头-8" class="headerlink" title="固定报头"></a>固定报头</h3><p>控制报文类型  12</p>
<p>剩余长度  0</p>
<p>无 可变报头，有效载荷</p>
<h3 id="响应-3"><a href="#响应-3" class="headerlink" title="响应"></a>响应</h3><p> 服务端必须发送 PINGRESP报文响应客户端的PINGREQ报文  </p>
<h2 id="PINGRESP-–-心跳响应"><a href="#PINGRESP-–-心跳响应" class="headerlink" title="PINGRESP – 心跳响应"></a>PINGRESP – 心跳响应</h2><p> 发送PINGRESP报文响应客户端的PINGREQ报文。表示服务端还活着。  </p>
<h3 id="固定报头-9"><a href="#固定报头-9" class="headerlink" title="固定报头"></a>固定报头</h3><p>控制报文类型 13</p>
<p><font style="color:rgb(38, 38, 38);">剩余长度 0<br></font><font style="color:rgb(38, 38, 38);">无 可变报头，有效载荷</font></p>
<h2 id="DISCONNECT-–断开连接"><a href="#DISCONNECT-–断开连接" class="headerlink" title=" DISCONNECT –断开连接  "></a><font style="color:rgb(38, 38, 38);"> DISCONNECT –断开连接  </font></h2><p>控制报文类型 14</p>
<p>保留位 0</p>
<p><font style="color:rgb(38, 38, 38);">剩余长度 0 无 可变报头，有效载荷</font></p>
<h3 id="Response"><a href="#Response" class="headerlink" title=" Response  "></a><font style="color:rgb(38, 38, 38);"> Response  </font></h3><p> 客户端发送DISCONNECT报文之后：  </p>
<ul>
<li>必须关闭网络连接  </li>
<li>不能通过那个网络连接再发送任何控制报文</li>
</ul>
<p>服务端收到 到DISCONNECT报文时：  </p>
<ul>
<li>必须丢弃任何与当前连接关联的未发布的遗嘱消息  </li>
<li>应该关闭网络连接，如果客户端 还没有这么做。</li>
</ul>
<h1 id="操作行为"><a href="#操作行为" class="headerlink" title="操作行为"></a>操作行为</h1><p> MQTT 3.1使用的传输层协议是 [RFC793] 定义的TCP&#x2F;IP协议。同事也支持 TLS、   WebSocket协议  </p>
<h2 id="服务质量等级和协议流程"><a href="#服务质量等级和协议流程" class="headerlink" title="服务质量等级和协议流程"></a>服务质量等级和协议流程</h2><h3 id="QoS-0-最多分发一次"><a href="#QoS-0-最多分发一次" class="headerlink" title="QoS 0:最多分发一次"></a>QoS 0:最多分发一次</h3><p> 消息的分发依赖于底层网络的能力。接收者不会发送响应，发送者也不会重试。  </p>
<h3 id="QoS-1-至少分发一次"><a href="#QoS-1-至少分发一次" class="headerlink" title="QoS 1: 至少分发一次"></a>QoS 1: 至少分发一次</h3><p> QoS 1 的 PUBLISH 报文的可变报头中包含一个报文标识符，需要 PUBACK报文确认。  </p>
<p>发送者：</p>
<ul>
<li><p>每次发送新的应用消息都必须分配一个未使用的报文标识符。  </p>
</li>
<li><p>发送的PUBLISH报文必须包含报文标识符且QoS等于1，DUP等于0。  </p>
</li>
<li><p><strong>必须将这个PUBLISH报文看作是 未确认的 ，直到从接收者那收到对应的PUBACK报文。</strong></p>
<p>接收者 ：</p>
</li>
<li><p><strong>响应的PUBACK报文必须包含一个报文标识符</strong>，这个标识符来自接收到的、已经接受所有权的 PUBLISH报文。  </p>
</li>
<li><p>发送了PUBACK报文之后，接收者必须将任何包含相同报文标识符的入站PUBLISH报文当作一 个新的消息，并忽略它的DUP标志的值。</p>
</li>
</ul>
<h2 id="QoS-2-仅分发一次"><a href="#QoS-2-仅分发一次" class="headerlink" title="QoS 2: 仅分发一次"></a>QoS 2: 仅分发一次</h2><p> 消息丢失和重复都是不可接受的。  </p>
<p>发送者：</p>
<ul>
<li>必须给要发送的新应用消息分配一个未使用的报文标识符。  </li>
<li>发送的PUBLISH报文必须包含报文标识符且报文的QoS等于2,，DUP等于0。  </li>
<li>**必须将这个PUBLISH报文看作是 未确认的 ，直到从接收者那收到对应的PUBREC报文。  **</li>
<li>收到PUBREC报文后必须发送一个PUBREL报文。PUBREL报文必须包含与原始PUBLISH报文 相同的报文标识符。  </li>
<li>**必须将这个PUBREL报文看作是 未确认的 ，直到从接收者那收到对应的PUBCOMP报文。  **</li>
<li>** 一旦发送了对应的PUBREL报文就不能重发这个PUBLISH报文。**</li>
</ul>
<p>响应者：</p>
<ul>
<li>响应的PUBREC报文必须包含报文标识符，这个标识符来自接收到的、已经接受所有权的 PUBLISH报文。  </li>
<li>在收<strong>到对应的PUBREL报文之前，接收者必须发送PUBREC报文确认任何后续的具有相同标识符 的PUBLISH报文。</strong> **在这种情况下，它不能重复分发消息给任何后续的接收者。  **</li>
<li>** **响应PUBREL报文的PUBCOMP报文必须包含与PUBREL报文相同的标识符。  </li>
<li>发送PUBCOMP报文之后，接收者必须将包含相同报文标识符的任何后续PUBLISH报文当作一 个新的发布。</li>
</ul>
<h2 id="消息分发重试"><a href="#消息分发重试" class="headerlink" title="消息分发重试"></a>消息分发重试</h2><p> 客户端和服务端必须使用原始的报文标识符重发 任何未确认的PUBLISH报文（如果QoS&gt;0）和PUBREL报文 。 这是唯一要求客户端或 服务端重发消息的情况。  </p>
<h2 id="主题名和主体过滤器"><a href="#主题名和主体过滤器" class="headerlink" title="主题名和主体过滤器"></a>主题名和主体过滤器</h2><h3 id="主题通配符"><a href="#主题通配符" class="headerlink" title="主题通配符"></a>主题通配符</h3><p> 斜杠（‘&#x2F;’ U+002F）用于分割主题的每个层级，为主题名提供一个分层结构。  </p>
<h3 id="多层通配符"><a href="#多层通配符" class="headerlink" title="多层通配符"></a>多层通配符</h3><p> 数字标志（‘#’ U+0023）是用于匹配主题中任意层级的通配符。  </p>
<h3 id="单层通配符"><a href="#单层通配符" class="headerlink" title="单层通配符"></a>单层通配符</h3><p>加号  (‘+’ U+002B) 是只能用于单个主题层级匹配的通配符。 </p>
]]></description>
    </item>
    <item>
      <title>奇异值分解（SVD）、主成分分析（PCA）和独立成分分析（ICA）</title>
      <link>https://raindrop2508.github.io/2022/04/02/tech/pca/</link>
      <guid>https://raindrop2508.github.io/2022/04/02/tech/pca/</guid>
      <pubDate>4 月 2 日，2022</pubDate>
      <description><![CDATA[<h2 id="奇异值分解（SVD）"><a href="#奇异值分解（SVD）" class="headerlink" title="奇异值分解（SVD）"></a>奇异值分解（SVD）</h2><p>奇异值分解是将矩阵分解为奇异向量（singular vector）和奇异值（singular value）。奇异值分解将原矩阵$A$分解为三个矩阵的乘积：</p>
<p>$A_{mn}&#x3D;U_{mm}D_{mn}V_{nn}^{T}$</p>
<p>其中矩阵$U$和$V$为正交矩阵，$D$为对角矩阵。$D$对角线上的元素称为$A$的奇异值，$U$的列向量被称为左奇异向量，$V$的列向量被称为右奇异向量。$A$的左奇异向量是$AA^{T}$的特征向量，$A$的右奇异向量是$A^{T}A$的特征向量。</p>
<p>设$r(D)&#x3D;k$,$r(A)\leqslant r(D)&#x3D;k$,若切割矩阵$A$取$m$行$k$列,$D$取$k$行$k$列,$V^{T}$取$k$行$n$列，则$A_{mn}&#x3D;UDV^{T}$</p>
<p>对于奇异值,它跟我们特征分解中的特征值类似，在奇异值矩阵中也是按照从大到小排列，而且奇异值的减少特别的快，在很多情况下，前10%甚至1%的奇异值的和就占了全部的奇异值之和的99%以上的比例。也就是说，我们也可以用最大的k个的奇异值和对应的左右奇异向量来近似描述矩阵。即$A_{mn}&#x3D;U_{mm}D_{mn}V_{nn}^{T}\approx U_{mk}D_{kk}V_{kn}^T$</p>
<h2 id="矩阵SVD分解"><a href="#矩阵SVD分解" class="headerlink" title="矩阵SVD分解"></a>矩阵SVD分解</h2><p>$$(A^{T}A)V_i&#x3D;\lambda_i V_i$$<br>$$(AA^{T})U_j&#x3D;\lambda_j U_j$$</p>
<p>由$V$正交，得$AV&#x3D;UDV^TV&#x3D;UD$，所以有$Av_i&#x3D;\sigma_i u_i$。由此可以得到矩阵$D$</p>
<h2 id="主成分分析（PCA）"><a href="#主成分分析（PCA）" class="headerlink" title="主成分分析（PCA）"></a>主成分分析（PCA）</h2><p>主成分分析主作为一种降维方法，将高维数据映射到低维上，并被整在低的每个轴上的方差最大。</p>
<p>推导的思路是：</p>
<blockquote>
<ul>
<li><p>假设$Z&#x3D;DX$;$Z$为pca降维之后的数据，$X$为原数据</p>
</li>
<li><p>限制条件：$Z$中各行方差尽可能大(保证最大可分性)同时$D$中各行正交。（D可以理解为将原数据从一个空间投影到另一个空间的轴，各轴之间应保持正交。而原数据转移到另一个空间在每个轴上的方差也应尽可能大。）</p>
</li>
<li><p>要保证$Var(Z)&#x3D;\frac{1}{N}\sum{tr(ZZ^T)}$最大，即$D^T\frac{1}{N}\sum{(x-\bar{x})(x-\bar{x})^T}D&#x3D;D^TCov(x)D&#x3D;D^TSD$即使得$(D)^TSD$最大【（$x-\bar{x})(x-\bar{x})^T$对数据中心化】。  </p>
</li>
<li><p>经过推导利用拉格朗日乘子法最终可以得到$XX^TD&#x3D;D\Lambda$即$XX^Td_i&#x3D;\lambda d_i$。通过对$X$进行SVD可以代替特征值分解。抽取其中几维即可实现降维。  </p>
</li>
<li><p>为了使得$ZD^T$更接近$X$可以尽可能选择较大的特征值。</p>
</li>
</ul>
</blockquote>
<h3 id="PCA的应用流程"><a href="#PCA的应用流程" class="headerlink" title="PCA的应用流程"></a>PCA的应用流程</h3><p>1.对输入的数据中心化处理<br>2.对于中心化的数据$X$,对$XX^T$做特征值分解<br>3.选择最大的的特征值对应的特征向量即为PCA降维后的数据</p>
<p>对于输入中心化的数据X也可通过SVD分解的形式得到降维后的数据。</p>
<h2 id="独立成分分析（ICA）"><a href="#独立成分分析（ICA）" class="headerlink" title="独立成分分析（ICA）"></a>独立成分分析（ICA）</h2><p>独立成分分析属于盲源分离的范畴。</p>
<p>大多是根据“鸡尾酒会问题”来推导。鸡尾酒会问题假设会场上同时发言的任务与麦克风的数量相同。为了通过麦克风录制的声音分理处不同发言者的声音（这有点类似于分离出音乐中不同声部的声音）。</p>
<p>主要思路是：</p>
<blockquote>
<ul>
<li>假设个声音（信号）源相互独立，对数据分布指定CDF(概率累计函数，概率密度函数是概率累计函数的导数)</li>
<li>使用似然估计和梯度上升推导出解混矩阵</li>
</ul>
</blockquote>
<p>具体可以参考<a href="https://zhuanlan.zhihu.com/p/376408679">ICA 独立成分分析</a></p>
<p>注意：<br>1.无法恢复高斯信号（若信号源分布旋转对称，则无法恢复）；<br>2.“麦克风”数量不能少于“声源数量”；<br>3.假设各信号相互独立。</p>
<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="http://speech.ee.ntu.edu.tw/~tlkagk/courses_ML17.html">李宏毅《机器学习》课程</a></li>
<li><a href="https://item.jd.com/11867803.html">周志华《机器学习》</a></li>
<li><a href="https://www.bilibili.com/video/BV1Mh411e7VU?p=14">【吃瓜教程】《机器学习公式详解》（南瓜书）与西瓜书公式推导直播合集</a></li>
<li><a href="https://zhuanlan.zhihu.com/p/376408679">ICA 独立成分分析</a></li>
<li><a href="http://cs229.stanford.edu/syllabus-autumn2018.html">CS229-2018秋:吴恩达,机器学习，第十六课</a></li>
</ul>
]]></description>
    </item>
    <item>
      <title>算法笔记7-图</title>
      <link>https://raindrop2508.github.io/2021/08/30/tech/%E7%AE%97%E6%B3%95%E7%AC%94%E8%AE%B07/</link>
      <guid>https://raindrop2508.github.io/2021/08/30/tech/%E7%AE%97%E6%B3%95%E7%AC%94%E8%AE%B07/</guid>
      <pubDate>8 月 30 日，2021</pubDate>
      <description><![CDATA[<p>图是比树更一般的结构，由节点和边组成。根据边是否有方向分为有向图和无向图。同时在每个边上可以加入权重，代表从一个顶点到另一个顶点的代价。</p>
<p>圈(Cycle):首尾顶点相同的路径。</p>
<h2 id="图的抽象数据类型"><a href="#图的抽象数据类型" class="headerlink" title="图的抽象数据类型"></a>图的抽象数据类型</h2><p>图的抽象数据类型主要通过邻接矩阵或邻接表来实现。</p>
<ul>
<li>邻接矩阵：每行每列代表图中的顶点，行列值表示节点是否相连;简单，但大多为稀疏矩阵效率低下。</li>
<li>邻接列表：一个列表中包含所有顶点，其中每个顶点关联一个与自身有连接的顶点的列表；存储空间紧凑高效。</li>
</ul>
<h2 id="词梯问题"><a href="#词梯问题" class="headerlink" title="词梯问题"></a>词梯问题</h2><ul>
<li>目标:找到从一个单词到另一个单词之间最短的变换序列。</li>
</ul>
<h3 id="步骤一：将所有单词的关系表达为图"><a href="#步骤一：将所有单词的关系表达为图" class="headerlink" title="步骤一：将所有单词的关系表达为图"></a>步骤一：将所有单词的关系表达为图</h3><ul>
<li><p>方法一：将所有单词之间互相比较；思路简单，但计算量大。</p>
</li>
<li><p>方法二：创建桶，将去掉一个字母后相同的单词放入统一桶中，最后对统一桶中的单词构建边。</p>
<pre><code class="language-python">def buildGraph(wordfile):
    d = {} #桶
    # 构建桶，并将相同的单词放入桶中
    for line in wfile:
        word = line[:-1]
        for i in range(len(word)):
            bucket = word[:i] + &#39;_&#39; + word[i+1:]
            if bucket in d:
                d[bucket].append(word)
            else:
                d[bucket]=word
    ......
</code></pre>
</li>
</ul>
<h3 id="步骤二：采用广度优先搜索-Breadth-First-Search-寻找所有有效路径"><a href="#步骤二：采用广度优先搜索-Breadth-First-Search-寻找所有有效路径" class="headerlink" title="步骤二：采用广度优先搜索(Breadth First Search)寻找所有有效路径"></a>步骤二：采用<strong>广度优先搜索</strong>(Breadth First Search)寻找所有有效路径</h3><p><strong>BFS搜索时，在达到更远距离K+1前会找到全部距离为K的顶点。</strong></p>
<p>搜索时需要添加距离，前驱节点，颜色三个属性，并使用队列对已发现顶点进行排列。其中颜色中白色代表尚未发现，灰色代表以及发现，黑色代表已经完成探索。</p>
<h3 id="步骤三：选择最优路径"><a href="#步骤三：选择最优路径" class="headerlink" title="步骤三：选择最优路径"></a>步骤三：选择最优路径</h3><p>最后通过追溯函数即可确定最短词梯。</p>
<h2 id="骑士周游问题"><a href="#骑士周游问题" class="headerlink" title="骑士周游问题"></a>骑士周游问题</h2><p>目标：从棋盘上一个点走遍所有棋盘上的点；建建立一个没有分支的最深的深度优先树，表现为一条线性的包含所有节点的退化树。</p>
<h3 id="步骤一：构建图"><a href="#步骤一：构建图" class="headerlink" title="步骤一：构建图"></a>步骤一：构建图</h3><p>根据马走’日’的特性设置相对位置并保证不会超过棋盘边界。之后通过遍历构建出图。</p>
<h3 id="步骤二：寻找一个路径恰好将所有节点包含所有节点且只经过一次"><a href="#步骤二：寻找一个路径恰好将所有节点包含所有节点且只经过一次" class="headerlink" title="步骤二：寻找一个路径恰好将所有节点包含所有节点且只经过一次"></a>步骤二：寻找一个路径恰好将所有节点包含所有节点且只经过一次</h3><p>通过深度优先搜索算法(Depth First Search)实现。**DFS:沿着树的单枝尽量深入向下搜索，如果无法找到解就回溯到上一层搜索下一枝。**在骑士周游问题中每个顶点仅访问一次。</p>
<p>如果沿单枝搜索至无法继续路径仍没有达到预定值（棋盘格数），就清除颜色标记递归调用函数切换至下一枝（灰色代表以及探索，白色代表未探索）。在搜索过程中使用一个栈还记录路径便于回溯。</p>
<h3 id="搜索算法的改进"><a href="#搜索算法的改进" class="headerlink" title="搜索算法的改进"></a>搜索算法的改进</h3><p>修改遍历下一格的次序。改为具有最少合法移动目标的格子优先搜索。（启发式规则）</p>
<h2 id="通用深度优先搜索"><a href="#通用深度优先搜索" class="headerlink" title="通用深度优先搜索"></a>通用深度优先搜索</h2><p>有时候深度优先搜索会创建多棵树，称为”深度优先森林”。</p>
<p>通用DFS算法中加入发现时间和结束时间两个属性。</p>
<blockquote>
<p>在访问一个节点时将节点设置为灰色（搜索中），并记录开始时间。对该节点连接的白色（未搜索）节点递归调用函数（深度优先向下搜索）。最后搜索完成后将节点改为黑色（搜索完成），并记录结束时间。</p>
</blockquote>
<h2 id="图的应用"><a href="#图的应用" class="headerlink" title="图的应用"></a>图的应用</h2><ul>
<li><p>拓扑排序：从工作流程图（DAG图：有向无环）得到工作次序排列的算法。利用DFS实现。</p>
</li>
<li><p>强连通分支：图G的子集C中任意两个顶点间都有路径来回，且C是具有这样性质的最大子集。图和专职图在强连通分支的数量和划分上是相同的。</p>
<blockquote>
<p>1.对图G使用DFS算法，计算每个顶点的结束时间。<br>2.将图G转置使用DFS算法，以顶点结束时间倒序顺序搜索。<br>3. 深度优先森林中的每一颗树就是一个强连通分支。</p>
</blockquote>
</li>
<li><p>最短路径：当图已知时，带权图的最短路径问题可以看作词梯问题的改进。使用Dijkstra算法实现。</p>
<blockquote>
<p>Dijkstra算法:在顶点vertex类中的成员dist用于记录开始顶点到到本顶点的最短带权路径长度（权重之和），算法对图中的每个顶点迭代一次，得出从一个顶点到其余所有顶点的最短路径。【只能处理权重大于0的图】</p>
</blockquote>
</li>
<li><p>最小生成树</p>
<p>  信息广播问题：</p>
<blockquote>
<p>1.最简单解法时由广播源维护一个收听者列表，将每个消息向每个收听者发送一次。<br>2.洪水解法：将每条消息在路由器间散布出去。每个路由器将消息分别转发到相邻的路由器和收听者，并加入TTL（消息源到收听者的最远距离）。<br>3.最小权重生成树。生成树：拥有图中所有顶点和最小数量的边，以保持连通的子图。使用Prim算法，每部都沿着最小权重的边向前搜索实现。</p>
</blockquote>
</li>
</ul>
<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://www.icourse163.org/learn/PKU-1206307812">数据结构与算法Python版_中国大学MOOC(慕课)</a></li>
</ul>
]]></description>
    </item>
    <item>
      <title>算法笔记6-树</title>
      <link>https://raindrop2508.github.io/2021/08/30/tech/%E7%AE%97%E6%B3%95%E7%AC%94%E8%AE%B06/</link>
      <guid>https://raindrop2508.github.io/2021/08/30/tech/%E7%AE%97%E6%B3%95%E7%AC%94%E8%AE%B06/</guid>
      <pubDate>8 月 30 日，2021</pubDate>
      <description><![CDATA[<ul>
<li>非线性数据结构；</li>
<li>包括根，枝，叶三部分；也可以说由节点(node)和边(edge)组成；</li>
<li>层次化，各个子节点之间独立每个叶节点具有唯一性；</li>
<li>树中所有节点的最大曾经称为树的高度<strong>根节点所在的层级为0</strong>。</li>
</ul>
<h2 id="树的实现"><a href="#树的实现" class="headerlink" title="树的实现"></a>树的实现</h2><h3 id="嵌套列表实现"><a href="#嵌套列表实现" class="headerlink" title="嵌套列表实现"></a>嵌套列表实现</h3><p>子树结构与树相同，是一种<strong>递归</strong>数据结构。</p>
<blockquote>
<p><code>atree=[&#39;a&#39;,[&#39;left&#39;,[],[]],[&#39;right&#39;,[],[]]]</code></p>
</blockquote>
<p>插入方法的实现(以插入左树为例)。</p>
<pre><code class="language-python">def insertLeft(root,newBranch):
  t = root.pop(1)  
  #提取左子节点
  if len(t) &gt; 1:
  #如果左子节点存在，则需要先插入新节点再将原来的节点作为其左子节点   
    root.insert(1,[newBranch,t,[]])
  else:
    root.insert(1,[newBranch,[],[]])
</code></pre>
<h3 id="树的链表实现"><a href="#树的链表实现" class="headerlink" title="树的链表实现"></a>树的链表实现</h3><ul>
<li>节点链表法：链表中每个节点保存节点的数据项以及左右子树的链接。</li>
</ul>
<p>插入方法：加入中间变量，将插入节点与原先节点相连接。</p>
<h2 id="应用"><a href="#应用" class="headerlink" title="应用"></a>应用</h2><ul>
<li>树表示表达式</li>
</ul>
<h2 id="树的遍历-Tree-Traversals"><a href="#树的遍历-Tree-Traversals" class="headerlink" title="树的遍历(Tree Traversals)"></a>树的遍历(Tree Traversals)</h2><p>遍历(Traversals)：对数据集中所有数据进行访问。对于线性数据结构，按顺序访问即可。</p>
<p>树的遍历包括：前序遍历（根–左–右），中序遍历（左–根–右）和后序遍历（左–右–根）。</p>
<h2 id="优先队列与二叉堆"><a href="#优先队列与二叉堆" class="headerlink" title="优先队列与二叉堆"></a>优先队列与二叉堆</h2><p>优先队列：先入先出，内部次序由优先级确定</p>
<h3 id="二叉堆（Binary-Heap）实现优先队列"><a href="#二叉堆（Binary-Heap）实现优先队列" class="headerlink" title="二叉堆（Binary Heap）实现优先队列"></a>二叉堆（Binary Heap）实现优先队列</h3><p>入队和出队的复杂度均为O（log n）。如果使操作始终保持再对数量级上，二叉树必须保持平衡。可以通过完全二叉树近似实现平衡。</p>
<p>完全二叉树：叶节点最多只出现在最底层和次底层；最底层的叶节点集中在左边（最多由有一个节点例外）。</p>
<p>性质：若节点下表为p,则其左子节点下标为2p，右子节点下标为2p+1。所以可以使用非嵌套列表表示完全二叉树。</p>
<p>堆次序（Heap Order）：父节点的key小于其子节点。</p>
<h3 id="二叉堆"><a href="#二叉堆" class="headerlink" title="二叉堆"></a>二叉堆</h3><p>二叉堆的性质：</p>
<ul>
<li>完全二叉树：可以用非嵌套数组表示</li>
<li>堆：任意路径为有序数列。</li>
</ul>
<h3 id="二叉堆操作的实现"><a href="#二叉堆操作的实现" class="headerlink" title="二叉堆操作的实现"></a>二叉堆操作的实现</h3><p>列表保存堆数据，列表中下标为0项不用。</p>
<pre><code class="language-python">class BinHeap:
  def __init__():
    self.heapList = [0]
    self.currentsize = 0
</code></pre>
<ul>
<li><p><code>insert（key）</code></p>
<p>为了保证二叉堆中堆的性质，需要将插入key上浮至正确的位置。插入时先添加到末尾并改变二叉树currentsize的值，之后进行上浮操作。</p>
<pre><code class="language-python">#上浮(i 二叉树节点数)
def perUp(self,i):
  while i // 2 &gt; 0:  #到根节点j结束循环
    #与父节点比较
    if self.heapList[i] &lt; self.heapList[i//2]:   
      #与父节点交换位置
      i = i // 2
</code></pre>
</li>
<li><p><code>delMin()</code></p>
<p>删除堆中最小的key，即根节点。最后一个节点代替根节点并下沉。</p>
<pre><code class="language-python"># 下沉
def percDown(self,i):
  # 是否存在左节点
  while (i * 2) &lt;= self.currentSize:
    mc = self.minChild(i) # 返回子节点的较小值的位置
    if self.heapList[i] &gt; self.heapList[mc]:
      # 交换下沉
      ......
    i = mc
</code></pre>
</li>
<li><p><code>buildHeap(lst)</code></p>
<p>利用下沉法从无序表生成堆。从最后一个节点的父节点处开始下沉（<code>len(list)//2</code>处），并不断迭代；为保证完全二叉树的性质，需要在无序表前端插入一个空值。</p>
</li>
</ul>
<h2 id="二叉查找树（Binary-Search-Tree）"><a href="#二叉查找树（Binary-Search-Tree）" class="headerlink" title="二叉查找树（Binary Search Tree）"></a>二叉查找树（Binary Search Tree）</h2><p>二叉查找树比父节点大key的在右子树，比父节点小的key在左子树。</p>
<h3 id="二叉搜索树的实现"><a href="#二叉搜索树的实现" class="headerlink" title="二叉搜索树的实现"></a>二叉搜索树的实现</h3><ul>
<li><p><code>put(key,val)</code></p>
<p>插入key构造BST。若无节点直接将插入节点作为根节点否则调用函数<code>_put(key,val,root)</code>来放至key。在该辅助函数中，若key比当前节点小，则放到左子树；若比当前节点大则放到右子树。索引赋值与插入方法相同，对于此类特殊方法需要前后加双下划线。</p>
<blockquote>
<p><a href="https://blog.csdn.net/qq_38608897/article/details/103834936">二叉树是一种动态查找表。特点是，树的结构不是一次生成的，而是在查找过程中，当树中不存在关键字等于给定值的结点时再进行插入。新插入的结点一定是一个新添加的叶子结点，并且是查找不成功时查找路径上访问的最后一个结点的左孩子或右孩子结点。</a></p>
</blockquote>
</li>
<li><p><code>get(self,key)</code>  </p>
<p>若根节点不存在返回空值，否则调用调用函数<code>_get(self,key,currentNode)</code>。在该函数中若找当对应的key则返回，否则比较key与树中key的大小，对其左、右子树递归调用函数。</p>
</li>
<li><p>迭代器<code>__iter__(self)</code></p>
<pre><code class="language-python">def __iter__(self):
  if self:
    if self.hasLeftChild():
      for elem in self.leftChild:
        # in在这里相当于递归
        yield elem
    yield self.key
    ......
</code></pre>
</li>
<li><p><code>delete(self,key)</code><br>先用_get方法找到要删除的节点之后执行删除<code>self.remove(nodeToRemove)</code>。删除节点时的情况：</p>
<ol>
<li>该节点无子节点（叶节点）—直接执行删除。</li>
<li>该节点有一个子节点—删除后将其子节点与其父节点连接，并对“删除节点是父节点的左&#x2F;右节点？删除节点唯一子节点是其左&#x2F;右节点？被删节点为根节点？”进行讨论。</li>
<li>该节点有两个子节点—将被删节点右子树最小的节点（根据BST的性质该节点有右子节点或为叶节点）替换被删节点。</li>
</ol>
</li>
</ul>
<p>二叉查找树的性能受到插入顺序的影响。</p>
<h2 id="平衡二叉树（AVL-Tree）"><a href="#平衡二叉树（AVL-Tree）" class="headerlink" title="平衡二叉树（AVL Tree）"></a>平衡二叉树（AVL Tree）</h2><p>在实现过程中，与 BST差别在于需要对每个节点加入平衡因子。平衡因子是左右子树的高度差，大于0为左重，小于0为右重。平衡树的每个节点的平衡因子在-1到1之间。AVL树的搜索时间复杂度为O（log n）。</p>
<p>在插入方法中需要接入更新平衡因子的函数，自下而上更新每个节点的平衡因子。</p>
<h3 id="重新平衡"><a href="#重新平衡" class="headerlink" title="重新平衡"></a>重新平衡</h3><p>将不平衡的子树进行旋转实现，左重右旋，右重左旋，左旋挂右，右旋挂左。在旋转过程中，只有新根节点和就根节点的平衡因子发生了变化。</p>
<pre><code class="language-python">def rotateLeft(self,rotRoot):
  newRoot = rotRoot.rightChild
  rotRoot.rightChild = newRoot.leftChild
  # 新根左子节点，挂到旧根的右子节点,父与子相连。左旋挂右。
  if newRoot.leftChild != None:
    newRoot.leftChild.parent = rotRoot 
    # 子与父相连
    ... ...
    # 整理节点之间关系
    ... ...
    rotRoot.blanceFactor = rotRoot.balanceFactor + 1 - min(newRoot.balanceFactor,0)
    newRoot.blanceFactor = newRoot.balanceFactor + 1 + max(newRoot.balanceFactor,0)
</code></pre>
<p>在左旋要检查右子节点的因子，若右子节点左重则应先右旋在左旋。同样，右旋也需要进行类似操作。</p>
<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://www.icourse163.org/learn/PKU-1206307812">数据结构与算法Python版_中国大学MOOC(慕课)</a></li>
</ul>
]]></description>
    </item>
    <item>
      <title>算法笔记5_排序与查找</title>
      <link>https://raindrop2508.github.io/2021/06/18/tech/%E7%AE%97%E6%B3%95%E7%AC%94%E8%AE%B05/</link>
      <guid>https://raindrop2508.github.io/2021/06/18/tech/%E7%AE%97%E6%B3%95%E7%AC%94%E8%AE%B05/</guid>
      <pubDate>6 月 18 日，2021</pubDate>
      <description><![CDATA[<h2 id="查找"><a href="#查找" class="headerlink" title="查找"></a>查找</h2><p>线性（顺序）关系：数据项保存在列表这样的集合中。 </p>
<ul>
<li><p><strong>顺序查找</strong>：按照顺序来访问和查找数据项；针对无序表当所要找的数据不在表中时查找需要遍历整个列表，而对于有序表可以设置一个提前结束标志减少计算量。但无论是有序表还是无序表其算法复杂度都为O(n)。 </p>
</li>
<li><p><strong>二分查找（binary search）</strong>：对于<strong>有序表</strong>可以从中间项开始匹配，不断缩小表的规模知道找到数据的位置。二分查找法也体现了<strong>分治算法</strong>。因此二分查找法可以用递归实现。算法复杂度为O(log n)。</p>
<pre><code class="language-python">def binarySearch(alist,item):  #二分查找的递归实现
......
midpoint = len(alist)//2
if alist[midpoint]==item:
    return True
else:   #减小规模，调用自身
    if item &lt;alist[midpoint]:
        return binarySearch(alist[:midpoint],item)
    else:
        return binarySearch(alist[midpoint+1:],item)
</code></pre>
</li>
</ul>
<p>对于两种查找算法，需要根据实际的应用情况进行选择。</p>
<h2 id="排序"><a href="#排序" class="headerlink" title="排序"></a>排序</h2><ul>
<li><p><strong>冒泡排序（bubble sort）</strong>:两两相邻的数据进行比较，多趟之后即可完成排序。与在c语言中交换两个位置的值需要中间变量不同，python中可以使用<code>alist[i],alist[i+1]=alist[i+1],alist[i]</code>语句完成。算法每趟的比较次数由n-1不断递减，因此算法复杂度为O(n^2)。</p>
<p>  冒泡排序常作为其他算法的对比基准（妥妥的工具人🙃），其中的对次比对交换操作时无效的。优势在于无需额外储存空间的开销。<br>  改进的冒泡排序：当某次比对未发生交换，便说明比对完成，可以提前结束。</p>
<pre><code class="language-python">def shortBubbleSort(alist):
    ......
    exchange = False
    for i in range(passnum):
        if alist[i]&gt;alist[i+1]:
            exchange = True
            alist[i],alist[i+1]=alist[i+1],alist[i]
        passnum = passnum-1
</code></pre>
</li>
<li><p><strong>选择排序(Selection Sort)</strong>：对冒泡算法中,每趟只进行一次交换。先记录最大项所在的位置再交换。</p>
</li>
<li><p><strong>插入排序（Insertion Sort）</strong>:复杂度仍为O(n^2);是中唯持一个已排好序的子列表，其位置始终在列表的前部。并将其逐步扩大只全表。列表越接近有序，插入排序的对比次数就越少。</p>
<pre><code class="language-python">def insertionSort(alist):
......
while position &gt; 0 and alist[position-1]&gt;currentvalue:
    alist[position]=alist[position-1]
    position = position - 1
    #每次与左侧的项进行比较，如果左侧的值大就将其右移，依次循环直到左侧值更小，在之前的空位中插入。
alist[position]=currentvalue
</code></pre>
</li>
<li><p><strong>谢尔排序（Shell Sort）</strong>:以“间隔”划分子列表，每个子列表执行插入排序，从而减少整体排序对比的次数。最后再进行标准的插入排序。减少了插入排序中无效的比对，介于O(n)于O(n^2)之间。</p>
</li>
</ul>
<h3 id="分治思想的排序算法"><a href="#分治思想的排序算法" class="headerlink" title="分治思想的排序算法"></a>分治思想的排序算法</h3><ul>
<li><p><strong>归并排序（Merge Sort）</strong>：不断将数据表分裂为原来的一半，直到表中仅有一个数据项。之后再从最小规模开始排序（<strong>两个列表中小的先放，迭代，最后归并剩余项</strong>）直到完成这个列表的排序。</p>
<p>  总体来说分为分裂和归并两个过程，算法复杂度分别为O(log n)和O(n)。但会占用更多的储存空间。</p>
</li>
<li><p><strong>快速排序（Quick Sort）</strong>:依据“中值”将数据项分为两半，排序。不断分解至仅有一个数据项。算法复杂度取决于“中值”选取的情况，综合为O(log n)，但不需要额外的储存空间。“中值”选取中很难取得列表中准确的中值（真要准确的中值不还得排序😐），因此只能尽可能通过各种方法使选取的值与“中值”接近。</p>
<p>  分裂方法：左标向右移动，直到遇到比中值大的左标；同时右标向右移动，直到遇到比中值小的。将左右标指向的值交换并继续向前移动。当左标到达右标的右侧，右标所指位置即为“中值”。</p>
</li>
</ul>
<h2 id="散列（Hashing）"><a href="#散列（Hashing）" class="headerlink" title="散列（Hashing）"></a>散列（Hashing）</h2><p>散列可以使查找算法的复杂度降到O(1)，散列表(hash table)包含用来储存<strong>数据的槽(slot)以及槽对应的唯一的名称。<strong>槽被数据项占据的比例称为</strong>负载因子</strong>。</p>
<p>数据与槽名称通过散列函数来对应。可以通过除余数来实现槽号与槽数据项的对应，但当要存储的两个数字余数相同时便会产生冲突。</p>
<p>如果一个散列函数能不会造成冲突便称之为<strong>完美散列函数</strong>。完美散列函数可以用于数据校验。将散列值看作槽中信息的摘要，由于该摘要具有唯一性因此可以通过摘要对比信息的异同。要具备数据校验的函数应具有以下特性；压缩性、易计算性、抗修改性和抗冲突性。</p>
<p>近似完美散列函数MD5,SHA。</p>
<h2 id="区块链"><a href="#区块链" class="headerlink" title="区块链"></a>区块链</h2><p>区块链是一种分布式数据库，具有去中心化的特点。区块链由区块(block)组成。每个区块中包含着头(head)和体(body)。头中记录着一些元数据和链接到前一个区块的信息（生成时间、前一个区块的散列值）。</p>
<p>区块链具有不可修改性。由于区块链的分布式的性质，同步速度较慢因此新区快的添加速度被限制。</p>
<p>PS:每次虚拟货币涨的时候，硬件价格便会水涨船高，加上黄牛党的推波助澜，DIY玩家苦不堪言。</p>
<h2 id="散列函数的设计"><a href="#散列函数的设计" class="headerlink" title="散列函数的设计"></a>散列函数的设计</h2><ul>
<li><strong>折叠法</strong>：对数据分段、相加、求余得到槽号。通过哥数反转的方法进行微调。</li>
<li><strong>平方取中法</strong>：数据平方、取中间两位、求余（相比折叠法，计算量提高）。</li>
</ul>
<blockquote>
<p>特殊情况<br>- 对于非数项可通过ASCⅡ表进行转换。<br>- 对于变为词，加入权重因子</p>
</blockquote>
<p>散列函数在设计时盈尽量简单并减少冲突。</p>
<h2 id="冲突解决方案"><a href="#冲突解决方案" class="headerlink" title="冲突解决方案"></a>冲突解决方案</h2><p>再找一个空槽放置冲突数据（<strong>开放定址、再散列</strong>）。  </p>
<ul>
<li>线性探测：向后逐个寻找空槽，查找时顺序查找。缺点：有聚集趋势。</li>
<li>跳跃式探测；选择固定步长或逐步变化进行探测。固定步长取值时不能被散列表大小整除，否则会产生周期。</li>
</ul>
<p><strong>数据项链</strong>：槽扩展为容纳数据项的集合。这会延长查找的时间</p>
<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://www.icourse163.org/learn/PKU-1206307812">数据结构与算法Python版_中国大学MOOC(慕课)</a></li>
</ul>
]]></description>
    </item>
    <item>
      <title>算法笔记4_递归Recursion</title>
      <link>https://raindrop2508.github.io/2021/05/04/tech/%E7%AE%97%E6%B3%95%E7%AC%94%E8%AE%B04/</link>
      <guid>https://raindrop2508.github.io/2021/05/04/tech/%E7%AE%97%E6%B3%95%E7%AC%94%E8%AE%B04/</guid>
      <pubDate>5 月 4 日，2021</pubDate>
      <description><![CDATA[<h2 id="开头的碎碎念"><a href="#开头的碎碎念" class="headerlink" title="开头的碎碎念"></a>开头的碎碎念</h2><p>个人认为，递归算法是一种十分优美的算法。它总是能够用十分短小的代码长度来解决许多复杂的问题。前端实际，在看快速傅里叶变换(FFT)时，看到递归算法仅用极小的代码量即可实现复杂的理论，内心感到十分的震撼。</p>
<hr>
<p>将一个分问题不断拆分成更小的相同问题，其在算法上的特点就是<strong>调用自身</strong>。</p>
<ol>
<li>结束条件</li>
<li>减小规模</li>
<li>调用自身</li>
</ol>
<p>递归调用的实现：函数每次调用将现场数据压入系统调用栈，当函数返回时则从栈顶返回恢复现场。</p>
<blockquote>
<p>现场数据：栈保存的一个函数调用所需要维护的信息。<br>每次调用，压入栈的现场数据成为栈帧。</p>
</blockquote>
<pre><code class="language-python">#python中递归深度的设置(默认为1000)
    import sys
    sys.getrecursionlimit()
    sys.setrecursion(3000)
</code></pre>
<h2 id="递归的应用"><a href="#递归的应用" class="headerlink" title="递归的应用"></a>递归的应用</h2><ul>
<li>递归数列求和：将问题拆分成两个数相加，当列表长度为1时结束。</li>
<li>0-16任意进制的转换：设置<code>convertstring = &quot;0123456789ABCDEF&quot;</code> 通过除以整除<code>\\</code>进制基<code>base</code>以及对进制基求余数来将整数拆开。当数小于进制基时结束。</li>
</ul>
<pre><code class="language-python">    convertstring = &quot;0123456789ABCDEF&quot;
    ......
    else:
        return toStr(n//base,base) + convertstring[n%base]
        #字符串连接；n//base表示整除
</code></pre>
<ul>
<li><p>迷宫：在原位置先向北走一步，如果找不到出口则按北南西东的顺序尝试。（这个实际上随意设置也行）；为了防止艳茹无限递归的死循环，需要加入之前的路径。</p>
</li>
<li><p>找零兑换：求兑换最少数量的硬币  </p>
<ol>
<li>贪心策略：从允许最多数量最大面值的硬币开始，余额则用下一最大面值硬币尽可能多的数量分解。由此直到到达最小面值或余额为0。为了减少算法中的重复计算，可以将中间结果保存在表中。递归之前查表，直接返回。<br>2.动态规划法</li>
</ol>
</li>
</ul>
<h2 id="动态规划"><a href="#动态规划" class="headerlink" title="动态规划"></a>动态规划</h2><p><strong>动态规划</strong>：从问题的最小规模的最优解开始，逐步扩大问题规模到要解决的问题。</p>
<p>最优化问题能用动态规划解决的必要条件：<strong>问题最优解包含和更小规模子问题的最优解</strong>。</p>
<h2 id="递归可视化"><a href="#递归可视化" class="headerlink" title="递归可视化"></a>递归可视化</h2><p>分形树，谢尔宾斯基三角形，汉诺塔</p>
<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://www.icourse163.org/learn/PKU-1206307812">数据结构与算法Python版_中国大学MOOC(慕课)</a></li>
</ul>
]]></description>
    </item>
    <item>
      <title>算法笔记3</title>
      <link>https://raindrop2508.github.io/2021/03/31/tech/%E7%AE%97%E6%B3%95%E7%AC%94%E8%AE%B03/</link>
      <guid>https://raindrop2508.github.io/2021/03/31/tech/%E7%AE%97%E6%B3%95%E7%AC%94%E8%AE%B03/</guid>
      <pubDate>3 月 31 日，2021</pubDate>
      <description><![CDATA[<h2 id="队列-queue"><a href="#队列-queue" class="headerlink" title="队列(queue)"></a>队列(queue)</h2><p>队列的特征：先进先出（FIFO）<br>队列仅有一个入口和出口。添加数据处为尾端，移除数据处为首端。</p>
<p>队列的操作<br><img loading="lazy" src="https://s1.ax1x.com/2020/03/30/Gnw9PO.png" alt="queue.png"></p>
<p>如果将list的首端作为队列尾端，list的末端作为队列的首端。enqueue()的复杂度为O(n),dequeue()的复杂度为O(1)。</p>
<p>首位倒过来时复杂度也倒过来</p>
<p><strong>双端队列（deque）</strong>:队首和队尾都可以进行加入和移出操作。</p>
<h2 id="无序表-unordered-list"><a href="#无序表-unordered-list" class="headerlink" title="无序表(unordered list)"></a>无序表(unordered list)</h2><p>数据的排列不具有顺序性，仅依靠相对位置</p>
<h3 id="利用链表实现无序表"><a href="#利用链表实现无序表" class="headerlink" title="利用链表实现无序表"></a>利用链表实现无序表</h3><p>链表的每个节点至少包含：数据项本身，下一节点的引用信息  </p>
<p>通过在数据项之间建立链接指向，就可以保持其前后相对位置。<br>通过链表实现的无序表仅仅包含对于受节点head的引用。</p>
<p><strong>添加(add)</strong>：将新的数据项添加至表头，减少算法复杂度。</p>
<pre><code class="language-python">temp.setNext(self.head)
self.head = temp
#以上两条语句不能交换位置
</code></pre>
<p>链表的查找(search)和长度(size)方法都使用遍历的思想。删除(remove)第n的节点时需要把第n-1个节点指向n+1个节点。因此，要实现该方法需要引用当前节点和上一节点。</p>
<h2 id="有序表-ordered-list"><a href="#有序表-ordered-list" class="headerlink" title="有序表(ordered list)"></a>有序表(ordered list)</h2><p>依照某种可比性，决定列表个元素的排序</p>
<blockquote>
<p>对于python，可以适用于所有定义了<code>__gt__</code>方法的数据类型</p>
</blockquote>
<p><strong>查找(search)方法</strong>：当遍历到的数据项大于所要查找的数据项，则说明所要查找的元素在链表中不存在。</p>
<p><strong>添加(add)方法</strong>：需要引用当前节点和上一节点，将新的元素加入到两者之间。</p>
<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://www.icourse163.org/learn/PKU-1206307812">数据结构与算法Python版_中国大学MOOC(慕课)</a></li>
</ul>
]]></description>
    </item>
    <item>
      <title>算法笔记2</title>
      <link>https://raindrop2508.github.io/2021/01/03/tech/%E7%AE%97%E6%B3%95%E7%AC%94%E8%AE%B02/</link>
      <guid>https://raindrop2508.github.io/2021/01/03/tech/%E7%AE%97%E6%B3%95%E7%AC%94%E8%AE%B02/</guid>
      <pubDate>1 月 3 日，2021</pubDate>
      <description><![CDATA[<h1 id="基本数据结构"><a href="#基本数据结构" class="headerlink" title="基本数据结构"></a>基本数据结构</h1><h2 id="线性结构（linear-structure）"><a href="#线性结构（linear-structure）" class="headerlink" title="线性结构（linear structure）"></a>线性结构（linear structure）</h2><ul>
<li>有序数据项的集合</li>
<li>每个数据项都有唯一的前驱和后继（第一个和最后一个除外）</li>
</ul>
<p>根据数据项增减的方式构成了数据结构 </p>
<ul>
<li>栈(Stack)–仅在表尾进行插入和删除操作的线性表</li>
<li>队列(Queue)–只允许在表的前端（front）进行删除操作，而在表的后端（rear）进行插入操作</li>
<li>双端队列(Deque)–限定插入和删除操作在表的两端进行的线性表</li>
<li>列表(List)–</li>
</ul>
<h2 id="栈"><a href="#栈" class="headerlink" title="栈"></a>栈</h2><p>进行操作的一端为栈顶，另一端为栈底<br>栈的特点：<strong>后进先出LIFO</strong></p>
<p><img loading="lazy" src="https://s2.ax1x.com/2020/03/02/3fPlFJ.png" alt="3fPlFJ.png"></p>
<p>栈通过python的实现可以借助list的数据类型</p>
<h2 id="栈的应用"><a href="#栈的应用" class="headerlink" title="栈的应用"></a>栈的应用</h2><h3 id="括号匹配–基本思路"><a href="#括号匹配–基本思路" class="headerlink" title="括号匹配–基本思路"></a>括号匹配–基本思路</h3><p><a href="https://imgchr.com/i/8psmYd"><img loading="lazy" src="https://s2.ax1x.com/2020/03/09/8psmYd.png" alt="8psmYd.png"></a></p>
<p>括号匹配与之前图灵机的模型有些相似； </p>
<ul>
<li>括号匹配可以用用于爬虫HTML数据的爬取；另外该方法也可通过正则表达式实现。</li>
</ul>
<h3 id="十进制与二进制的转换"><a href="#十进制与二进制的转换" class="headerlink" title="十进制与二进制的转换"></a>十进制与二进制的转换</h3><p>十进制转换二进制是余数的连续求取，并将求得的余数倒过来书写。通过栈后进先出的特性可以实现。</p>
<p>同时由此可以进行十进制到其他进制的转换。当转换的进制为十一禁进制以上时，可以使用数组来保存其中的字母</p>
<pre><code class="language-python">    digits = &quot;0123456789ABC&quot;
</code></pre>
<h3 id="表达式转换"><a href="#表达式转换" class="headerlink" title="表达式转换"></a>表达式转换</h3><p>根据表达式操作符的的位置分为中缀、前缀和后缀，距离操作数最近的操作符先执行</p>
<ul>
<li><p>中缀表达式转换为前缀和后缀表达式<br>将表达式转换为全括号形式，将内部每个运算符移到对应的左括号或右括号处边可以转换为前缀、后缀表达式</p>
</li>
<li><p>中缀转后缀算法</p>
</li>
</ul>
<p>从左到右扫描过程中，采用栈来暂存为处理的操作符，当遇到一个新的操作符，就需要跟栈顶的操作符比较下优先级，再行处理。</p>
<p>算法流程</p>
<blockquote>
<p>从左到右扫描<br>- 当遇到操作数，直接输出至列表末尾<br>- 当遇到左括号压入栈顶<br>- 当遇到右括号，反复弹出栈顶加入至输出列表末尾，直到碰到左括号<br>- 当遇到操作符，与栈顶其他操作符比较。栈顶操作符高于或等于它，则将输出栈顶的操作符直到优先级低于它</p>
</blockquote>
<h3 id="后缀表达式的求值"><a href="#后缀表达式的求值" class="headerlink" title="后缀表达式的求值"></a>后缀表达式的求值</h3><p>后缀表达式的操作符在操作数的后面，因此要暂存操作数,直到碰到操作符才进行运算(从这可以利用栈的特性)</p>
<p><strong>在实际运算时，先弹出的时右操作数然后才是左操作数，对于‘-’和‘&#x2F;’要注意两个操作数的位置</strong></p>
<p>算法流程</p>
<ul>
<li><p>创建空栈用于暂存操作数</p>
</li>
<li><p>从左到右扫面单词列表</p>
<ol>
<li>如果是操作数，压入栈顶</li>
<li>如果是操作符，从栈顶弹出两个操作数，进行计算。（注意操作数的位置）</li>
</ol>
</li>
<li><p>最后扫描结束后，表达式的值被存在栈顶（如果表达式正确，则栈中仅有最后的结果一个元素）</p>
</li>
</ul>
<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://www.icourse163.org/learn/PKU-1206307812">数据结构与算法Python版_中国大学MOOC(慕课)</a></li>
</ul>
]]></description>
    </item>
    <item>
      <title>算法笔记1</title>
      <link>https://raindrop2508.github.io/2021/01/03/tech/%E7%AE%97%E6%B3%95%E7%AC%94%E8%AE%B01/</link>
      <guid>https://raindrop2508.github.io/2021/01/03/tech/%E7%AE%97%E6%B3%95%E7%AC%94%E8%AE%B01/</guid>
      <pubDate>1 月 3 日，2021</pubDate>
      <description><![CDATA[<h1 id="算法分析"><a href="#算法分析" class="headerlink" title="算法分析"></a>算法分析</h1><h2 id="算法和算法复杂性"><a href="#算法和算法复杂性" class="headerlink" title="算法和算法复杂性"></a>算法和算法复杂性</h2><p>可计算的问题：  </p>
<ul>
<li>what：分类问题——树状判定分支</li>
<li>why： 证明问题——公式序列解决</li>
<li>how： 或称问题——算法流程解决</li>
</ul>
<p>欧几里得算法的python实现  </p>
<pre><code class="language-python">def alg(a,b):
    if b==0 :
        return a
    print(a)
    c=a%b
    alg(b,c)
def main():   #a,b为要计算的两个数
    a=15
    b=9
    print(alg(a,b))
main()
</code></pre>
<p>计算复杂性研究的是问题的难度，算法实在资源约束的情况下寻找最优方案。  </p>
<p>不可计算问题  </p>
<ul>
<li>停机问题</li>
<li>几乎所有无理数都无法通过算法找出任意一位精确数</li>
</ul>
<h2 id="程序与算法的区别"><a href="#程序与算法的区别" class="headerlink" title="程序与算法的区别"></a>程序与算法的区别</h2><p>算法是描述问题解决的分布步骤而程序则是通过某种编程语言实现的算法。<br>算法分析主要是从计算机资源消耗的角度来评判和比较算法。评判的两个标准有两种：算法执行时间和空间（内存或存储空间）。  </p>
<p>利用python的time库中 <code>time.time()</code>函数可以计算算法的执行时间。  </p>
<blockquote>
<p><code>time.time()</code> 是计算从1970到现在的时长，并将值返回。</p>
</blockquote>
<p>对于算法运行时间的检测也收到语言性能、机器的性能的影响。  </p>
<h2 id="大O表示法"><a href="#大O表示法" class="headerlink" title="大O表示法"></a>大O表示法</h2><p>一个算法所实施的操作数量或步骤可作为独立于程序&#x2F;机器的度量指标。<br>程序设计语言中除与计算资源无关的定义语句外，主要是三种控制流语句和赋值语句。  </p>
<p><strong>一个赋值语句包含了（表达式）计算和（变量）存储两个基本资源。而控制流语句仅起组织语句的作用，并不涉及处理。</strong></p>
<p>算法分析的目标是找出问题规模如何影响执行时间</p>
<h3 id="数量级函数"><a href="#数量级函数" class="headerlink" title="数量级函数"></a>数量级函数</h3><p>数量级函数描述了T（n）中随着n增加而增加速度最快的主导部分。称作大O表示法记作O(f(n)),其中f(n)表示T(n)中的主导部分。</p>
<blockquote>
<p>T(n)&#x3D;5n^2+27n+1005<br>当n越来越大起主导作用的是5*n^2,其中系数5对n^2的增长速度无影响。因此可以表示为O(n^2)</p>
</blockquote>
<p>具体数据也会影响算法运行时间，如排序算法。此时分为最好，最差和平均状况，主要还是通过平均状况分析性能。  </p>
<p><img loading="lazy" src="https://s2.ax1x.com/2020/02/03/10fUJO.png"></p>
<p>大O表示所有上限中最小的那个上限<br>大Ω表示所有下限中最大的那个下限<br>如果上下限相同用Θ大表示  </p>
<h2 id="变位词"><a href="#变位词" class="headerlink" title="变位词"></a>变位词</h2><blockquote>
<p>写一个布尔函数，一两个此作为参数，返回两个词是否为变位词</p>
</blockquote>
<ul>
<li>将每一个单词逐个检查</li>
</ul>
<p>课程给的代码示例：</p>
<pre><code class="language-python">def solu1_example(s1,s2):
   alist = list(s2)
   pos1 = 0
   stillok = True
   while pos1 &lt; len(s1) and stillok: #只要字符中有一个没找到就可以通过32行退出
       pos2 = 0
       found =False
       while pos2 &lt; len(alist) and not found:     
           if s1[pos2] ==  alist[pos2]:
               found = True
           else:
               pos2 = pos2 + 1
           if found:
               alist[pos2] = None
           else:
               stillok = False
       pos1 = pos1 + 1
   return stillok   
</code></pre>
<p>如果将自己一个一个对比，要注意同一个字符在字符串中可能会出现很多次。因此，在判断是找到一个相同时必须要设置为none</p>
<h3 id="数量级的计算"><a href="#数量级的计算" class="headerlink" title="数量级的计算"></a>数量级的计算</h3><p>两层循环，外层循环为n次，每次到内层每次循环查找次数为1–n之间。因此总和为：1&#x2F;2*(n^2)+1&#x2F;2*n。所以数量级为O(n^2)</p>
<ul>
<li>排序比较：先将字符串按字母顺序排好，再一一对比</li>
</ul>
<pre><code class="language-python">def solu2(s1,s2):
    alist1 = list(s1)
    alist2 = list(s2)

    alist1.sort()    #列表排序函数
    alist2.sort()
    pos=0
    matches = True
    while pos &lt; len(s1) and matches:
        if alist1[pos] == alist2[pos]:
            pos = pos + 1
        else:
            matches = False
        return matches
</code></pre>
<p>看似很简单仅有一个循环，但在两个数组排序过程消耗的时间不可忽视。时间运算数量级为O(nlog n)</p>
<h3 id="暴力法"><a href="#暴力法" class="headerlink" title="暴力法"></a>暴力法</h3><p>将S1中的字符进行全排列，然后查看S2是否在其中。<br>用暴力算法解决时，数量级会以N!的方式增长。</p>
<h3 id="计数比较"><a href="#计数比较" class="headerlink" title="计数比较"></a>计数比较</h3><p>检查26个字母在字符中出现的情况，若两者相同则输出。</p>
<p><code>ord()</code>函数：返回字符的Unicode编码值</p>
<p>T(n)&#x3D;2n + 26;<br>因此数量级为O(n);相比之下，该项算法性能较优，然而该算法需要的内存空间较大。  </p>
<pre><code class="language-python">def solu4(s1,s2):
    c1 = [0] * 26
    c2 = [0] * 26
    for i in range(len(s1)):
        pos = ord(s1[i]) - ord(&#39;a&#39;)   #返回一个字符的uincode编码
        c1[pos] = c1[pos] + 1
    for i in range(len(s2)):
        pos = ord(s2[i]) - ord(&#39;a&#39;)
        c2[pos] = c2[pos] + 1
    j = 0
    stillok = True
    while j &lt; 26 and stillok:
        if c1[j] == c2[j]:
            j = j + 1
        else:
            stillok = False
        return stillok
</code></pre>
<h1 id="Python数据类型的性能"><a href="#Python数据类型的性能" class="headerlink" title="Python数据类型的性能"></a>Python数据类型的性能</h1><h2 id="list和dict"><a href="#list和dict" class="headerlink" title="list和dict"></a>list和dict</h2><p><img loading="lazy" src="https://s2.ax1x.com/2020/03/02/3WYNM6.png" alt="3WYNM6.png"></p>
<p>按索引取值和赋值：由于列表的随机访问特性，均为O(1)  </p>
<p>列表添加append()和_add_()”+”  </p>
<ul>
<li>list.append(v)–O(1),lst&#x3D; lst+ [v]  </li>
<li>实行时间为O(n+k),k为所加列表长度</li>
</ul>
<h2 id="pop的复杂度"><a href="#pop的复杂度" class="headerlink" title="pop的复杂度"></a>pop的复杂度</h2><p>从尾部移除数组的元素是复杂度为O(1),移除数组中某一元素为O(N)；原因是移除中间元素需要将这个元素后面的元素前移。这是为了保证按索引取值和赋值速度的妥协。</p>
<p>在列表中in操作复杂度为O(N),字典中为O(1)。</p>
<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ul>
<li><a href="https://www.icourse163.org/learn/PKU-1206307812">数据结构与算法Python版_中国大学MOOC(慕课)</a></li>
</ul>
]]></description>
    </item>
    <item>
      <title>算法笔记0</title>
      <link>https://raindrop2508.github.io/2021/01/03/tech/%E7%AE%97%E6%B3%95%E7%AC%94%E8%AE%B00/</link>
      <guid>https://raindrop2508.github.io/2021/01/03/tech/%E7%AE%97%E6%B3%95%E7%AC%94%E8%AE%B00/</guid>
      <pubDate>1 月 3 日，2021</pubDate>
      <description><![CDATA[<h2 id="抽象数据类型（ADT）"><a href="#抽象数据类型（ADT）" class="headerlink" title="抽象数据类型（ADT）"></a>抽象数据类型（ADT）</h2><p>是对数据进行处理的一种逻辑描述，并不设计如何实现这些处理。</p>
<h2 id="递归"><a href="#递归" class="headerlink" title="递归"></a>递归</h2><p>编写递归代码要注意的三点：</p>
<blockquote>
<ul>
<li>递归总有一个最简单的情况—方法的第一条语句总是包含 return 的条件语句。  </li>
<li>递归调用总是去尝试解决一个规模更小的子问题，这样递归才能收敛到最简单的情况。</li>
<li>递归调用的父问题和尝试解决的子问题之间不应该有交集。</li>
</ul>
</blockquote>
]]></description>
    </item>
    <item>
      <title>《红拂夜奔》</title>
      <link>https://raindrop2508.github.io/2021/01/03/other/hongfu/</link>
      <guid>https://raindrop2508.github.io/2021/01/03/other/hongfu/</guid>
      <pubDate>1 月 3 日，2021</pubDate>
      <description><![CDATA[<p>王小波的书读起来都不是那么的晦涩难懂，但读着读着却好像抓住了一些隐晦的道理，这些看似简单但有耐人寻味。</p>
<p>这本书越是读到后面越是感觉的一丝丝凄凉。红拂，卫公，王二放荡不羁，心中有着一个诗意的世界。都以为自己可以挣脱一切束缚，却不知早已掉进更大的陷阱中去。卫公和红拂跳出了洛阳城，却又困在长安城中再也无法离开。而和人糟心的是，这是卫公亲手打造的长安城。书中说，这就好比把自己当成猪一样，自己还给自己造了围栏。</p>
<p>这不禁让我想起:曾经，我天真地以为进入初中，高中，大学，社会就会过得更自由，可以做自己想做的事。但到最后连能体面地活下去都是奢望了。不曾想自己早已变成了那万众韭菜中的一抹绿。 于是，我才明白。人生就像俄罗斯套娃一样，而我一直是在最里面那层。随着你不断的长大，你回发现那束缚一层一层，无穷无尽。即使你能够傲视群雄，仍不能放浪形骸。</p>
<p>这个世界许多时候真的太糟糕了，甚至各种匪夷所思的事情在各地轮番上演，比任何小说都魔幻。书中的话”一个人只拥有此生此世是不够的，他还应该拥有诗意的世界。”对我来说更像是一种安慰，在世界上唯一能让自己无拘无束的，还是心中的那个世界啊。</p>
]]></description>
    </item>
    <item>
      <title>雾</title>
      <link>https://raindrop2508.github.io/2021/01/03/other/%E9%9B%BE/</link>
      <guid>https://raindrop2508.github.io/2021/01/03/other/%E9%9B%BE/</guid>
      <pubDate>1 月 3 日，2021</pubDate>
      <description><![CDATA[<p>清晨，街道;</p>
<p>单车，外套。</p>
<p>家中的黑猫，</p>
<p>不辞而别。</p>
<p>我拼命的寻找，</p>
<p>却仿佛从未出现。</p>
<hr>
]]></description>
    </item>
    <item>
      <title>Yolo学习笔记＿１</title>
      <link>https://raindrop2508.github.io/2019/08/17/tech/yolo%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0%EF%BC%BF%EF%BC%91/</link>
      <guid>https://raindrop2508.github.io/2019/08/17/tech/yolo%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0%EF%BC%BF%EF%BC%91/</guid>
      <pubDate>8 月 17 日，2019</pubDate>
      <description><![CDATA[<h1 id="yolov3学习笔记"><a href="#yolov3学习笔记" class="headerlink" title="yolov3学习笔记"></a>yolov3学习笔记</h1><h1 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h1><p><strong>下采样</strong>即图像缩小；将x*x个像素点取均值换算为一个。<br><strong>上采样</strong>即图像放大，利用插值算法。</p>
<p><strong>ground truth</strong>即监督学习中数据标注的正确的范围。也就是标准答案的意思。<br><strong>IOU</strong>预测的范围（bounding box）与ground truth的交集&#x2F;两者并集</p>
<p><strong>bounding box(bbox)</strong> 在目标检测中不仅要知道目标的类别还要知道它的位置。在木变检测中常用<strong>边界框</strong>来描述目标位置。<br><strong>anchor box</strong>目标检测算法通常会在输入图像中采样大量的区域，然后判断这些区域中是否包含我们感兴趣的目标，并调整区域边缘从而更准确地预测目标的真实边界框（ground-truth bounding box）。不同的模型使用的区域采样方法可能不同。这里我们介绍其中的一种方法：它以每个像素为中心生成多个大小和宽高比（aspect ratio）不同的边界框。这些边界框被称为<strong>锚框（anchor box）</strong>。(<strong>——动手做深度学习</strong>)</p>
<p><strong>精确率与召回率</strong>实际上非常简单，精确率是针对我们预测结果而言的，它表示的是预测为正的样本中有多少是真正的正样本。那么预测为正就有两种可能了，一种就是把正类预测为正类(TP)，另一种就是把负类预测为正类(FP)，也就是<br>P&#x3D;TP&#x2F;(TP+FP)<br>而召回率是针对我们原来的样本而言的，它表示的是样本中的正例有多少被预测正确了。那也有两种可能，一种是把原来的正类预测成正类(TP)，另一种就是把原来的正类预测为负类(FN)。<br>R&#x3D;TP&#x2F;TP+FN</p>
<p><strong>非极大值抑制</strong>简称为NMS算法，思想是搜索局部最大值，抑制极大值。<br>流程如下：  </p>
<blockquote>
<p>根据置信度得分进行排序<br>选择置信度最高的比边界框添加到最终输出列表中，将其从边界框列表中删除<br>计算所有边界框的面积<br>计算置信度最高的边界框与其它候选框的IoU。<br>删除IoU大于阈值的边界框<br>重复上述过程，直至边界框列表为空。  </p>
</blockquote>
<p>**批标准化(batch normalization)**将分散的数据统一的做法，便与神经网络的学习和优化。normalization 预处理, 使得输入的 x 变化范围不会太大, 让输入值经过激励函数的敏感部分。BN实在每个全连接层之间都进行批数据标准化。(作者：<br><img loading="lazy" src="https://pic3.zhimg.com/80/v2-083ca0bcd0749fd0f236a690b50442e6_hd.png" alt="BN"></p>
<h2 id="聚类算法"><a href="#聚类算法" class="headerlink" title="聚类算法"></a><a href="https://zhuanlan.zhihu.com/p/21387568">聚类算法</a></h2><p>聚类算法是根据样本之间的距离来将他们归为一类的，这个距离不是普通的距离，理论上叫做欧氏距离。<br>一般面向大量的，同时维度在2个或两个以上的样本群。</p>
<blockquote>
<p>1、在样本中随机选择K个点，作为每个类别的初始中心点，这K是自己定的，假如你想将样本分成3个类K就等于3,4个类K就等于4；<br>2、计算所有样本离这K个初始中心点的距离并分别进行比较，选出其中最近的距离并把这个样本归到这个初始中心点的类别里，即总共划分成K个类别；<br>3、舍弃原来的初始中心点，在划分好的K个类别里分别计算出新的中心点，使得这些中心点距离他类别里的所有样本的距离之和最小；<br>4、判断新获得的中心点是否与旧中心点一样，如不一样则回到第2步，重新计算所有样本离这K个新的中心点的距离并进行比较，选出其中最近的距离并归到这个新的中心点的类别里，继续下面的步奏；如一样则完成，即收敛。</p>
</blockquote>
<h2 id="残差网络"><a href="#残差网络" class="headerlink" title="残差网络"></a><a href="https://www.cnblogs.com/wuliytTaotao/p/9560205.html">残差网络</a></h2><h3 id="残差"><a href="#残差" class="headerlink" title="残差"></a>残差</h3><p>残差：实际观测值与估计值（拟合值）之间的差。如果回归模型正确则残差可以看做误差的观测值。</p>
<p>理论上，可以训练一个 shallower 网络，然后在这个训练好的 shallower 网络上堆几层 identity mapping（恒等映射） 的层，即输出等于输入的层，构建出一个 deeper 网络。这两个网络（shallower 和 deeper）得到的结果应该是一模一样的.</p>
<h3 id="退化问题"><a href="#退化问题" class="headerlink" title="退化问题"></a>退化问题</h3><p>为什么属于随着层数的增多<strong>训练集</strong>上的效果变差？</p>
<p>原因是随着网络越来越深，训练变得原来越难，网络的优化变得越来越难。理论上，越深的网络，效果应该更好；但实际上，由于训练难度，过深的网络会产生退化问题，效果反而不如相对较浅的网络。而残差网络就可以解决这个问题的，残差网络越深，训练集上的效果会越好。（测试集上的效果可能涉及过拟合问题。过拟合问题指的是测试集上的效果和训练集上的效果之间有差距。）<strong>这里要注意到过拟合与退化问题之间的区别</strong></p>
<h3 id="残差块"><a href="#残差块" class="headerlink" title="残差块"></a>残差块</h3><p>残差网络是通过加入shortcut connections,变得更容易被优化。包含一个shortcut connection的几层网络被称为一个残差块。shortcut即图中x到<br>⨁的箭头。</p>
<p><img loading="lazy" src="https://img2018.cnblogs.com/blog/1351564/201809/1351564-20180914164307733-889668964.png" alt="残差块"></p>
<h3 id="残差块（residual-block）"><a href="#残差块（residual-block）" class="headerlink" title="残差块（residual block）"></a>残差块（residual block）</h3><p>𝑥 表示输入，𝐹(𝑥) 表示残差块在第二层激活函数之前的输出，即 𝐹(𝑥)&#x3D;𝑊2𝜎(𝑊1𝑥)，其中 𝑊1 和 𝑊2 表示第一层和第二层的权重，𝜎 表示 ReLU 激活函数。（这里省略了 bias。）最后残差块的输出是 𝜎(𝐹(𝑥)+𝑥)。</p>
<p><strong>激活函数</strong>：上层节点的输出与下层节点的输入之间的函数关系。<br>常用的激活函数有：sigmoid函数，tanh函数，relu函数（这个比较常用）… …</p>
<p>残差块中的网络可以使全连接层也可以是卷积层。</p>
<blockquote>
<p>设第二层网络在激活函数之前的输出为 𝐻(𝑥)。如果在该 2 层网络中，最优的输出就是输入 𝑥，那么对于没有 shortcut connection 的网络，就需要将其优化成 𝐻(𝑥)&#x3D;𝑥；对于有 shortcut connection 的网络，即残差块，最优输出是 𝑥，则只需要将 𝐹(𝑥)&#x3D;𝐻(𝑥)−𝑥 优化为 0 即可。后者的优化会比前者简单。这也是残差这一叫法的由来。</p>
</blockquote>
<p>上面相当于优化了恒等映射。残差网络可以不是神经网络，用全连接层也可以。</p>
<h3 id="为什么残差网络会work"><a href="#为什么残差网络会work" class="headerlink" title="为什么残差网络会work"></a>为什么残差网络会work</h3><p>我们给一个网络不论在中间还是末尾加上一个残差块，并给残差块中的 weights 加上 L2 regularization（weight decay），这样图 1 中 𝐹(𝑥)&#x3D;0 是很容易的。这种情况下加上一个残差块和不加之前的效果会是一样，所以加上残差块不会使得效果变得差。如果残差块中的隐藏单元学到了一些有用信息，那么它可能比 identity mapping（即 𝐹(𝑥)&#x3D;0）表现的更好。</p>
<h2 id="边界框回归"><a href="#边界框回归" class="headerlink" title="边界框回归"></a><a href="https://blog.csdn.net/zijin0802034/article/details/77685438">边界框回归</a></h2><p>对于窗口一般使用四维向量(x,y,w,h) 来表示， 分别表示窗口的中心点坐标和宽高。 对于图 2, 红色的框 P 代表原始的Proposal, 绿色的框 G 代表目标的 Ground Truth， 我们的目标是寻找一种关系使得输入原始的窗口 P 经过映射得到一个跟真实窗口 G 更接近的回归窗口Ĝ。</p>
<p><img loading="lazy" src="https://img-blog.csdn.net/20170831205020797?watermark/2/text/aHR0cDovL2Jsb2cuY3Nkbi5uZXQvemlqaW4wODAyMDM0/font/5a6L5L2T/fontsize/400/fill/I0JBQkFCMA==/dissolve/70/gravity/SouthEast" alt="图2"></p>
<p>边框回归的目的既是：给定(Px,Py,Pw,Ph)(Px,Py,Pw,Ph)寻找一种映射ff， 使得f(Px,Py,Pw,Ph)&#x3D;(Gx^,Gy^,Gw^,Gh^)f(Px,Py,Pw,Ph)&#x3D;(Gx^,Gy^,Gw^,Gh^) 并且(Gx^,Gy^,Gw^,Gh^)≈(Gx,Gy,Gw,Gh)</p>
<p><strong>边框回归的方法：平移+尺度缩放</strong></p>
<h1 id="yolov1-yolov3"><a href="#yolov1-yolov3" class="headerlink" title="yolov1-yolov3"></a><a href="https://blog.csdn.net/xd1723138323/article/details/82532973">yolov1-yolov3</a></h1><h2 id="yolov1"><a href="#yolov1" class="headerlink" title="yolov1"></a>yolov1</h2><h3 id="检测处理理想"><a href="#检测处理理想" class="headerlink" title="检测处理理想"></a>检测处理理想</h3><p>将输入图片划分为S * S的格子（逻辑区域），如果物体的中心坐标落在某个格子中，那么这个格子就负责检测这个物体（包括bounding box的坐标和类别概率）。每个格子预测B个bounding boxes和B个置信度，这个置信度表示这个格子预测的bounding  boxes包含物体的可信程度，论文作者将置信度定义为 Pr(Object)* IOU。Pr(Object)的值为0或者1，表示存不存在目标物体。</p>
<p>每一个bounding box包括五个元素，x，y，w，h，confidence。坐标(x，y)表示的是box的中心坐标，并且是相对于格子的边界而言，w和h也是相对于整张图片的宽度和高度而言。置信度预测的值表示预测框和真实框的IOU值。虽然每个格子预测B个bounding box，但是每个格子只预测一组类条件概率，注意这里不是anchor，因为多个bounding box共用了一组class probability(表示的是<strong>先验概率</strong>)。所以网络架构的最后一层实际预测了B个bouding box和一组类别概率，注意这只是针对于一个格子，所以在不考虑batch size的情况下，实际预测输出长度为S* S*（B*5+C)</p>
<h3 id="训练过程"><a href="#训练过程" class="headerlink" title="训练过程"></a>训练过程</h3><p>对于前面所说bounding box，作者也做了一些处理，将bounding box的w,h除以训练图像的宽度和高度（448*448），使其值在0-1之间。至于bouding box的x,y的实际值意义，作者认为不是相对于整张图片的中心坐标值，而是相对于的对应格子坐标左上边界偏移值，所以x,y的值也在0-1之间，这个地方有些难以理解。</p>
<p>通过置信度为每一个各自从B个bounding box中筛选一个最终的bounding box。于是每个各自的坐标为（0+x，0+y），（0+x, 1+y）…….</p>
<p>通过我们设定的置信度阈值使用非极大值抑制方法从这49个bounding box中筛选；还有一种做法就是直接从98个bounding boxes使用非极大值抑制方法筛选。从图片的全局性来考虑，第一种方法更能体现图片的整体性。确定完bounding boxes后，用bounding box坐标值乘以与原图的比例值就可以在原图中框出相应的物体。</p>
<h2 id="yolov2"><a href="#yolov2" class="headerlink" title="yolov2"></a>yolov2</h2><p>1.对数据进行批标准化处理。</p>
<p>2.High Resolution Classifier:低分辨率训练分类网络，高分辨率训练检测网络，在两个过程见了一个适应性微调。</p>
<p>3.Convolutional With Anchor Boxes（锚框卷积）：作者去掉了YOLOv1的全连接层，使用anchor boxes来预测bounding boxes，同时也去掉了最后池化层使得最后feature maps的分辨率大一些。与YOLOv1不同的是，YOLOv2为每一个bounding box预测一个类条件概率【YOLOv1中B个bounding box共用一个类条件概率】。在YOLOv1中bounding boxes数目为：S* S*(5* B+C)，而在YOLOv2中bounding box数目为S * S*B（5+C）。使用anchor box没有使精度提升，提高了召回率。</p>
<p>4.作者采用维度聚类的方法对数据集的真实标签的bounding box进行聚类分析从而确定B的取值。如果用欧式距离来衡量K-means的距离，会使得大的bounding box比小的bounding box产生更大的误差，于是作者调整距离计算公式为：<br><img loading="lazy" src="https://private.codecogs.com/gif.latex?%5Cdpi%7B120%7D%20%5Csmall%20d(box,centroid)=1-IOU(box,centroid)"></p>
<p>5.13* 13的feature map可以提供足够信息预测较大的物体，但是对于小物体而言提供的信息仍然不够，所以作者提供了一个passthrough层，利用26*26的feature map来预测小物体（可能是受SSD的启发，不同大小的特征图检测不同大小的物体）。</p>
<p>6.YOLOv2的网络只有卷积层和池化层，所以就可以在训练进行的过程改变feature maps的shape。 为了使得YOLOv2更具鲁棒性，我们让模型能够对不同大小的图片进行训练。在训练过程中，每10个batch就换一组新尺度的图片（这里的新图片指大小不同，图片的其他属性是一样的）。</p>
<blockquote>
<p>作者提出了一种将分类数据集和检测数据集联合训练的方法，在训练的过程，我们将这两类数据集混合，当输入的检测数据集时，反向传播全部的误差损失（YOLOv2的loss），当遇到分类数据集时，只反向传播分类误差损失。</p>
</blockquote>
<h2 id="yolov3"><a href="#yolov3" class="headerlink" title="yolov3"></a>yolov3</h2><h3 id="1-边界框预测"><a href="#1-边界框预测" class="headerlink" title="1.边界框预测"></a>1.边界框预测</h3><blockquote>
<p>YOLOv3中引入一个<strong>Objectness score</strong>概念，使用logistic regression为每一个bounding box预测一个Objectness score。将预测的bounding boxes中与真实bounding box重叠最大的bounding box的Objectness score赋值为1，如果这个bounding box与真实bounding box重叠值达到设定的阈值0.5，这个bounding box的损失为0，其同一格子里面的其他bounding box只计算置信度损失，忽略坐标损失和类别损失。(可以认为0.5表示预测的边界框已经能很好地标记框出物体，所以不用计算它的损失。每一个格子中的object由一个预测的bounding box预测。既然已经找到了较好地bounding box，所以同一个格子里面的其他bounding box没有必要计算坐标误差和分类误差，只需要计算置信度误差，用来调整置信度，最理想的效果是这些bounding box的置信度值更新为0）。<br>这里bounding box是指网络模型预测的confidence，bx，by，bw，bh，计算置信度误差时，用真实标签坐标和预测坐标（bx，by，bw，bh）计算IOU，Objectness score相当于Pr(Object)，所以最终的置信度计算为IOU*Pr（Object）</p>
</blockquote>
<h3 id="2-类别预测"><a href="#2-类别预测" class="headerlink" title="2.类别预测"></a>2.类别预测</h3><p>类别预测使用了多标签分类（多分类），没有使用softmax，而是对每一个类各自对立地使用了logistic分类器，用binary cross-entroy loss替代softmax loss，这样能较好地处理标签重叠（包含）关系（例如：女人和人）。</p>
<h3 id="3-交叉尺度-多尺度-预测"><a href="#3-交叉尺度-多尺度-预测" class="headerlink" title="3.交叉尺度(多尺度)预测"></a>3.交叉尺度(多尺度)预测</h3><p>YOLOv3预测三种不同尺度的box，每一种尺度预测三个anchor boxes，即N<em>N</em>(3*(4+1+80))，所以最终的输出是3*【N<em>N</em>(3*(4+1+80))】，最前面的3表示三种尺度。3种尺度，3个anchor box，是由聚类数为9决定的，按照一定的顺序（面积从小到大）将这个聚蔟box分配给不同尺度。在YOLOv2中，为了加强对小物体的检测，引入了passthrough层，假设最后提取的feature map的size是13<em>13，passthrough层的作用是将上层26</em>26的feature map和最后层13*13的feature map连接，作为网络最后的输出。</p>
<p>YOLOv3是采用了低分辨率feature map上采用和高分辨率feature map做融合，形成新的feature map层，对新的feature map层单独做预测。这样就是多尺度预测。如果YOLOv3最后一层也是13<em>13的话，那么三个尺度的大小为（13</em>13，26<em>26，52</em>52），第三个尺度只用第二个尺度上采样并做融合。值得说明的是：YOLOv2中的多尺度指输入图像的大小不同，YOLOv3的多尺度是指用不同的分辨率的feature map做预测。</p>
<hr>
<h2 id="参考文献："><a href="#参考文献：" class="headerlink" title="参考文献："></a>参考文献：</h2><p><a href="https://zhuanlan.zhihu.com/p/24810318">什么是批标准化</a>　　作者：莫烦</p>
<p><a href="https://zhuanlan.zhihu.com/p/21387568">聚类算法</a>　　作者：挖数</p>
<p><a href="https://www.cnblogs.com/wuliytTaotao/p/9560205.html">残差网络</a>　　作者：wuliytTaotao</p>
<p><a href="https://blog.csdn.net/zijin0802034/article/details/77685438">边框回归(Bounding Box Regression)详解</a>　　作者：南有乔木NTU</p>
<p><a href="https://blog.csdn.net/xd1723138323/article/details/82532973">目标检测之YOLO系列-V1至V3改进详解</a>　　作者：xd1723138323</p>
]]></description>
    </item>
    <item>
      <title>Ubuntu笔记</title>
      <link>https://raindrop2508.github.io/2019/06/29/tech/ubuntu_note/</link>
      <guid>https://raindrop2508.github.io/2019/06/29/tech/ubuntu_note/</guid>
      <pubDate>6 月 29 日，2019</pubDate>
      <description><![CDATA[<blockquote>
<p>这篇笔记会根据情况隔段时间更新一次</p>
</blockquote>
<h1 id="终端操作"><a href="#终端操作" class="headerlink" title="终端操作"></a>终端操作</h1><blockquote>
<p>cd+文件夹名  进入文件夹 可以使用TAB键进行自动补全或选择 <br><br>cd   ..( 退出几层就加几个..)  退出到上n层路径<br>ls 显示文件夹中的目录<br>pwd 显示当前路径<br>gedit 使用文本编辑器编辑文件<br>cat 在终端中查看文件（多用做文本文件）<br>sudo  进入管理员模式（超级用户)<br>sudo su 进入根用户<br>nvidia-smi  查看显卡运行状态（A卡没有试过）</p>
</blockquote>
<p>在ubuntu中一些文件被写保护，需要更高级权限才能修改，可以在命令前加sudo  </p>
<h2 id="复制一个文件到指定目录"><a href="#复制一个文件到指定目录" class="headerlink" title="复制一个文件到指定目录"></a>复制一个文件到指定目录</h2><blockquote>
<p>(1) 在桌面上打开终端，输入sudo su<br>(2) 输入密码，就切换到root用户下<br>(3) 切换到桌面 命令输入为 cd 桌面<br>(4) 然后输入复制命令行 cp -r studio.zip &#x2F;home&#x2F;androidstudio<br>(5) 回车 大功告成 可以看看&#x2F;home&#x2F;androidstudio目录下是否有studio.zip  </p>
</blockquote>
<p>作者：飞奔的小付<br>来源：CSDN<br><a href="https://blog.csdn.net/feibendexiaoma/article/details/73739319">原文</a> </p>
<p>移动文件（假设现在在该文件的目录下）</p>
<blockquote>
<p>sudo mv xxx.xx 新的位置的路径  </p>
</blockquote>
<p>解压文件 </p>
<blockquote>
<p>sudo unzip opencv-3.3.0.zip  </p>
</blockquote>
<p>重命名文件 </p>
<blockquote>
<p>sudo cp 原文件名 新文件名  </p>
</blockquote>
<h1 id="软件安装"><a href="#软件安装" class="headerlink" title="软件安装"></a>软件安装</h1><p>软件安装前一般要先更新软件目录</p>
<blockquote>
<p>sudo apt-get update  </p>
</blockquote>
<p>升级已有软件</p>
<blockquote>
<p>sudo apt-get upgrade<br>常用软件安装<br><a href="https://www.jianshu.com/p/f44e1ae080a5">https://www.jianshu.com/p/f44e1ae080a5</a></p>
</blockquote>
<p>下载好网易云音乐安装完成后可能会遇到问题打不开。可以在终端中使用</p>
<blockquote>
<p>sudo netease  TAb(自动补全)  </p>
</blockquote>
<p>当下在好软件安装包后，也可以在本地安装</p>
<blockquote>
<p>sudo sh xxx.sh<br>dpkg -i xxx.deb</p>
</blockquote>
<p><a href="https://jingyan.baidu.com/article/335530da98061b19cb41c31d.html">google浏览器安装</a>  </p>
<h1 id="踩过的坑"><a href="#踩过的坑" class="headerlink" title="踩过的坑"></a>踩过的坑</h1><p><a href="https://blog.csdn.net/sannerlittle/article/details/77479656">没有声音解决办法</a><br>具体情况是：可以调节音量，但却没有声音<br> 这个问题到现在，我仍旧是每次开机仍要打开文章中所说的软件修改一次<br>ubuntu和win双系统中无法访问win中的磁盘分区  </p>
<blockquote>
<p>sudo ntfsfix &#x2F;dev&#x2F;磁盘号<br>如sudo ntfsfix &#x2F;dev&#x2F;sda6</p>
</blockquote>
<p><a href="https://blog.csdn.net/u010801439/article/details/79485914">ubuntu中python2和3共存</a> </p>
<blockquote>
<p>source activate  [要激活的环境名称]<br>source deactivate  </p>
</blockquote>
<p><a href="https://linux.cn/article-5409-1.html">如何修复 apt-get update 无法添加新的 CD-ROM 的错误</a></p>
<p><strong>侵权即删</strong></p>
]]></description>
    </item>
</channel>
</rss>